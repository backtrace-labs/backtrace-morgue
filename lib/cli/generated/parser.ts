// AUTO-GENERATED -- DO NOT EDIT
// Generated by scripts/generate-cli.ts from usage.json

import { Command, Option } from 'commander';
import type {
  CliCommand,
  GlobalOptions,
  LoginCommand,
  LogoutCommand,
  CleanCommand,
  DescribeCommand,
  GetCommand,
  PutCommand,
  SetCommand,
  AttachmentAddCommand,
  AttachmentGetCommand,
  AttachmentListCommand,
  AttachmentDeleteCommand,
  ListCommand,
  DeleteCommand,
  ProjectCreateCommand,
  ProjectsListCommand,
  FlamegraphCommand,
  SymboldStatusCommand,
  SymboldSymbolserverListCommand,
  SymboldSymbolserverDetailsCommand,
  SymboldSymbolserverLogsCommand,
  SymboldSymbolserverLogsFilterCommand,
  SymboldSymbolserverAddCommand,
  SymboldSymbolserverUpdateCommand,
  SymboldSymbolserverDeleteCommand,
  SymboldSymbolserverDisableCommand,
  SymboldSymbolserverEnableCommand,
  SymboldWhitelistAddCommand,
  SymboldWhitelistRemoveCommand,
  SymboldWhitelistListCommand,
  SymboldBlacklistAddCommand,
  SymboldBlacklistRemoveCommand,
  SymboldBlacklistListCommand,
  SymboldSkiplistFindCommand,
  SymboldSkiplistRemoveCommand,
  SymboldSkiplistRemoveAllCommand,
  SymboldSkiplistRemoveFilterCommand,
  SymboldQueueListCommand,
  SymboldQueueAddCommand,
  SymboldQueueSizeCommand,
  SymboldQueueSymbolsCommand,
  ReportListCommand,
  ReportCreateCommand,
  ReportDeleteCommand,
  ReportSendCommand,
  MergeCommand,
  UnmergeCommand,
  RepairCommand,
  ReprocessCommand,
  RetentionListCommand,
  RetentionSetCommand,
  RetentionStatusCommand,
  RetentionClearCommand,
  SamplingStatusCommand,
  SamplingResetCommand,
  SamplingConfigureCommand,
  SymbolCommand,
  ScrubberListCommand,
  ScrubberCreateCommand,
  ScrubberModifyCommand,
  ScrubberDeleteCommand,
  SetupCommand,
  NukeCommand,
  TokenCreateCommand,
  TokenListCommand,
  TokenDeleteCommand,
  UserResetCommand,
  UsersAddSignupWhitelistCommand,
  UsersListTeamlessUsersCommand,
  TenantCreateCommand,
  TenantDeleteCommand,
  TenantListCommand,
  SimilarityCommand,
  InviteCreateCommand,
  InviteListCommand,
  InviteDeleteCommand,
  CallstackEvaluateCommand,
  AccessTeamCreateCommand,
  AccessTeamRemoveCommand,
  AccessTeamDetailsCommand,
  AccessTeamAddUserCommand,
  AccessTeamRemoveUserCommand,
  AccessTeamListCommand,
  AccessProjectAddTeamCommand,
  AccessProjectRemoveTeamCommand,
  AccessProjectAddUserCommand,
  AccessProjectRemoveUserCommand,
  AccessProjectDetailsCommand,
  StabilityCreateMetricCommand,
  MetricsImporterSourceCheckQueryCommand,
  MetricsImporterImporterCreateCommand,
  MetricsImporterLogsCommand,
  AlertsTargetCreateCommand,
  AlertsTargetListCommand,
  AlertsTargetGetCommand,
  AlertsTargetUpdateCommand,
  AlertsTargetDeleteCommand,
  AlertsAlertListCommand,
  AlertsAlertGetCommand,
  AlertsAlertUpdateCommand,
  AlertsAlertDeleteCommand,
  AlertsAlertCreateCommand,
  WorkflowsConnectionCreateCommand,
  WorkflowsConnectionListCommand,
  WorkflowsConnectionGetCommand,
  WorkflowsConnectionUpdateCommand,
  WorkflowsConnectionDeleteCommand,
  WorkflowsIntegrationCreateCommand,
  WorkflowsIntegrationListCommand,
  WorkflowsIntegrationGetCommand,
  WorkflowsIntegrationUpdateCommand,
  WorkflowsIntegrationDeleteCommand,
  WorkflowsAlertListCommand,
  WorkflowsAlertGetCommand,
  WorkflowsAlertCreateCommand,
  WorkflowsAlertUpdateCommand,
  WorkflowsAlertDeleteCommand,
  ActionsGetCommand,
  ActionsDisableCommand,
  ActionsEnableCommand,
  ActionsUploadCommand,
  ActionsDeleteCommand,
  AttributeCreateCommand,
  AttributeDeleteCommand,
  ViewCreateCommand,
  ViewDeleteCommand,
  ErrorCommand,
  LogListCommand,
  LogActivateCommand,
  LogDeactivateCommand,
  LogExtractCommand,
  AuditExtractCommand,
  BpgListCommand,
  ControlCommand,
  DeduplicationCommand,
  DeduplicationAddCommand,
  DeduplicationDeleteCommand,
  DeduplicationModifyCommand,
  DeduplicationListCommand,
  LatencyListCommand,
  LatencyActivateCommand,
  LatencyDeactivateCommand,
  LatencyExtractCommand,
  CtsCommand,
  SessionListCommand,
  SessionSetCommand,
  SessionUnsetCommand,
  LimitListCommand,
  LimitResetCommand,
  LimitDeleteCommand,
  LimitCreateCommand,
  ServiceListCommand,
  ServiceStatusCommand,
  ServiceRescanCommand,
  StatusReloadCommand,
} from './types';

const revealHidden = !!process.env.MORGUE_REVEAL_HIDDEN;

function collectRepeatable(val: string, prev: string[]): string[] {
  return prev.concat([val]);
}

function extractGlobalOptions(cmd: Command): GlobalOptions {
  const opts = cmd.optsWithGlobals();
  return {
    endpoint: opts['endpoint'],
    token: opts['token'],
    universe: opts['universe'],
    project: opts['project'],
    timeout: opts['timeout'],
    debug: opts['debug'] ?? false,
    k: opts['k'] ?? false,
  };
}

export function createProgram(): { program: Command; getResult: () => CliCommand | undefined } {
  let result: CliCommand | undefined;
  const program = new Command('morgue');
  program.allowExcessArguments(true);

  program.option('--endpoint <endpoint>', 'Specify API endpoint');
  program.option('--token <token>', 'Specify API token');
  program.option('--universe <universe>', 'Universe name');
  program.option('--project <project>', 'Project name');
  program.option('--timeout <timeout>', 'Set the timeout on API requests in milliseconds');
  program.option('--debug', 'Enable verbose debug printing', false);
  program.option('-k', 'Disable SSL verification with CA', false);

  // --- login ---
  program.command('login')
    .argument('<url>', 'url')
    .action((url, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'login',
        globalOptions: g,
        url,
      } satisfies LoginCommand;
    });

  // --- logout ---
  program.command('logout')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'logout',
        globalOptions: g,
      } satisfies LogoutCommand;
    });

  // --- clean ---
  program.command('clean')
    .argument('<project>', 'project')
    .option('--filter <filter>', 'Filter expression: attribute,operation,value')
    .option('--limit <limit>', 'Number of rows to return')
    .option('--offset <offset>', 'Skip first N rows')
    .option('--select <select>', 'Select specific attributes', collectRepeatable, [])
    .addOption(new Option('--select-wildcard <select-wildcard>', 'Select all attributes of the given type').choices(["physical","derived","virtual"]))
    .option('--age <age>', 'Relative timestamp (e.g., 1h, 42d, 10s, etc). Valid suffixes: y (years), M (months), w (weeks), d (days), h (hours), m (minutes), s (seconds)')
    .option('--time <time>', 'Time range using natural language')
    .option('--unique <unique>', 'Count distinct values')
    .option('--histogram <histogram>', 'Show all distinct values')
    .option('--distribution <distribution>', 'Show truncated histogram')
    .option('--mean <mean>', 'Calculate mean')
    .option('--sum <sum>', 'Sum all values')
    .option('--range <range>', 'Show min and max values')
    .option('--count <count>', 'Count non-null values')
    .option('--bin <bin>', 'Linear histogram of values')
    .option('--head <head>', 'First value in factor')
    .option('--tail <tail>', 'Last value in factor')
    .option('--object <object>', 'Maximum object identifier')
    .addOption(new Option('--last <last>', 'Last object identifier').hideHelp(!revealHidden))
    .addOption(new Option('--first <first>', 'First object identifier').hideHelp(!revealHidden))
    .addOption(new Option('--min <min>', 'Min object identifier').hideHelp(!revealHidden))
    .addOption(new Option('--max <max>', 'Max object identifier').hideHelp(!revealHidden))
    .option('--sort <sort>', 'Sort results: [-](column|fold_term)', collectRepeatable, [])
    .option('--quantize-uint <quantize-uint>', 'Compute quantized column: output_column,input_column,size[,offset]', collectRepeatable, [])
    .addOption(new Option('--raw-query <raw-query>', 'Raw query to use (overrides all other flags)').hideHelp(!revealHidden))
    .addOption(new Option('--table <table>', 'Table to query against').hideHelp(!revealHidden))
    .addOption(new Option('--timestamp-attribute <timestamp-attribute>', 'Attribute to use as timestamp').hideHelp(!revealHidden))
    .option('--reverse <reverse>', 'Reverse sort order')
    .option('--template <template>', 'TODO: (ex: select)')
    .option('--factor <factor>', 'Group by attribute')
    .option('--fingerprint <fingerprint>', 'Filter by an exact fingerprint, or a regex anchored to the beginning of the fingerprint')
    .option('--keep <keep>', 'Number of objects to keep per fingerprint')
    .option('--oldest <oldest>', 'Keep N oldest objects')
    .option('--output', 'Output object IDs to stdout', false)
    .option('--verbose', 'Verbose output', false)
    .action((project, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'clean',
        globalOptions: g,
        queryOptions: {
          filter: opts['filter'],
          limit: opts['limit'],
          offset: opts['offset'],
          select: opts['select'],
          selectWildcard: opts['selectWildcard'],
          age: opts['age'],
          time: opts['time'],
          unique: opts['unique'],
          histogram: opts['histogram'],
          distribution: opts['distribution'],
          mean: opts['mean'],
          sum: opts['sum'],
          range: opts['range'],
          count: opts['count'],
          bin: opts['bin'],
          head: opts['head'],
          tail: opts['tail'],
          object: opts['object'],
          last: opts['last'],
          first: opts['first'],
          min: opts['min'],
          max: opts['max'],
          sort: opts['sort'],
          quantizeUint: opts['quantizeUint'],
          rawQuery: opts['rawQuery'],
          table: opts['table'],
          timestampAttribute: opts['timestampAttribute'],
          reverse: opts['reverse'],
          template: opts['template'],
          factor: opts['factor'],
          fingerprint: opts['fingerprint'],
        },
        project,
        keep: opts['keep'],
        oldest: opts['oldest'],
        output: opts['output'],
        verbose: opts['verbose'],
      } satisfies CleanCommand;
    });

  // --- describe ---
  program.command('describe')
    .argument('<project>', 'project')
    .argument('[substring]', 'substring')
    .option('-r', 'Show disabled attributes', false)
    .option('-u', 'Show only custom attributes', false)
    .option('-a', 'Show all attributes including unused', false)
    .option('-l', 'Show filter and aggregation info', false)
    .option('--json', 'Output JSON format', false)
    .addOption(new Option('--table <table>', 'Table to query against').hideHelp(!revealHidden))
    .action((project, substring, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'describe',
        globalOptions: g,
        project,
        substring,
        r: opts['r'],
        u: opts['u'],
        a: opts['a'],
        l: opts['l'],
        json: opts['json'],
        table: opts['table'],
      } satisfies DescribeCommand;
    });

  // --- get ---
  program.command('get')
    .argument('<project>', 'project')
    .argument('<object_id>', 'object_id')
    .option('-o, --output <output>', 'Output to file')
    .option('--resource <resource>', 'Fetch specific resource')
    .addOption(new Option('--outdir <outdir>', 'Output directory for multiple objects').hideHelp(!revealHidden))
    .addOption(new Option('--first <first>', 'First object ID in range').hideHelp(!revealHidden))
    .addOption(new Option('--last <last>', 'Last object ID in range').hideHelp(!revealHidden))
    .action((project, object_id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'get',
        globalOptions: g,
        project,
        object_id,
        output: opts['output'],
        resource: opts['resource'],
        outdir: opts['outdir'],
        first: opts['first'],
        last: opts['last'],
      } satisfies GetCommand;
    });

  // --- put ---
  program.command('put')
    .argument('<project>', 'project')
    .argument('<file>', 'file')
    .addOption(new Option('--format <format>', 'File format').choices(["btt","minidump","json","plcrash","symbols","symbols-proguard","sourcemap"]))
    .addOption(new Option('--compression <compression>', 'Compression: gzip|deflate').choices(["gzip","deflate"]))
    .option('--kv <kv>', 'Key-value pairs: key1:value1,key2:value2')
    .option('--form_data', 'Upload using multipart/form-data POST request', false)
    .addOption(new Option('--tag <tag>', 'Symbol tag (used with symbols format)').hideHelp(!revealHidden))
    .addOption(new Option('--symbolication-id <symbolication-id>', 'Symbolication identifier').hideHelp(!revealHidden))
    .addOption(new Option('--sync', 'Synchronous submission').default(false).hideHelp(!revealHidden))
    .addOption(new Option('--reuse', 'Reuse HTTP connections').default(false).hideHelp(!revealHidden))
    .addOption(new Option('--attachment <attachment>', 'Attachment file(s) for minidump uploads').hideHelp(!revealHidden))
    .addOption(new Option('--multipart', 'Force multipart form submission').default(false).hideHelp(!revealHidden))
    .addOption(new Option('--benchmark', 'Run submission benchmark').default(false).hideHelp(!revealHidden))
    .addOption(new Option('--samples <samples>', 'Number of benchmark samples').hideHelp(!revealHidden))
    .addOption(new Option('--concurrency <concurrency>', 'Benchmark concurrency level').hideHelp(!revealHidden))
    .addOption(new Option('--printids', 'Print object IDs after upload').default(false).hideHelp(!revealHidden))
    .action((project, file, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'put',
        globalOptions: g,
        project,
        file,
        format: opts['format'],
        compression: opts['compression'],
        kv: opts['kv'],
        form_data: opts['form_data'],
        tag: opts['tag'],
        symbolicationId: opts['symbolicationId'],
        sync: opts['sync'],
        reuse: opts['reuse'],
        attachment: opts['attachment'],
        multipart: opts['multipart'],
        benchmark: opts['benchmark'],
        samples: opts['samples'],
        concurrency: opts['concurrency'],
        printids: opts['printids'],
      } satisfies PutCommand;
    });

  // --- set ---
  program.command('set')
    .argument('<project>', 'project')
    .argument('<query>', 'query')
    .argument('<assignment>', 'assignment')
    .option('--filter <filter>', 'Filter expression: attribute,operation,value')
    .option('--limit <limit>', 'Number of rows to return')
    .option('--offset <offset>', 'Skip first N rows')
    .option('--select <select>', 'Select specific attributes', collectRepeatable, [])
    .addOption(new Option('--select-wildcard <select-wildcard>', 'Select all attributes of the given type').choices(["physical","derived","virtual"]))
    .option('--age <age>', 'Relative timestamp (e.g., 1h, 42d, 10s, etc). Valid suffixes: y (years), M (months), w (weeks), d (days), h (hours), m (minutes), s (seconds)')
    .option('--time <time>', 'Time range using natural language')
    .option('--unique <unique>', 'Count distinct values')
    .option('--histogram <histogram>', 'Show all distinct values')
    .option('--distribution <distribution>', 'Show truncated histogram')
    .option('--mean <mean>', 'Calculate mean')
    .option('--sum <sum>', 'Sum all values')
    .option('--range <range>', 'Show min and max values')
    .option('--count <count>', 'Count non-null values')
    .option('--bin <bin>', 'Linear histogram of values')
    .option('--head <head>', 'First value in factor')
    .option('--tail <tail>', 'Last value in factor')
    .option('--object <object>', 'Maximum object identifier')
    .addOption(new Option('--last <last>', 'Last object identifier').hideHelp(!revealHidden))
    .addOption(new Option('--first <first>', 'First object identifier').hideHelp(!revealHidden))
    .addOption(new Option('--min <min>', 'Min object identifier').hideHelp(!revealHidden))
    .addOption(new Option('--max <max>', 'Max object identifier').hideHelp(!revealHidden))
    .option('--sort <sort>', 'Sort results: [-](column|fold_term)', collectRepeatable, [])
    .option('--quantize-uint <quantize-uint>', 'Compute quantized column: output_column,input_column,size[,offset]', collectRepeatable, [])
    .addOption(new Option('--raw-query <raw-query>', 'Raw query to use (overrides all other flags)').hideHelp(!revealHidden))
    .addOption(new Option('--table <table>', 'Table to query against').hideHelp(!revealHidden))
    .addOption(new Option('--timestamp-attribute <timestamp-attribute>', 'Attribute to use as timestamp').hideHelp(!revealHidden))
    .option('--reverse <reverse>', 'Reverse sort order')
    .option('--template <template>', 'TODO: (ex: select)')
    .option('--factor <factor>', 'Group by attribute')
    .option('--fingerprint <fingerprint>', 'Filter by an exact fingerprint, or a regex anchored to the beginning of the fingerprint')
    .action((project, query, assignment, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'set',
        globalOptions: g,
        queryOptions: {
          filter: opts['filter'],
          limit: opts['limit'],
          offset: opts['offset'],
          select: opts['select'],
          selectWildcard: opts['selectWildcard'],
          age: opts['age'],
          time: opts['time'],
          unique: opts['unique'],
          histogram: opts['histogram'],
          distribution: opts['distribution'],
          mean: opts['mean'],
          sum: opts['sum'],
          range: opts['range'],
          count: opts['count'],
          bin: opts['bin'],
          head: opts['head'],
          tail: opts['tail'],
          object: opts['object'],
          last: opts['last'],
          first: opts['first'],
          min: opts['min'],
          max: opts['max'],
          sort: opts['sort'],
          quantizeUint: opts['quantizeUint'],
          rawQuery: opts['rawQuery'],
          table: opts['table'],
          timestampAttribute: opts['timestampAttribute'],
          reverse: opts['reverse'],
          template: opts['template'],
          factor: opts['factor'],
          fingerprint: opts['fingerprint'],
        },
        project,
        query,
        assignment,
      } satisfies SetCommand;
    });

  // --- attachment ---
  const cmd_attachment = program.command('attachment');
  cmd_attachment.command('add')
    .argument('<project>', 'project')
    .argument('<oid>', 'oid')
    .argument('<filename>', 'filename')
    .option('--content-type <content-type>', 'Content-Type for attachment')
    .option('--attachment-name <attachment-name>', 'Attachment name')
    .action((project, oid, filename, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'attachment.add',
        globalOptions: g,
        project,
        oid,
        filename,
        contentType: opts['contentType'],
        attachmentName: opts['attachmentName'],
      } satisfies AttachmentAddCommand;
    });
  cmd_attachment.command('get')
    .argument('<project>', 'project')
    .argument('<oid>', 'oid')
    .option('--attachment-id <attachment-id>', 'Attachment ID')
    .option('--attachment-name <attachment-name>', 'Attachment name')
    .action((project, oid, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'attachment.get',
        globalOptions: g,
        project,
        oid,
        attachmentId: opts['attachmentId'],
        attachmentName: opts['attachmentName'],
      } satisfies AttachmentGetCommand;
    });
  cmd_attachment.command('list')
    .argument('<project>', 'project')
    .argument('<oid>', 'oid')
    .action((project, oid, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'attachment.list',
        globalOptions: g,
        project,
        oid,
      } satisfies AttachmentListCommand;
    });
  cmd_attachment.command('delete')
    .argument('<project>', 'project')
    .argument('<oid>', 'oid')
    .option('--attachment-id <attachment-id>', 'Attachment ID')
    .option('--attachment-name <attachment-name>', 'Attachment name')
    .action((project, oid, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'attachment.delete',
        globalOptions: g,
        project,
        oid,
        attachmentId: opts['attachmentId'],
        attachmentName: opts['attachmentName'],
      } satisfies AttachmentDeleteCommand;
    });

  // --- list ---
  program.command('list')
    .argument('<project>', 'project')
    .argument('[substring]', 'substring')
    .option('--filter <filter>', 'Filter expression: attribute,operation,value')
    .option('--limit <limit>', 'Number of rows to return')
    .option('--offset <offset>', 'Skip first N rows')
    .option('--select <select>', 'Select specific attributes', collectRepeatable, [])
    .addOption(new Option('--select-wildcard <select-wildcard>', 'Select all attributes of the given type').choices(["physical","derived","virtual"]))
    .option('--age <age>', 'Relative timestamp (e.g., 1h, 42d, 10s, etc). Valid suffixes: y (years), M (months), w (weeks), d (days), h (hours), m (minutes), s (seconds)')
    .option('--time <time>', 'Time range using natural language')
    .option('--unique <unique>', 'Count distinct values')
    .option('--histogram <histogram>', 'Show all distinct values')
    .option('--distribution <distribution>', 'Show truncated histogram')
    .option('--mean <mean>', 'Calculate mean')
    .option('--sum <sum>', 'Sum all values')
    .option('--range <range>', 'Show min and max values')
    .option('--count <count>', 'Count non-null values')
    .option('--bin <bin>', 'Linear histogram of values')
    .option('--head <head>', 'First value in factor')
    .option('--tail <tail>', 'Last value in factor')
    .option('--object <object>', 'Maximum object identifier')
    .addOption(new Option('--last <last>', 'Last object identifier').hideHelp(!revealHidden))
    .addOption(new Option('--first <first>', 'First object identifier').hideHelp(!revealHidden))
    .addOption(new Option('--min <min>', 'Min object identifier').hideHelp(!revealHidden))
    .addOption(new Option('--max <max>', 'Max object identifier').hideHelp(!revealHidden))
    .option('--sort <sort>', 'Sort results: [-](column|fold_term)', collectRepeatable, [])
    .option('--quantize-uint <quantize-uint>', 'Compute quantized column: output_column,input_column,size[,offset]', collectRepeatable, [])
    .addOption(new Option('--raw-query <raw-query>', 'Raw query to use (overrides all other flags)').hideHelp(!revealHidden))
    .addOption(new Option('--table <table>', 'Table to query against').hideHelp(!revealHidden))
    .addOption(new Option('--timestamp-attribute <timestamp-attribute>', 'Attribute to use as timestamp').hideHelp(!revealHidden))
    .option('--reverse <reverse>', 'Reverse sort order')
    .option('--template <template>', 'TODO: (ex: select)')
    .option('--factor <factor>', 'Group by attribute')
    .option('--fingerprint <fingerprint>', 'Filter by an exact fingerprint, or a regex anchored to the beginning of the fingerprint')
    .option('--csv <csv>', 'Output to CSV file')
    .option('--json', 'Output JSON format', false)
    .option('--query', 'Show the built query JSON and exit without performing the request', false)
    .option('--verbose', 'Show detailed query performance data', false)
    .addOption(new Option('--raw', 'Output raw JSON response (use with --query to also execute)').default(false).hideHelp(!revealHidden))
    .addOption(new Option('--set <set>', 'Set attributes on matching objects: key=value').hideHelp(!revealHidden))
    .addOption(new Option('--clear <clear>', 'Clear attributes on matching objects').hideHelp(!revealHidden))
    .addOption(new Option('--implicit-filters <implicit-filters>', 'Control implicit time filters (default: true)').hideHelp(!revealHidden))
    .addOption(new Option('--benchmark', 'Run query benchmark').default(false).hideHelp(!revealHidden))
    .addOption(new Option('--concurrency <concurrency>', 'Benchmark concurrency level').hideHelp(!revealHidden))
    .addOption(new Option('--samples <samples>', 'Number of benchmark samples').hideHelp(!revealHidden))
    .action((project, substring, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'list',
        globalOptions: g,
        queryOptions: {
          filter: opts['filter'],
          limit: opts['limit'],
          offset: opts['offset'],
          select: opts['select'],
          selectWildcard: opts['selectWildcard'],
          age: opts['age'],
          time: opts['time'],
          unique: opts['unique'],
          histogram: opts['histogram'],
          distribution: opts['distribution'],
          mean: opts['mean'],
          sum: opts['sum'],
          range: opts['range'],
          count: opts['count'],
          bin: opts['bin'],
          head: opts['head'],
          tail: opts['tail'],
          object: opts['object'],
          last: opts['last'],
          first: opts['first'],
          min: opts['min'],
          max: opts['max'],
          sort: opts['sort'],
          quantizeUint: opts['quantizeUint'],
          rawQuery: opts['rawQuery'],
          table: opts['table'],
          timestampAttribute: opts['timestampAttribute'],
          reverse: opts['reverse'],
          template: opts['template'],
          factor: opts['factor'],
          fingerprint: opts['fingerprint'],
        },
        project,
        substring,
        csv: opts['csv'],
        json: opts['json'],
        query: opts['query'],
        verbose: opts['verbose'],
        raw: opts['raw'],
        set: opts['set'],
        clear: opts['clear'],
        implicitFilters: opts['implicitFilters'],
        benchmark: opts['benchmark'],
        concurrency: opts['concurrency'],
        samples: opts['samples'],
      } satisfies ListCommand;
    });

  // --- delete ---
  program.command('delete')
    .argument('<project>', 'project')
    .argument('[target...]', 'target')
    .option('--filter <filter>', 'Filter expression: attribute,operation,value')
    .option('--limit <limit>', 'Number of rows to return')
    .option('--offset <offset>', 'Skip first N rows')
    .option('--select <select>', 'Select specific attributes', collectRepeatable, [])
    .addOption(new Option('--select-wildcard <select-wildcard>', 'Select all attributes of the given type').choices(["physical","derived","virtual"]))
    .option('--age <age>', 'Relative timestamp (e.g., 1h, 42d, 10s, etc). Valid suffixes: y (years), M (months), w (weeks), d (days), h (hours), m (minutes), s (seconds)')
    .option('--time <time>', 'Time range using natural language')
    .option('--unique <unique>', 'Count distinct values')
    .option('--histogram <histogram>', 'Show all distinct values')
    .option('--distribution <distribution>', 'Show truncated histogram')
    .option('--mean <mean>', 'Calculate mean')
    .option('--sum <sum>', 'Sum all values')
    .option('--range <range>', 'Show min and max values')
    .option('--count <count>', 'Count non-null values')
    .option('--bin <bin>', 'Linear histogram of values')
    .option('--head <head>', 'First value in factor')
    .option('--tail <tail>', 'Last value in factor')
    .option('--object <object>', 'Maximum object identifier')
    .addOption(new Option('--last <last>', 'Last object identifier').hideHelp(!revealHidden))
    .addOption(new Option('--first <first>', 'First object identifier').hideHelp(!revealHidden))
    .addOption(new Option('--min <min>', 'Min object identifier').hideHelp(!revealHidden))
    .addOption(new Option('--max <max>', 'Max object identifier').hideHelp(!revealHidden))
    .option('--sort <sort>', 'Sort results: [-](column|fold_term)', collectRepeatable, [])
    .option('--quantize-uint <quantize-uint>', 'Compute quantized column: output_column,input_column,size[,offset]', collectRepeatable, [])
    .addOption(new Option('--raw-query <raw-query>', 'Raw query to use (overrides all other flags)').hideHelp(!revealHidden))
    .addOption(new Option('--table <table>', 'Table to query against').hideHelp(!revealHidden))
    .addOption(new Option('--timestamp-attribute <timestamp-attribute>', 'Attribute to use as timestamp').hideHelp(!revealHidden))
    .option('--reverse <reverse>', 'Reverse sort order')
    .option('--template <template>', 'TODO: (ex: select)')
    .option('--factor <factor>', 'Group by attribute')
    .option('--fingerprint <fingerprint>', 'Filter by an exact fingerprint, or a regex anchored to the beginning of the fingerprint')
    .option('--physical-only', 'Delete only physical data (default)', false)
    .option('--crdb-only', 'Delete only indexed data', false)
    .option('--all', 'Delete all related data', false)
    .addOption(new Option('--sync', 'Synchronous deletion').default(false).hideHelp(!revealHidden))
    .addOption(new Option('--chunklen <chunklen>', 'Batch size for deletions').hideHelp(!revealHidden))
    .action((project, target, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'delete',
        globalOptions: g,
        queryOptions: {
          filter: opts['filter'],
          limit: opts['limit'],
          offset: opts['offset'],
          select: opts['select'],
          selectWildcard: opts['selectWildcard'],
          age: opts['age'],
          time: opts['time'],
          unique: opts['unique'],
          histogram: opts['histogram'],
          distribution: opts['distribution'],
          mean: opts['mean'],
          sum: opts['sum'],
          range: opts['range'],
          count: opts['count'],
          bin: opts['bin'],
          head: opts['head'],
          tail: opts['tail'],
          object: opts['object'],
          last: opts['last'],
          first: opts['first'],
          min: opts['min'],
          max: opts['max'],
          sort: opts['sort'],
          quantizeUint: opts['quantizeUint'],
          rawQuery: opts['rawQuery'],
          table: opts['table'],
          timestampAttribute: opts['timestampAttribute'],
          reverse: opts['reverse'],
          template: opts['template'],
          factor: opts['factor'],
          fingerprint: opts['fingerprint'],
        },
        project,
        target,
        physicalOnly: opts['physicalOnly'],
        crdbOnly: opts['crdbOnly'],
        all: opts['all'],
        sync: opts['sync'],
        chunklen: opts['chunklen'],
      } satisfies DeleteCommand;
    });

  // --- project ---
  const cmd_project = program.command('project');
  cmd_project.command('create')
    .argument('<name>', 'name')
    .action((name, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'project.create',
        globalOptions: g,
        name,
      } satisfies ProjectCreateCommand;
    });

  // --- projects ---
  const cmd_projects = program.command('projects');
  cmd_projects.command('list')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'projects.list',
        globalOptions: g,
      } satisfies ProjectsListCommand;
    });

  // --- flamegraph ---
  program.command('flamegraph')
    .argument('<project>', 'project')
    .option('-o, --output <output>', 'Output SVG file')
    .option('--unique', 'Sample only unique crashes', false)
    .option('--reverse', 'Sample from leaf functions', false)
    .option('--filter <filter>', 'Filter expression: attribute,operation,value')
    .option('--limit <limit>', 'Number of rows to return')
    .option('--offset <offset>', 'Skip first N rows')
    .option('--select <select>', 'Select specific attributes', collectRepeatable, [])
    .addOption(new Option('--select-wildcard <select-wildcard>', 'Select all attributes of the given type').choices(["physical","derived","virtual"]))
    .option('--age <age>', 'Relative timestamp (e.g., 1h, 42d, 10s, etc). Valid suffixes: y (years), M (months), w (weeks), d (days), h (hours), m (minutes), s (seconds)')
    .option('--time <time>', 'Time range using natural language')
    .option('--sort <sort>', 'Sort results: [-](column|fold_term)', collectRepeatable, [])
    .option('--quantize-uint <quantize-uint>', 'Compute quantized column: output_column,input_column,size[,offset]', collectRepeatable, [])
    .addOption(new Option('--raw-query <raw-query>', 'Raw query to use (overrides all other flags)').hideHelp(!revealHidden))
    .addOption(new Option('--table <table>', 'Table to query against').hideHelp(!revealHidden))
    .addOption(new Option('--timestamp-attribute <timestamp-attribute>', 'Attribute to use as timestamp').hideHelp(!revealHidden))
    .option('--template <template>', 'TODO: (ex: select)')
    .option('--factor <factor>', 'Group by attribute')
    .option('--fingerprint <fingerprint>', 'Filter by an exact fingerprint, or a regex anchored to the beginning of the fingerprint')
    .action((project, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'flamegraph',
        globalOptions: g,
        project,
        output: opts['output'],
        unique: opts['unique'],
        reverse: opts['reverse'],
        filter: opts['filter'],
        limit: opts['limit'],
        offset: opts['offset'],
        select: opts['select'],
        selectWildcard: opts['selectWildcard'],
        age: opts['age'],
        time: opts['time'],
        sort: opts['sort'],
        quantizeUint: opts['quantizeUint'],
        rawQuery: opts['rawQuery'],
        table: opts['table'],
        timestampAttribute: opts['timestampAttribute'],
        template: opts['template'],
        factor: opts['factor'],
        fingerprint: opts['fingerprint'],
      } satisfies FlamegraphCommand;
    });

  // --- symbold ---
  const cmd_symbold = program.command('symbold');
  cmd_symbold.command('status')
    .argument('<project>', 'project')
    .action((project, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.status',
        globalOptions: g,
        project,
      } satisfies SymboldStatusCommand;
    });
  const cmd_symbold_symbolserver = cmd_symbold.command('symbolserver');
  cmd_symbold_symbolserver.command('list')
    .argument('<project>', 'project')
    .option('--page <page>', 'Page number')
    .option('--take <take>', 'Items per page')
    .action((project, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.symbolserver.list',
        globalOptions: g,
        project,
        page: opts['page'],
        take: opts['take'],
      } satisfies SymboldSymbolserverListCommand;
    });
  cmd_symbold_symbolserver.command('details')
    .argument('<id>', 'id')
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.symbolserver.details',
        globalOptions: g,
        id,
      } satisfies SymboldSymbolserverDetailsCommand;
    });
  const cmd_symbold_symbolserver_logs = cmd_symbold_symbolserver.command('logs');
  cmd_symbold_symbolserver_logs
    .argument('<id>', 'id')
    .option('--take <take>', 'Number of logs')
    .option('--page <page>', 'Page number')
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.symbolserver.logs',
        globalOptions: g,
        id,
        take: opts['take'],
        page: opts['page'],
      } satisfies SymboldSymbolserverLogsCommand;
    });
  cmd_symbold_symbolserver.command('add')
    .argument('<project>', 'project')
    .argument('<url>', 'url')
    .option('--name <name>', 'Server name')
    .option('--concurrentdownload <concurrentdownload>', 'Max concurrent downloads')
    .option('--retrylimit <retrylimit>', 'Retry limit')
    .option('--timeout <timeout>', 'Download timeout')
    .addOption(new Option('--whitelist <whitelist>', 'Use whitelist').choices(["true","false"]))
    .addOption(new Option('--retain <retain>', 'Retain original symbols').choices(["true","false"]))
    .option('--servercredentials.username <servercredentials.username>', 'Symbol server auth user name')
    .option('--servercredentials.password <servercredentials.password>', 'Symbol server auth password')
    .option('--aws.accesskey <aws.accesskey>', 'AWS S3 access key')
    .option('--aws.secret <aws.secret>', 'AWS S3 secret')
    .option('--aws.bucketname <aws.bucketname>', 'AWS S3 bucket name')
    .addOption(new Option('--aws.lowerfile <aws.lowerfile>', 'Determine if symbold should use lower case symbol name').choices(["true","false"]))
    .addOption(new Option('--aws.lowerid <aws.lowerid>', 'Determine if symbold should use lower case debug id').choices(["true","false"]))
    .addOption(new Option('--aws.usepdb <aws.usepdb>', 'Determine a way to generate url to S3 symbols').choices(["true","false"]))
    .option('--proxy.host <proxy.host>', 'Proxy host')
    .option('--proxy.port <proxy.port>', 'Proxy port')
    .option('--proxy.username <proxy.username>', 'Proxy username')
    .option('--proxy.password <proxy.password>', 'Proxy password')
    .addOption(new Option('--force <force>', 'Force creation').choices(["true","false"]))
    .option('--retrytimeout <retrytimeout>', 'Download retry timeout')
    .action((project, url, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.symbolserver.add',
        globalOptions: g,
        project,
        url,
        name: opts['name'],
        concurrentdownload: opts['concurrentdownload'],
        retrylimit: opts['retrylimit'],
        timeout: opts['timeout'],
        whitelist: opts['whitelist'],
        retain: opts['retain'],
        servercredentialsUsername: opts['servercredentialsUsername'],
        servercredentialsPassword: opts['servercredentialsPassword'],
        awsAccesskey: opts['awsAccesskey'],
        awsSecret: opts['awsSecret'],
        awsBucketname: opts['awsBucketname'],
        awsLowerfile: opts['awsLowerfile'],
        awsLowerid: opts['awsLowerid'],
        awsUsepdb: opts['awsUsepdb'],
        proxyHost: opts['proxyHost'],
        proxyPort: opts['proxyPort'],
        proxyUsername: opts['proxyUsername'],
        proxyPassword: opts['proxyPassword'],
        force: opts['force'],
        retrytimeout: opts['retrytimeout'],
      } satisfies SymboldSymbolserverAddCommand;
    });
  cmd_symbold_symbolserver.command('update')
    .argument('<id>', 'id')
    .option('--name <name>', 'Server name')
    .option('--symbolServerUrl <symbolServerUrl>', 'Symbol server URL')
    .option('--concurrentdownload <concurrentdownload>', 'Max concurrent downloads')
    .option('--retrylimit <retrylimit>', 'Retry limit')
    .option('--timeout <timeout>', 'Download timeout')
    .option('--retrytimeout <retrytimeout>', 'Download retry timeout')
    .addOption(new Option('--whitelist <whitelist>', 'Use whitelist').choices(["true","false"]))
    .addOption(new Option('--retain <retain>', 'Retain original symbols').choices(["true","false"]))
    .option('--servercredentials.username <servercredentials.username>', 'Symbol server auth user name')
    .option('--servercredentials.password <servercredentials.password>', 'Symbol server auth password')
    .option('--aws.accesskey <aws.accesskey>', 'AWS S3 access key')
    .option('--aws.secret <aws.secret>', 'AWS S3 secret')
    .option('--aws.bucketname <aws.bucketname>', 'AWS S3 bucket name')
    .addOption(new Option('--aws.lowerfile <aws.lowerfile>', 'Determine if symbold should use lower case symbol name').choices(["true","false"]))
    .addOption(new Option('--aws.lowerid <aws.lowerid>', 'Determine if symbold should use lower case debug id').choices(["true","false"]))
    .addOption(new Option('--aws.usepdb <aws.usepdb>', 'Determine a way to generate url to S3 symbols').choices(["true","false"]))
    .option('--proxy.host <proxy.host>', 'Proxy host')
    .option('--proxy.port <proxy.port>', 'Proxy port')
    .option('--proxy.username <proxy.username>', 'Proxy username')
    .option('--proxy.password <proxy.password>', 'Proxy password')
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.symbolserver.update',
        globalOptions: g,
        id,
        name: opts['name'],
        symbolServerUrl: opts['symbolServerUrl'],
        concurrentdownload: opts['concurrentdownload'],
        retrylimit: opts['retrylimit'],
        timeout: opts['timeout'],
        retrytimeout: opts['retrytimeout'],
        whitelist: opts['whitelist'],
        retain: opts['retain'],
        servercredentialsUsername: opts['servercredentialsUsername'],
        servercredentialsPassword: opts['servercredentialsPassword'],
        awsAccesskey: opts['awsAccesskey'],
        awsSecret: opts['awsSecret'],
        awsBucketname: opts['awsBucketname'],
        awsLowerfile: opts['awsLowerfile'],
        awsLowerid: opts['awsLowerid'],
        awsUsepdb: opts['awsUsepdb'],
        proxyHost: opts['proxyHost'],
        proxyPort: opts['proxyPort'],
        proxyUsername: opts['proxyUsername'],
        proxyPassword: opts['proxyPassword'],
      } satisfies SymboldSymbolserverUpdateCommand;
    });
  cmd_symbold_symbolserver.command('delete')
    .argument('<id>', 'id')
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.symbolserver.delete',
        globalOptions: g,
        id,
      } satisfies SymboldSymbolserverDeleteCommand;
    });
  cmd_symbold_symbolserver.command('disable')
    .argument('<id>', 'id')
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.symbolserver.disable',
        globalOptions: g,
        id,
      } satisfies SymboldSymbolserverDisableCommand;
    });
  cmd_symbold_symbolserver.command('enable')
    .argument('<id>', 'id')
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.symbolserver.enable',
        globalOptions: g,
        id,
      } satisfies SymboldSymbolserverEnableCommand;
    });
  const cmd_symbold_whitelist = cmd_symbold.command('whitelist');
  cmd_symbold_whitelist.command('add')
    .argument('<server_id>', 'server_id')
    .option('--name <name>', 'Item name')
    .action((server_id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.whitelist.add',
        globalOptions: g,
        server_id,
        name: opts['name'],
      } satisfies SymboldWhitelistAddCommand;
    });
  cmd_symbold_whitelist.command('remove')
    .argument('<id>', 'id')
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.whitelist.remove',
        globalOptions: g,
        id,
      } satisfies SymboldWhitelistRemoveCommand;
    });
  cmd_symbold_whitelist.command('list')
    .argument('<server_id>', 'server_id')
    .option('--page <page>', 'Page number')
    .option('--take <take>', 'Items per page')
    .action((server_id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.whitelist.list',
        globalOptions: g,
        server_id,
        page: opts['page'],
        take: opts['take'],
      } satisfies SymboldWhitelistListCommand;
    });
  const cmd_symbold_blacklist = cmd_symbold.command('blacklist');
  cmd_symbold_blacklist.command('add')
    .argument('<server_id>', 'server_id')
    .option('--name <name>', 'Item name')
    .action((server_id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.blacklist.add',
        globalOptions: g,
        server_id,
        name: opts['name'],
      } satisfies SymboldBlacklistAddCommand;
    });
  cmd_symbold_blacklist.command('remove')
    .argument('<id>', 'id')
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.blacklist.remove',
        globalOptions: g,
        id,
      } satisfies SymboldBlacklistRemoveCommand;
    });
  cmd_symbold_blacklist.command('list')
    .argument('<server_id>', 'server_id')
    .option('--page <page>', 'Page number')
    .option('--take <take>', 'Items per page')
    .action((server_id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.blacklist.list',
        globalOptions: g,
        server_id,
        page: opts['page'],
        take: opts['take'],
      } satisfies SymboldBlacklistListCommand;
    });
  const cmd_symbold_skiplist = cmd_symbold.command('skiplist');
  cmd_symbold_skiplist.command('find')
    .argument('<server_id>', 'server_id')
    .argument('<filter>', 'filter')
    .option('--page <page>', 'Page number')
    .option('--take <take>', 'Items per page')
    .action((server_id, filter, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.skiplist.find',
        globalOptions: g,
        server_id,
        filter,
        page: opts['page'],
        take: opts['take'],
      } satisfies SymboldSkiplistFindCommand;
    });
  const cmd_symbold_skiplist_remove = cmd_symbold_skiplist.command('remove');
  cmd_symbold_skiplist_remove
    .argument('[id]', 'id')
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.skiplist.remove',
        globalOptions: g,
        id,
      } satisfies SymboldSkiplistRemoveCommand;
    });
  const cmd_symbold_queue = cmd_symbold.command('queue');
  cmd_symbold_queue.command('list')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.queue.list',
        globalOptions: g,
      } satisfies SymboldQueueListCommand;
    });
  cmd_symbold_queue.command('add')
    .argument('<project>', 'project')
    .argument('<symbol>', 'symbol')
    .argument('<oid>', 'oid')
    .action((project, symbol, oid, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.queue.add',
        globalOptions: g,
        project,
        symbol,
        oid,
      } satisfies SymboldQueueAddCommand;
    });
  cmd_symbold_queue.command('size')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.queue.size',
        globalOptions: g,
      } satisfies SymboldQueueSizeCommand;
    });
  cmd_symbold_queue.command('symbols')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.queue.symbols',
        globalOptions: g,
      } satisfies SymboldQueueSymbolsCommand;
    });
  cmd_symbold_symbolserver_logs.command('filter')
    .argument('<id>', 'id')
    .argument('<filter>', 'filter')
    .action((id, filter, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.symbolserver.logs.filter',
        globalOptions: g,
        id,
        filter,
      } satisfies SymboldSymbolserverLogsFilterCommand;
    });
  cmd_symbold_skiplist_remove.command('all')
    .argument('[id]', 'id')
    .argument('<server_id>', 'server_id')
    .action((id, server_id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.skiplist.remove.all',
        globalOptions: g,
        id,
        server_id,
      } satisfies SymboldSkiplistRemoveAllCommand;
    });
  cmd_symbold_skiplist_remove.command('filter')
    .argument('[id]', 'id')
    .argument('<server_id>', 'server_id')
    .argument('<filter>', 'filter')
    .action((id, server_id, filter, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.skiplist.remove.filter',
        globalOptions: g,
        id,
        server_id,
        filter,
      } satisfies SymboldSkiplistRemoveFilterCommand;
    });

  // --- report ---
  const cmd_report = program.command('report');
  cmd_report.command('list')
    .argument('<project>', 'project')
    .action((project, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'report.list',
        globalOptions: g,
        project,
      } satisfies ReportListCommand;
    });
  cmd_report.command('create')
    .argument('<project>', 'project')
    .option('--filter <filter>', 'Filter expression: attribute,operation,value')
    .option('--limit <limit>', 'Number of rows to return')
    .option('--offset <offset>', 'Skip first N rows')
    .option('--select <select>', 'Select specific attributes', collectRepeatable, [])
    .addOption(new Option('--select-wildcard <select-wildcard>', 'Select all attributes of the given type').choices(["physical","derived","virtual"]))
    .option('--age <age>', 'Relative timestamp (e.g., 1h, 42d, 10s, etc). Valid suffixes: y (years), M (months), w (weeks), d (days), h (hours), m (minutes), s (seconds)')
    .option('--time <time>', 'Time range using natural language')
    .option('--unique <unique>', 'Count distinct values')
    .option('--histogram <histogram>', 'Show all distinct values')
    .option('--distribution <distribution>', 'Show truncated histogram')
    .option('--mean <mean>', 'Calculate mean')
    .option('--sum <sum>', 'Sum all values')
    .option('--range <range>', 'Show min and max values')
    .option('--count <count>', 'Count non-null values')
    .option('--bin <bin>', 'Linear histogram of values')
    .option('--head <head>', 'First value in factor')
    .option('--tail <tail>', 'Last value in factor')
    .option('--object <object>', 'Maximum object identifier')
    .addOption(new Option('--last <last>', 'Last object identifier').hideHelp(!revealHidden))
    .addOption(new Option('--first <first>', 'First object identifier').hideHelp(!revealHidden))
    .addOption(new Option('--min <min>', 'Min object identifier').hideHelp(!revealHidden))
    .addOption(new Option('--max <max>', 'Max object identifier').hideHelp(!revealHidden))
    .option('--sort <sort>', 'Sort results: [-](column|fold_term)', collectRepeatable, [])
    .option('--quantize-uint <quantize-uint>', 'Compute quantized column: output_column,input_column,size[,offset]', collectRepeatable, [])
    .addOption(new Option('--raw-query <raw-query>', 'Raw query to use (overrides all other flags)').hideHelp(!revealHidden))
    .addOption(new Option('--table <table>', 'Table to query against').hideHelp(!revealHidden))
    .addOption(new Option('--timestamp-attribute <timestamp-attribute>', 'Attribute to use as timestamp').hideHelp(!revealHidden))
    .option('--reverse <reverse>', 'Reverse sort order')
    .option('--template <template>', 'TODO: (ex: select)')
    .option('--factor <factor>', 'Group by attribute')
    .option('--fingerprint <fingerprint>', 'Filter by an exact fingerprint, or a regex anchored to the beginning of the fingerprint')
    .option('--rcpt <rcpt>', 'Recipient email', collectRepeatable, [])
    .option('--title <title>', 'Report title')
    .addOption(new Option('--period <period>', 'Period: week|day (default: week)').choices(["week","day"]))
    .addOption(new Option('--timezone <timezone>', 'Timezone for report').hideHelp(!revealHidden))
    .addOption(new Option('--day <day>', 'Day of week (1=Monday)').hideHelp(!revealHidden))
    .addOption(new Option('--hour <hour>', 'Hour of day to send (default: 9)').hideHelp(!revealHidden))
    .addOption(new Option('--include-users', 'Whether to include users in the report').default(false).hideHelp(!revealHidden))
    .action((project, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'report.create',
        globalOptions: g,
        queryOptions: {
          filter: opts['filter'],
          limit: opts['limit'],
          offset: opts['offset'],
          select: opts['select'],
          selectWildcard: opts['selectWildcard'],
          age: opts['age'],
          time: opts['time'],
          unique: opts['unique'],
          histogram: opts['histogram'],
          distribution: opts['distribution'],
          mean: opts['mean'],
          sum: opts['sum'],
          range: opts['range'],
          count: opts['count'],
          bin: opts['bin'],
          head: opts['head'],
          tail: opts['tail'],
          object: opts['object'],
          last: opts['last'],
          first: opts['first'],
          min: opts['min'],
          max: opts['max'],
          sort: opts['sort'],
          quantizeUint: opts['quantizeUint'],
          rawQuery: opts['rawQuery'],
          table: opts['table'],
          timestampAttribute: opts['timestampAttribute'],
          reverse: opts['reverse'],
          template: opts['template'],
          factor: opts['factor'],
          fingerprint: opts['fingerprint'],
        },
        project,
        rcpt: opts['rcpt'],
        title: opts['title'],
        period: opts['period'],
        timezone: opts['timezone'],
        day: opts['day'],
        hour: opts['hour'],
        includeUsers: opts['includeUsers'],
      } satisfies ReportCreateCommand;
    });
  cmd_report.command('delete')
    .argument('<project>', 'project')
    .argument('<id>', 'id')
    .action((project, id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'report.delete',
        globalOptions: g,
        project,
        id,
      } satisfies ReportDeleteCommand;
    });
  cmd_report.command('send')
    .argument('<project>', 'project')
    .argument('<id>', 'id')
    .argument('<email>', 'email')
    .action((project, id, email, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'report.send',
        globalOptions: g,
        project,
        id,
        email,
      } satisfies ReportSendCommand;
    });

  // --- merge ---
  program.command('merge')
    .argument('<project>', 'project')
    .argument('<fingerprints...>', 'fingerprints')
    .action((project, fingerprints, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'merge',
        globalOptions: g,
        project,
        fingerprints,
      } satisfies MergeCommand;
    });

  // --- unmerge ---
  program.command('unmerge')
    .argument('<project>', 'project')
    .argument('<fingerprints...>', 'fingerprints')
    .action((project, fingerprints, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'unmerge',
        globalOptions: g,
        project,
        fingerprints,
      } satisfies UnmergeCommand;
    });

  // --- repair ---
  program.command('repair')
    .argument('<project>', 'project')
    .action((project, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'repair',
        globalOptions: g,
        project,
      } satisfies RepairCommand;
    });

  // --- reprocess ---
  program.command('reprocess')
    .argument('<project>', 'project')
    .argument('[target...]', 'target')
    .option('--first <first>', 'First object ID')
    .option('--last <last>', 'Last object ID')
    .action((project, target, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'reprocess',
        globalOptions: g,
        project,
        target,
        first: opts['first'],
        last: opts['last'],
      } satisfies ReprocessCommand;
    });

  // --- retention ---
  const cmd_retention = program.command('retention');
  cmd_retention.command('list')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'retention.list',
        globalOptions: g,
      } satisfies RetentionListCommand;
    });
  cmd_retention.command('set')
    .argument('<name>', 'name')
    .option('--dryrun', 'Show command without executing', false)
    .addOption(new Option('--type <type>', 'Which scope to set the retention policy on').choices(["instance","universe","project"]))
    .option('--rules <rules>', 'Number of rules')
    .option('--age <age>', 'Object age: [R,]O,T[,TE]')
    .option('--max-age <max-age>', 'Max age in seconds')
    .option('--compress <compress>', 'Compress matching objects')
    .option('--delete <delete>', 'Delete subsets: physical|crdb')
    .option('--physical-only <physical-only>', 'Delete only physical data')
    .action((name, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'retention.set',
        globalOptions: g,
        name,
        dryrun: opts['dryrun'],
        type: opts['type'],
        rules: opts['rules'],
        age: opts['age'],
        maxAge: opts['maxAge'],
        compress: opts['compress'],
        delete: opts['delete'],
        physicalOnly: opts['physicalOnly'],
      } satisfies RetentionSetCommand;
    });
  cmd_retention.command('status')
    .argument('[name]', 'name')
    .addOption(new Option('--type <type>', 'Type: universe|project').choices(["universe","project"]))
    .option('--recursive', 'Show recursive status for child namespaces', false)
    .option('--raw', 'Output raw JSON', false)
    .option('--verbose', 'Verbosity level (0-3, repeatable for more detail)', false)
    .option('--instances', 'Show namespace instances per rule', false)
    .option('--includeall', 'Include all namespaces (e.g. symbols)', false)
    .action((name, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'retention.status',
        globalOptions: g,
        name,
        type: opts['type'],
        recursive: opts['recursive'],
        raw: opts['raw'],
        verbose: opts['verbose'],
        instances: opts['instances'],
        includeall: opts['includeall'],
      } satisfies RetentionStatusCommand;
    });
  cmd_retention.command('clear')
    .argument('<name>', 'name')
    .addOption(new Option('--type <type>', 'Type: project').choices(["instance","universe","project"]))
    .action((name, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'retention.clear',
        globalOptions: g,
        name,
        type: opts['type'],
      } satisfies RetentionClearCommand;
    });

  // --- sampling ---
  const cmd_sampling = program.command('sampling');
  cmd_sampling.command('status')
    .option('--fingerprint <fingerprint>', 'Specific fingerprint')
    .option('--project <project>', 'Specific project')
    .option('--max-groups <max-groups>', 'Max groups to display')
    .option('-a, --all', 'Display all groups (no truncation)', false)
    .option('--verbose', 'Show additional accept/reject statistics', false)
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'sampling.status',
        globalOptions: g,
        fingerprint: opts['fingerprint'],
        project: opts['project'],
        maxGroups: opts['maxGroups'],
        all: opts['all'],
        verbose: opts['verbose'],
      } satisfies SamplingStatusCommand;
    });
  cmd_sampling.command('reset')
    .option('--fingerprint <fingerprint>', 'Specific fingerprint')
    .option('--project <project>', 'Specific project')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'sampling.reset',
        globalOptions: g,
        fingerprint: opts['fingerprint'],
        project: opts['project'],
      } satisfies SamplingResetCommand;
    });
  cmd_sampling.command('configure')
    .option('--project <project>', 'Project name')
    .option('--universe <universe>', 'Universe name')
    .option('--disable', 'Disable sampling', false)
    .option('--clear', 'Clear project config', false)
    .option('--attribute <attribute>', 'Sample by attribute')
    .option('--backoff <backoff>', 'Backoff: count,interval')
    .option('--buckets <buckets>', 'Max sampling buckets')
    .option('--process-whitelisted <process-whitelisted>', 'Sample whitelisted symbols')
    .option('--process-private <process-private>', 'Sample private symbols')
    .option('--reset-interval <reset-interval>', 'Reset interval')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'sampling.configure',
        globalOptions: g,
        project: opts['project'],
        universe: opts['universe'],
        disable: opts['disable'],
        clear: opts['clear'],
        attribute: opts['attribute'],
        backoff: opts['backoff'],
        buckets: opts['buckets'],
        processWhitelisted: opts['processWhitelisted'],
        processPrivate: opts['processPrivate'],
        resetInterval: opts['resetInterval'],
      } satisfies SamplingConfigureCommand;
    });

  // --- symbol ---
  program.command('symbol')
    .argument('<project>', 'project')
    .argument('[action]', 'action')
    .option('--tag <tag>', 'Filter by tag')
    .option('--filter <filter>', 'Filter symbols')
    .option('--json', 'JSON output', false)
    .option('-o, --output <output>', 'Output to file')
    .action((project, action, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbol',
        globalOptions: g,
        project,
        action,
        tag: opts['tag'],
        filter: opts['filter'],
        json: opts['json'],
        output: opts['output'],
      } satisfies SymbolCommand;
    });

  // --- scrubber ---
  const cmd_scrubber = program.command('scrubber');
  cmd_scrubber.command('list')
    .argument('<project>', 'project')
    .action((project, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'scrubber.list',
        globalOptions: g,
        project,
      } satisfies ScrubberListCommand;
    });
  cmd_scrubber.command('create')
    .argument('<project>', 'project')
    .option('--name <name>', 'Scrubber name')
    .option('--regexp <regexp>', 'Pattern to match')
    .addOption(new Option('--builtin <builtin>', 'Which builtin to use').choices(["ssn","ccn","key","env","all"]))
    .option('--format <format>', 'Scrubber format (default: all)')
    .option('--target <target>', 'Scrubber target (default: all)')
    .option('--enable <enable>', 'Enable scrubber')
    .action((project, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'scrubber.create',
        globalOptions: g,
        project,
        name: opts['name'],
        regexp: opts['regexp'],
        builtin: opts['builtin'],
        format: opts['format'],
        target: opts['target'],
        enable: opts['enable'],
      } satisfies ScrubberCreateCommand;
    });
  cmd_scrubber.command('modify')
    .argument('<project>', 'project')
    .argument('<id>', 'id')
    .option('--name <name>', 'Scrubber name')
    .option('--regexp <regexp>', 'New pattern')
    .option('--builtin <builtin>', 'Builtin type (not modifiable)')
    .option('--format <format>', 'Scrubber format')
    .option('--target <target>', 'Scrubber target')
    .option('--enable <enable>', 'Enable/disable')
    .action((project, id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'scrubber.modify',
        globalOptions: g,
        project,
        id,
        name: opts['name'],
        regexp: opts['regexp'],
        builtin: opts['builtin'],
        format: opts['format'],
        target: opts['target'],
        enable: opts['enable'],
      } satisfies ScrubberModifyCommand;
    });
  cmd_scrubber.command('delete')
    .argument('<project>', 'project')
    .argument('<id>', 'id')
    .action((project, id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'scrubber.delete',
        globalOptions: g,
        project,
        id,
      } satisfies ScrubberDeleteCommand;
    });

  // --- setup ---
  program.command('setup')
    .argument('<url>', 'url')
    .action((url, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'setup',
        globalOptions: g,
        url,
      } satisfies SetupCommand;
    });

  // --- nuke ---
  program.command('nuke')
    .option('--universe <universe>', 'Universe name')
    .option('--project <project>', 'Project name')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'nuke',
        globalOptions: g,
        universe: opts['universe'],
        project: opts['project'],
      } satisfies NukeCommand;
    });

  // --- token ---
  const cmd_token = program.command('token');
  cmd_token.command('create')
    .option('--project <project>', 'Project name')
    .addOption(new Option('--capability <capability>', 'Capabilities to assign to this token').choices(["symbol:post","error:post","query:post","sync:post"]).default([]))
    .option('--metadata <metadata>', 'Token metadata JSON')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'token.create',
        globalOptions: g,
        project: opts['project'],
        capability: opts['capability'],
        metadata: opts['metadata'],
      } satisfies TokenCreateCommand;
    });
  cmd_token.command('list')
    .option('--universe <universe>', 'Universe name')
    .option('--project <project>', 'Project name')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'token.list',
        globalOptions: g,
        universe: opts['universe'],
        project: opts['project'],
      } satisfies TokenListCommand;
    });
  cmd_token.command('delete')
    .argument('<token>', 'token')
    .action((token, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'token.delete',
        globalOptions: g,
        token,
      } satisfies TokenDeleteCommand;
    });

  // --- user ---
  const cmd_user = program.command('user');
  cmd_user.command('reset')
    .option('--universe <universe>', 'Universe name')
    .option('--user <user>', 'Username')
    .option('--password <password>', 'New password')
    .option('--role <role>', 'User role')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'user.reset',
        globalOptions: g,
        universe: opts['universe'],
        user: opts['user'],
        password: opts['password'],
        role: opts['role'],
      } satisfies UserResetCommand;
    });

  // --- users ---
  const cmd_users = program.command('users');
  cmd_users.command('add-signup-whitelist')
    .option('--universe <universe>', 'Universe name')
    .option('--domain <domain>', 'Domain name')
    .option('--role <role>', 'Default role')
    .option('--method <method>', 'Auth method')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'users.add-signup-whitelist',
        globalOptions: g,
        universe: opts['universe'],
        domain: opts['domain'],
        role: opts['role'],
        method: opts['method'],
      } satisfies UsersAddSignupWhitelistCommand;
    });
  cmd_users.command('list-teamless-users')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'users.list-teamless-users',
        globalOptions: g,
      } satisfies UsersListTeamlessUsersCommand;
    });

  // --- tenant ---
  const cmd_tenant = program.command('tenant');
  cmd_tenant.command('create')
    .argument('<name>', 'name')
    .action((name, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'tenant.create',
        globalOptions: g,
        name,
      } satisfies TenantCreateCommand;
    });
  cmd_tenant.command('delete')
    .argument('<name>', 'name')
    .action((name, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'tenant.delete',
        globalOptions: g,
        name,
      } satisfies TenantDeleteCommand;
    });
  cmd_tenant.command('list')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'tenant.list',
        globalOptions: g,
      } satisfies TenantListCommand;
    });

  // --- similarity ---
  program.command('similarity')
    .argument('<project>', 'project')
    .argument('[filter]', 'filter')
    .option('--threshold <threshold>', 'Min callstack length')
    .option('--truncate <truncate>', 'Shorten callstack before comparing')
    .option('--intersection <intersection>', 'Min common symbols')
    .option('--distance <distance>', 'Max edit distance')
    .option('--fingerprint <fingerprint>', 'Compare to specific fingerprint')
    .option('--json', 'Return JSON result', false)
    .action((project, filter, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'similarity',
        globalOptions: g,
        project,
        filter,
        threshold: opts['threshold'],
        truncate: opts['truncate'],
        intersection: opts['intersection'],
        distance: opts['distance'],
        fingerprint: opts['fingerprint'],
        json: opts['json'],
      } satisfies SimilarityCommand;
    });

  // --- invite ---
  const cmd_invite = program.command('invite');
  cmd_invite.command('create')
    .argument('<username>', 'username')
    .argument('<email>', 'email')
    .addOption(new Option('--role <role>', 'Role to assign the user').choices(["guest","member","admin"]))
    .option('--metadata <metadata>', 'Additional metadata')
    .option('--tenant <tenant>', 'Tenant name')
    .addOption(new Option('--method <method>', 'Authentication method to use').choices(["password","saml","pam"]))
    .action((username, email, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'invite.create',
        globalOptions: g,
        username,
        email,
        role: opts['role'],
        metadata: opts['metadata'],
        tenant: opts['tenant'],
        method: opts['method'],
      } satisfies InviteCreateCommand;
    });
  cmd_invite.command('list')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'invite.list',
        globalOptions: g,
      } satisfies InviteListCommand;
    });
  cmd_invite.command('delete')
    .argument('<email>', 'email')
    .option('--universe <universe>', 'Universe name')
    .action((email, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'invite.delete',
        globalOptions: g,
        email,
        universe: opts['universe'],
      } satisfies InviteDeleteCommand;
    });

  // --- callstack ---
  const cmd_callstack = program.command('callstack');
  cmd_callstack.command('evaluate')
    .argument('<project>', 'project')
    .argument('<target>', 'target')
    .action((project, target, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'callstack.evaluate',
        globalOptions: g,
        project,
        target,
      } satisfies CallstackEvaluateCommand;
    });

  // --- access ---
  const cmd_access = program.command('access');
  const cmd_access_team = cmd_access.command('team');
  cmd_access_team.command('create')
    .argument('<team>', 'team')
    .action((team, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'access.team.create',
        globalOptions: g,
        team,
      } satisfies AccessTeamCreateCommand;
    });
  cmd_access_team.command('remove')
    .argument('<team>', 'team')
    .action((team, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'access.team.remove',
        globalOptions: g,
        team,
      } satisfies AccessTeamRemoveCommand;
    });
  cmd_access_team.command('details')
    .argument('<team>', 'team')
    .action((team, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'access.team.details',
        globalOptions: g,
        team,
      } satisfies AccessTeamDetailsCommand;
    });
  cmd_access_team.command('add-user')
    .argument('<team>', 'team')
    .argument('<user>', 'user')
    .action((team, user, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'access.team.add-user',
        globalOptions: g,
        team,
        user,
      } satisfies AccessTeamAddUserCommand;
    });
  cmd_access_team.command('remove-user')
    .argument('<team>', 'team')
    .argument('<user>', 'user')
    .action((team, user, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'access.team.remove-user',
        globalOptions: g,
        team,
        user,
      } satisfies AccessTeamRemoveUserCommand;
    });
  cmd_access_team.command('list')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'access.team.list',
        globalOptions: g,
      } satisfies AccessTeamListCommand;
    });
  const cmd_access_project = cmd_access.command('project');
  cmd_access_project.command('add-team')
    .argument('<project>', 'project')
    .argument('<team>', 'team')
    .argument('<role>', 'role')
    .action((project, team, role, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'access.project.add-team',
        globalOptions: g,
        project,
        team,
        role,
      } satisfies AccessProjectAddTeamCommand;
    });
  cmd_access_project.command('remove-team')
    .argument('<project>', 'project')
    .argument('<team>', 'team')
    .action((project, team, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'access.project.remove-team',
        globalOptions: g,
        project,
        team,
      } satisfies AccessProjectRemoveTeamCommand;
    });
  cmd_access_project.command('add-user')
    .argument('<project>', 'project')
    .argument('<user>', 'user')
    .argument('<role>', 'role')
    .action((project, user, role, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'access.project.add-user',
        globalOptions: g,
        project,
        user,
        role,
      } satisfies AccessProjectAddUserCommand;
    });
  cmd_access_project.command('remove-user')
    .argument('<project>', 'project')
    .argument('<user>', 'user')
    .action((project, user, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'access.project.remove-user',
        globalOptions: g,
        project,
        user,
      } satisfies AccessProjectRemoveUserCommand;
    });
  cmd_access_project.command('details')
    .argument('<project>', 'project')
    .action((project, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'access.project.details',
        globalOptions: g,
        project,
      } satisfies AccessProjectDetailsCommand;
    });

  // --- stability ---
  const cmd_stability = program.command('stability');
  cmd_stability.command('create-metric')
    .option('--universe <universe>', 'Universe name')
    .option('--project <project>', 'Project name')
    .option('--metric-group <metric-group>', 'Metric group name')
    .option('--name <name>', 'Metric name')
    .option('--attribute <attribute>', 'Attribute: name,value')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'stability.create-metric',
        globalOptions: g,
        universe: opts['universe'],
        project: opts['project'],
        metricGroup: opts['metricGroup'],
        name: opts['name'],
        attribute: opts['attribute'],
      } satisfies StabilityCreateMetricCommand;
    });

  // --- metrics-importer ---
  const cmd_metricsImporter = program.command('metrics-importer');
  cmd_metricsImporter.command('logs')
    .option('--project <project>', 'Project name')
    .option('--source-id <source-id>', 'Source ID')
    .option('--importer-id <importer-id>', 'Importer ID')
    .option('--limit <limit>', 'Log limit')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'metrics-importer.logs',
        globalOptions: g,
        project: opts['project'],
        sourceId: opts['sourceId'],
        importerId: opts['importerId'],
        limit: opts['limit'],
      } satisfies MetricsImporterLogsCommand;
    });
  const cmd_metricsImporter_source = cmd_metricsImporter.command('source');
  cmd_metricsImporter_source.command('check-query')
    .option('--source <source>', 'Source UUID')
    .option('--project <project>', 'Project name')
    .option('--query <query>', 'Query to validate')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'metrics-importer.source.check-query',
        globalOptions: g,
        source: opts['source'],
        project: opts['project'],
        query: opts['query'],
      } satisfies MetricsImporterSourceCheckQueryCommand;
    });
  const cmd_metricsImporter_importer = cmd_metricsImporter.command('importer');
  cmd_metricsImporter_importer.command('create')
    .option('--project <project>', 'Project name')
    .option('--source <source>', 'Source UUID')
    .option('--name <name>', 'Importer name')
    .option('--start-at <start-at>', 'Start time (RFC3339)')
    .option('--metric <metric>', 'Metric name')
    .option('--metric-group <metric-group>', 'Metric group')
    .option('--query <query>', 'Data query')
    .option('--delay <delay>', 'Delay in seconds')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'metrics-importer.importer.create',
        globalOptions: g,
        project: opts['project'],
        source: opts['source'],
        name: opts['name'],
        startAt: opts['startAt'],
        metric: opts['metric'],
        metricGroup: opts['metricGroup'],
        query: opts['query'],
        delay: opts['delay'],
      } satisfies MetricsImporterImporterCreateCommand;
    });

  // --- alerts ---
  const cmd_alerts = program.command('alerts');
  const cmd_alerts_target = cmd_alerts.command('target');
  cmd_alerts_target.command('create')
    .option('--project <project>', 'Project name')
    .option('--name <name>', 'Target name')
    .option('--workflow-name <workflow-name>', 'Workflow name')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'alerts.target.create',
        globalOptions: g,
        project: opts['project'],
        name: opts['name'],
        workflowName: opts['workflowName'],
      } satisfies AlertsTargetCreateCommand;
    });
  cmd_alerts_target.command('list')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'alerts.target.list',
        globalOptions: g,
      } satisfies AlertsTargetListCommand;
    });
  cmd_alerts_target.command('get')
    .option('--id <id>', 'Target ID')
    .option('--name <name>', 'Target name')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'alerts.target.get',
        globalOptions: g,
        id: opts['id'],
        name: opts['name'],
      } satisfies AlertsTargetGetCommand;
    });
  cmd_alerts_target.command('update')
    .option('--id <id>', 'Target ID')
    .option('--name <name>', 'Target name')
    .option('--rename <rename>', 'New name')
    .option('--workflow-name <workflow-name>', 'New workflow')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'alerts.target.update',
        globalOptions: g,
        id: opts['id'],
        name: opts['name'],
        rename: opts['rename'],
        workflowName: opts['workflowName'],
      } satisfies AlertsTargetUpdateCommand;
    });
  cmd_alerts_target.command('delete')
    .option('--id <id>', 'Target ID')
    .option('--name <name>', 'Target name')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'alerts.target.delete',
        globalOptions: g,
        id: opts['id'],
        name: opts['name'],
      } satisfies AlertsTargetDeleteCommand;
    });
  const cmd_alerts_alert = cmd_alerts.command('alert');
  cmd_alerts_alert.command('list')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'alerts.alert.list',
        globalOptions: g,
      } satisfies AlertsAlertListCommand;
    });
  cmd_alerts_alert.command('get')
    .option('--id <id>', 'Alert ID')
    .option('--name <name>', 'Alert name')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'alerts.alert.get',
        globalOptions: g,
        id: opts['id'],
        name: opts['name'],
      } satisfies AlertsAlertGetCommand;
    });
  cmd_alerts_alert.command('update')
    .option('--filter <filter>', 'Filter expression: attribute,operation,value')
    .option('--limit <limit>', 'Number of rows to return')
    .option('--offset <offset>', 'Skip first N rows')
    .option('--select <select>', 'Select specific attributes', collectRepeatable, [])
    .addOption(new Option('--select-wildcard <select-wildcard>', 'Select all attributes of the given type').choices(["physical","derived","virtual"]))
    .option('--age <age>', 'Relative timestamp (e.g., 1h, 42d, 10s, etc). Valid suffixes: y (years), M (months), w (weeks), d (days), h (hours), m (minutes), s (seconds)')
    .option('--time <time>', 'Time range using natural language')
    .option('--unique <unique>', 'Count distinct values')
    .option('--histogram <histogram>', 'Show all distinct values')
    .option('--distribution <distribution>', 'Show truncated histogram')
    .option('--mean <mean>', 'Calculate mean')
    .option('--sum <sum>', 'Sum all values')
    .option('--range <range>', 'Show min and max values')
    .option('--count <count>', 'Count non-null values')
    .option('--bin <bin>', 'Linear histogram of values')
    .option('--head <head>', 'First value in factor')
    .option('--tail <tail>', 'Last value in factor')
    .option('--object <object>', 'Maximum object identifier')
    .addOption(new Option('--last <last>', 'Last object identifier').hideHelp(!revealHidden))
    .addOption(new Option('--first <first>', 'First object identifier').hideHelp(!revealHidden))
    .addOption(new Option('--min <min>', 'Min object identifier').hideHelp(!revealHidden))
    .addOption(new Option('--max <max>', 'Max object identifier').hideHelp(!revealHidden))
    .option('--sort <sort>', 'Sort results: [-](column|fold_term)', collectRepeatable, [])
    .option('--quantize-uint <quantize-uint>', 'Compute quantized column: output_column,input_column,size[,offset]', collectRepeatable, [])
    .addOption(new Option('--raw-query <raw-query>', 'Raw query to use (overrides all other flags)').hideHelp(!revealHidden))
    .addOption(new Option('--table <table>', 'Table to query against').hideHelp(!revealHidden))
    .addOption(new Option('--timestamp-attribute <timestamp-attribute>', 'Attribute to use as timestamp').hideHelp(!revealHidden))
    .option('--reverse <reverse>', 'Reverse sort order')
    .option('--template <template>', 'TODO: (ex: select)')
    .option('--factor <factor>', 'Group by attribute')
    .option('--fingerprint <fingerprint>', 'Filter by an exact fingerprint, or a regex anchored to the beginning of the fingerprint')
    .option('--id <id>', 'Alert ID')
    .option('--name <name>', 'Alert name')
    .option('--rename <rename>', 'New name')
    .option('--enabled <enabled>', 'Enable alert')
    .option('--query-period <query-period>', 'Query period')
    .option('--min-notification-interval <min-notification-interval>', 'Min notification interval')
    .option('--mute-until <mute-until>', 'Mute until timestamp')
    .option('--target-name <target-name>', 'Target name', collectRepeatable, [])
    .option('--target-id <target-id>', 'Target ID', collectRepeatable, [])
    .option('--trigger <trigger>', 'Trigger spec: column,index,op,warn,crit')
    .option('--replace-query', 'Replace query', false)
    .option('--clear-targets', 'Clear targets', false)
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'alerts.alert.update',
        globalOptions: g,
        queryOptions: {
          filter: opts['filter'],
          limit: opts['limit'],
          offset: opts['offset'],
          select: opts['select'],
          selectWildcard: opts['selectWildcard'],
          age: opts['age'],
          time: opts['time'],
          unique: opts['unique'],
          histogram: opts['histogram'],
          distribution: opts['distribution'],
          mean: opts['mean'],
          sum: opts['sum'],
          range: opts['range'],
          count: opts['count'],
          bin: opts['bin'],
          head: opts['head'],
          tail: opts['tail'],
          object: opts['object'],
          last: opts['last'],
          first: opts['first'],
          min: opts['min'],
          max: opts['max'],
          sort: opts['sort'],
          quantizeUint: opts['quantizeUint'],
          rawQuery: opts['rawQuery'],
          table: opts['table'],
          timestampAttribute: opts['timestampAttribute'],
          reverse: opts['reverse'],
          template: opts['template'],
          factor: opts['factor'],
          fingerprint: opts['fingerprint'],
        },
        id: opts['id'],
        name: opts['name'],
        rename: opts['rename'],
        enabled: opts['enabled'],
        queryPeriod: opts['queryPeriod'],
        minNotificationInterval: opts['minNotificationInterval'],
        muteUntil: opts['muteUntil'],
        targetName: opts['targetName'],
        targetId: opts['targetId'],
        trigger: opts['trigger'],
        replaceQuery: opts['replaceQuery'],
        clearTargets: opts['clearTargets'],
      } satisfies AlertsAlertUpdateCommand;
    });
  cmd_alerts_alert.command('delete')
    .option('--id <id>', 'Alert ID')
    .option('--name <name>', 'Alert name')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'alerts.alert.delete',
        globalOptions: g,
        id: opts['id'],
        name: opts['name'],
      } satisfies AlertsAlertDeleteCommand;
    });
  cmd_alerts_alert.command('create')
    .option('--filter <filter>', 'Filter expression: attribute,operation,value')
    .option('--limit <limit>', 'Number of rows to return')
    .option('--offset <offset>', 'Skip first N rows')
    .option('--select <select>', 'Select specific attributes', collectRepeatable, [])
    .addOption(new Option('--select-wildcard <select-wildcard>', 'Select all attributes of the given type').choices(["physical","derived","virtual"]))
    .option('--age <age>', 'Relative timestamp (e.g., 1h, 42d, 10s, etc). Valid suffixes: y (years), M (months), w (weeks), d (days), h (hours), m (minutes), s (seconds)')
    .option('--time <time>', 'Time range using natural language')
    .option('--unique <unique>', 'Count distinct values')
    .option('--histogram <histogram>', 'Show all distinct values')
    .option('--distribution <distribution>', 'Show truncated histogram')
    .option('--mean <mean>', 'Calculate mean')
    .option('--sum <sum>', 'Sum all values')
    .option('--range <range>', 'Show min and max values')
    .option('--count <count>', 'Count non-null values')
    .option('--bin <bin>', 'Linear histogram of values')
    .option('--head <head>', 'First value in factor')
    .option('--tail <tail>', 'Last value in factor')
    .option('--object <object>', 'Maximum object identifier')
    .addOption(new Option('--last <last>', 'Last object identifier').hideHelp(!revealHidden))
    .addOption(new Option('--first <first>', 'First object identifier').hideHelp(!revealHidden))
    .addOption(new Option('--min <min>', 'Min object identifier').hideHelp(!revealHidden))
    .addOption(new Option('--max <max>', 'Max object identifier').hideHelp(!revealHidden))
    .option('--sort <sort>', 'Sort results: [-](column|fold_term)', collectRepeatable, [])
    .option('--quantize-uint <quantize-uint>', 'Compute quantized column: output_column,input_column,size[,offset]', collectRepeatable, [])
    .addOption(new Option('--raw-query <raw-query>', 'Raw query to use (overrides all other flags)').hideHelp(!revealHidden))
    .addOption(new Option('--table <table>', 'Table to query against').hideHelp(!revealHidden))
    .addOption(new Option('--timestamp-attribute <timestamp-attribute>', 'Attribute to use as timestamp').hideHelp(!revealHidden))
    .option('--reverse <reverse>', 'Reverse sort order')
    .option('--template <template>', 'TODO: (ex: select)')
    .option('--factor <factor>', 'Group by attribute')
    .option('--fingerprint <fingerprint>', 'Filter by an exact fingerprint, or a regex anchored to the beginning of the fingerprint')
    .option('--universe <universe>', 'Universe name')
    .option('--project <project>', 'Project name')
    .option('--name <name>', 'Alert name')
    .option('--enabled <enabled>', 'Enable alert')
    .option('--query-period <query-period>', 'Query period')
    .option('--min-notification-interval <min-notification-interval>', 'Min notification interval')
    .option('--mute-until <mute-until>', 'Mute until timestamp')
    .option('--target-name <target-name>', 'Target name', collectRepeatable, [])
    .option('--target-id <target-id>', 'Target ID', collectRepeatable, [])
    .option('--trigger <trigger>', 'Trigger spec: column,index,op,warn,crit')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'alerts.alert.create',
        globalOptions: g,
        queryOptions: {
          filter: opts['filter'],
          limit: opts['limit'],
          offset: opts['offset'],
          select: opts['select'],
          selectWildcard: opts['selectWildcard'],
          age: opts['age'],
          time: opts['time'],
          unique: opts['unique'],
          histogram: opts['histogram'],
          distribution: opts['distribution'],
          mean: opts['mean'],
          sum: opts['sum'],
          range: opts['range'],
          count: opts['count'],
          bin: opts['bin'],
          head: opts['head'],
          tail: opts['tail'],
          object: opts['object'],
          last: opts['last'],
          first: opts['first'],
          min: opts['min'],
          max: opts['max'],
          sort: opts['sort'],
          quantizeUint: opts['quantizeUint'],
          rawQuery: opts['rawQuery'],
          table: opts['table'],
          timestampAttribute: opts['timestampAttribute'],
          reverse: opts['reverse'],
          template: opts['template'],
          factor: opts['factor'],
          fingerprint: opts['fingerprint'],
        },
        universe: opts['universe'],
        project: opts['project'],
        name: opts['name'],
        enabled: opts['enabled'],
        queryPeriod: opts['queryPeriod'],
        minNotificationInterval: opts['minNotificationInterval'],
        muteUntil: opts['muteUntil'],
        targetName: opts['targetName'],
        targetId: opts['targetId'],
        trigger: opts['trigger'],
      } satisfies AlertsAlertCreateCommand;
    });

  // --- workflows ---
  const cmd_workflows = program.command('workflows');
  const cmd_workflows_connection = cmd_workflows.command('connection');
  cmd_workflows_connection.command('create')
    .option('--name <name>', 'Connection name')
    .option('--plugin <plugin>', 'Plugin ID')
    .option('--options <options>', 'Plugin options')
    .option('--from-file <from-file>', 'Load from file')
    .option('--raw', 'Output raw JSON', false)
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'workflows.connection.create',
        globalOptions: g,
        name: opts['name'],
        plugin: opts['plugin'],
        options: opts['options'],
        fromFile: opts['fromFile'],
        raw: opts['raw'],
      } satisfies WorkflowsConnectionCreateCommand;
    });
  cmd_workflows_connection.command('list')
    .option('--raw', 'Output raw JSON', false)
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'workflows.connection.list',
        globalOptions: g,
        raw: opts['raw'],
      } satisfies WorkflowsConnectionListCommand;
    });
  cmd_workflows_connection.command('get')
    .argument('<id>', 'id')
    .option('--raw', 'Output raw JSON', false)
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'workflows.connection.get',
        globalOptions: g,
        id,
        raw: opts['raw'],
      } satisfies WorkflowsConnectionGetCommand;
    });
  cmd_workflows_connection.command('update')
    .argument('<id>', 'id')
    .option('--name <name>', 'New name')
    .option('--options <options>', 'New options')
    .option('--from-file <from-file>', 'Load from file')
    .option('--raw', 'Output raw JSON', false)
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'workflows.connection.update',
        globalOptions: g,
        id,
        name: opts['name'],
        options: opts['options'],
        fromFile: opts['fromFile'],
        raw: opts['raw'],
      } satisfies WorkflowsConnectionUpdateCommand;
    });
  cmd_workflows_connection.command('delete')
    .argument('<id>', 'id')
    .option('--raw', 'Output raw JSON', false)
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'workflows.connection.delete',
        globalOptions: g,
        id,
        raw: opts['raw'],
      } satisfies WorkflowsConnectionDeleteCommand;
    });
  const cmd_workflows_integration = cmd_workflows.command('integration');
  cmd_workflows_integration.command('create')
    .option('--project <project>', 'Project name')
    .option('--name <name>', 'Integration name')
    .option('--plugin <plugin>', 'Plugin ID')
    .option('--options <options>', 'Plugin options')
    .addOption(new Option('--state <state>', 'Initial state to set for the integration').choices(["enabled","disabled","stopped"]))
    .option('--connection <connection>', 'Connection ID')
    .option('--synchronize-issues <synchronize-issues>', 'Enable issue sync')
    .option('--synchronize-issues-on-add <synchronize-issues-on-add>', 'Sync on add')
    .option('--from-file <from-file>', 'Load from file')
    .option('--raw', 'Output raw JSON', false)
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'workflows.integration.create',
        globalOptions: g,
        project: opts['project'],
        name: opts['name'],
        plugin: opts['plugin'],
        options: opts['options'],
        state: opts['state'],
        connection: opts['connection'],
        synchronizeIssues: opts['synchronizeIssues'],
        synchronizeIssuesOnAdd: opts['synchronizeIssuesOnAdd'],
        fromFile: opts['fromFile'],
        raw: opts['raw'],
      } satisfies WorkflowsIntegrationCreateCommand;
    });
  cmd_workflows_integration.command('list')
    .option('--raw', 'Output raw JSON', false)
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'workflows.integration.list',
        globalOptions: g,
        raw: opts['raw'],
      } satisfies WorkflowsIntegrationListCommand;
    });
  cmd_workflows_integration.command('get')
    .argument('<id>', 'id')
    .option('--raw', 'Output raw JSON', false)
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'workflows.integration.get',
        globalOptions: g,
        id,
        raw: opts['raw'],
      } satisfies WorkflowsIntegrationGetCommand;
    });
  cmd_workflows_integration.command('update')
    .argument('<id>', 'id')
    .option('--options <options>', 'New options')
    .option('--state <state>', 'New state')
    .option('--connection <connection>', 'New connection ID')
    .option('--from-file <from-file>', 'Load from file')
    .option('--raw', 'Output raw JSON', false)
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'workflows.integration.update',
        globalOptions: g,
        id,
        options: opts['options'],
        state: opts['state'],
        connection: opts['connection'],
        fromFile: opts['fromFile'],
        raw: opts['raw'],
      } satisfies WorkflowsIntegrationUpdateCommand;
    });
  cmd_workflows_integration.command('delete')
    .argument('<id>', 'id')
    .option('--raw', 'Output raw JSON', false)
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'workflows.integration.delete',
        globalOptions: g,
        id,
        raw: opts['raw'],
      } satisfies WorkflowsIntegrationDeleteCommand;
    });
  const cmd_workflows_alert = cmd_workflows.command('alert');
  cmd_workflows_alert.command('list')
    .option('--raw', 'Output raw JSON', false)
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'workflows.alert.list',
        globalOptions: g,
        raw: opts['raw'],
      } satisfies WorkflowsAlertListCommand;
    });
  cmd_workflows_alert.command('get')
    .argument('<id>', 'id')
    .option('--raw', 'Output raw JSON', false)
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'workflows.alert.get',
        globalOptions: g,
        id,
        raw: opts['raw'],
      } satisfies WorkflowsAlertGetCommand;
    });
  cmd_workflows_alert.command('create')
    .option('--project <project>', 'Project name')
    .option('--name <name>', 'Alert name')
    .option('--condition.name <condition.name>', 'Condition type')
    .option('--frequency <frequency>', 'Frequency in ms')
    .option('--execution-delay <execution-delay>', 'Delay in ms')
    .addOption(new Option('--state <state>', 'Initial state to set for the integration').choices(["enabled","disabled","stopped"]))
    .option('--filter <filter>', 'Filter expression')
    .option('--integration <integration>', 'Integration ID')
    .option('--from-file <from-file>', 'Load from file')
    .option('--raw', 'Output raw JSON', false)
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'workflows.alert.create',
        globalOptions: g,
        project: opts['project'],
        name: opts['name'],
        conditionName: opts['conditionName'],
        frequency: opts['frequency'],
        executionDelay: opts['executionDelay'],
        state: opts['state'],
        filter: opts['filter'],
        integration: opts['integration'],
        fromFile: opts['fromFile'],
        raw: opts['raw'],
      } satisfies WorkflowsAlertCreateCommand;
    });
  cmd_workflows_alert.command('update')
    .argument('<id>', 'id')
    .option('--name <name>', 'New name')
    .option('--from-file <from-file>', 'Load from file')
    .option('--raw', 'Output raw JSON', false)
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'workflows.alert.update',
        globalOptions: g,
        id,
        name: opts['name'],
        fromFile: opts['fromFile'],
        raw: opts['raw'],
      } satisfies WorkflowsAlertUpdateCommand;
    });
  cmd_workflows_alert.command('delete')
    .argument('<id>', 'id')
    .option('--raw', 'Output raw JSON', false)
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'workflows.alert.delete',
        globalOptions: g,
        id,
        raw: opts['raw'],
      } satisfies WorkflowsAlertDeleteCommand;
    });

  // --- actions ---
  const cmd_actions = program.command('actions');
  cmd_actions.command('get')
    .option('--universe <universe>', 'Universe name')
    .option('--project <project>', 'Project name')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'actions.get',
        globalOptions: g,
        universe: opts['universe'],
        project: opts['project'],
      } satisfies ActionsGetCommand;
    });
  cmd_actions.command('disable')
    .option('--universe <universe>', 'Universe name')
    .option('--project <project>', 'Project name')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'actions.disable',
        globalOptions: g,
        universe: opts['universe'],
        project: opts['project'],
      } satisfies ActionsDisableCommand;
    });
  cmd_actions.command('enable')
    .option('--universe <universe>', 'Universe name')
    .option('--project <project>', 'Project name')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'actions.enable',
        globalOptions: g,
        universe: opts['universe'],
        project: opts['project'],
      } satisfies ActionsEnableCommand;
    });
  cmd_actions.command('upload')
    .argument('<path>', 'path')
    .option('--universe <universe>', 'Universe name')
    .option('--project <project>', 'Project name')
    .action((path, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'actions.upload',
        globalOptions: g,
        path,
        universe: opts['universe'],
        project: opts['project'],
      } satisfies ActionsUploadCommand;
    });
  cmd_actions.command('delete')
    .option('--universe <universe>', 'Universe name')
    .option('--project <project>', 'Project name')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'actions.delete',
        globalOptions: g,
        universe: opts['universe'],
        project: opts['project'],
      } satisfies ActionsDeleteCommand;
    });

  // --- attribute ---
  const cmd_attribute = program.command('attribute');
  cmd_attribute.command('create')
    .argument('<project>', 'project')
    .argument('<name>', 'name')
    .option('--description <description>', 'Attribute description')
    .addOption(new Option('--type <type>', 'Attribute value type').choices(["bitmap","uint8","uint16","uint32","uint64","uint128","uuid","dictionary"]))
    .addOption(new Option('--format <format>', 'Attribute display format').choices(["none","commit","semver","callstack","hostname","bytes","kilobytes","gigabytes","nanoseconds","milliseconds","seconds","unix_timestamp","js_timestamp","gps_timestamp","memory_address","labels","sha256","uuid","ipv4","ipv6"]))
    .action((project, name, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'attribute.create',
        globalOptions: g,
        project,
        name,
        description: opts['description'],
        type: opts['type'],
        format: opts['format'],
      } satisfies AttributeCreateCommand;
    });
  cmd_attribute.command('delete')
    .argument('<project>', 'project')
    .argument('<name>', 'name')
    .action((project, name, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'attribute.delete',
        globalOptions: g,
        project,
        name,
      } satisfies AttributeDeleteCommand;
    });

  // --- view ---
  const cmd_view = program.command('view');
  cmd_view.command('create')
    .argument('<project>', 'project')
    .argument('<name>', 'name')
    .option('--queries <queries>', 'Queries JSON file')
    .option('--payload <payload>', 'Payload JSON file')
    .action((project, name, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'view.create',
        globalOptions: g,
        project,
        name,
        queries: opts['queries'],
        payload: opts['payload'],
      } satisfies ViewCreateCommand;
    });
  cmd_view.command('delete')
    .argument('<project>', 'project')
    .argument('<name>', 'name')
    .action((project, name, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'view.delete',
        globalOptions: g,
        project,
        name,
      } satisfies ViewDeleteCommand;
    });

  // --- error ---
  program.command('error', { hidden: !revealHidden })
    .argument('<message>', 'message')
    .action((message, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'error',
        globalOptions: g,
        message,
      } satisfies ErrorCommand;
    });

  // --- log ---
  const cmd_log = program.command('log', { hidden: !revealHidden });
  cmd_log.command('list', { hidden: !revealHidden })
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'log.list',
        globalOptions: g,
      } satisfies LogListCommand;
    });
  cmd_log.command('activate', { hidden: !revealHidden })
    .argument('<name>', 'name')
    .action((name, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'log.activate',
        globalOptions: g,
        name,
      } satisfies LogActivateCommand;
    });
  cmd_log.command('deactivate', { hidden: !revealHidden })
    .argument('<name>', 'name')
    .action((name, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'log.deactivate',
        globalOptions: g,
        name,
      } satisfies LogDeactivateCommand;
    });
  cmd_log.command('extract', { hidden: !revealHidden })
    .argument('<name>', 'name')
    .option('--json', 'Output JSON format', false)
    .option('--table', 'Output as table', false)
    .action((name, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'log.extract',
        globalOptions: g,
        name,
        json: opts['json'],
        table: opts['table'],
      } satisfies LogExtractCommand;
    });

  // --- audit ---
  const cmd_audit = program.command('audit', { hidden: !revealHidden });
  cmd_audit.command('extract', { hidden: !revealHidden })
    .option('--json', 'Output JSON format', false)
    .option('--table', 'Output as table', false)
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'audit.extract',
        globalOptions: g,
        json: opts['json'],
        table: opts['table'],
      } satisfies AuditExtractCommand;
    });

  // --- bpg ---
  const cmd_bpg = program.command('bpg', { hidden: !revealHidden });
  cmd_bpg.command('list', { hidden: !revealHidden })
    .argument('<type>', 'type')
    .option('--raw <raw>', 'Raw BPG JSON request')
    .action((type, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'bpg.list',
        globalOptions: g,
        type,
        raw: opts['raw'],
      } satisfies BpgListCommand;
    });

  // --- control ---
  program.command('control', { hidden: !revealHidden })
    .option('--smr', 'Trigger grace period', false)
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'control',
        globalOptions: g,
        smr: opts['smr'],
      } satisfies ControlCommand;
    });

  // --- deduplication ---
  const cmd_deduplication = program.command('deduplication', { hidden: !revealHidden });
  cmd_deduplication
    .argument('<project>', 'project')
    .action((project, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'deduplication',
        globalOptions: g,
        project,
      } satisfies DeduplicationCommand;
    });
  cmd_deduplication.command('add', { hidden: !revealHidden })
    .argument('<project>', 'project')
    .option('--name <name>', 'Rule name')
    .option('--rules <rules>', 'Rules JSON file')
    .option('--priority <priority>', 'Rule priority')
    .option('--platform <platform>', 'Platform filter')
    .action((project, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'deduplication.add',
        globalOptions: g,
        project,
        name: opts['name'],
        rules: opts['rules'],
        priority: opts['priority'],
        platform: opts['platform'],
      } satisfies DeduplicationAddCommand;
    });
  cmd_deduplication.command('delete', { hidden: !revealHidden })
    .argument('<project>', 'project')
    .option('--name <name>', 'Rule name')
    .action((project, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'deduplication.delete',
        globalOptions: g,
        project,
        name: opts['name'],
      } satisfies DeduplicationDeleteCommand;
    });
  cmd_deduplication.command('modify', { hidden: !revealHidden })
    .argument('<project>', 'project')
    .option('--name <name>', 'Rule name')
    .option('--rules <rules>', 'Rules JSON file')
    .option('--priority <priority>', 'Rule priority')
    .action((project, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'deduplication.modify',
        globalOptions: g,
        project,
        name: opts['name'],
        rules: opts['rules'],
        priority: opts['priority'],
      } satisfies DeduplicationModifyCommand;
    });
  cmd_deduplication.command('list', { hidden: !revealHidden })
    .argument('<project>', 'project')
    .option('--name <name>', 'Filter by rule name')
    .option('--verbose', 'Show detailed rule info', false)
    .action((project, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'deduplication.list',
        globalOptions: g,
        project,
        name: opts['name'],
        verbose: opts['verbose'],
      } satisfies DeduplicationListCommand;
    });

  // --- latency ---
  const cmd_latency = program.command('latency', { hidden: !revealHidden });
  cmd_latency.command('list', { hidden: !revealHidden })
    .argument('[pattern]', 'pattern')
    .action((pattern, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'latency.list',
        globalOptions: g,
        pattern,
      } satisfies LatencyListCommand;
    });
  cmd_latency.command('activate', { hidden: !revealHidden })
    .argument('<name>', 'name')
    .option('--samples <samples>', 'Number of samples')
    .action((name, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'latency.activate',
        globalOptions: g,
        name,
        samples: opts['samples'],
      } satisfies LatencyActivateCommand;
    });
  cmd_latency.command('deactivate', { hidden: !revealHidden })
    .argument('<name>', 'name')
    .action((name, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'latency.deactivate',
        globalOptions: g,
        name,
      } satisfies LatencyDeactivateCommand;
    });
  cmd_latency.command('extract', { hidden: !revealHidden })
    .argument('<name>', 'name')
    .option('--raw', 'Output raw values', false)
    .action((name, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'latency.extract',
        globalOptions: g,
        name,
        raw: opts['raw'],
      } satisfies LatencyExtractCommand;
    });

  // --- cts ---
  program.command('cts', { hidden: !revealHidden })
    .argument('<project>', 'project')
    .argument('<attribute>', 'attribute')
    .argument('<value>', 'value')
    .option('--query', 'Show query JSON without executing', false)
    .action((project, attribute, value, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'cts',
        globalOptions: g,
        project,
        attribute,
        value,
        query: opts['query'],
      } satisfies CtsCommand;
    });

  // --- session ---
  const cmd_session = program.command('session', { hidden: !revealHidden });
  cmd_session.command('list', { hidden: !revealHidden })
    .addOption(new Option('--scope <scope>', 'Session scope').choices(["global","user","session"]))
    .option('-g', 'Shorthand for --scope=global', false)
    .option('-u', 'Shorthand for --scope=user', false)
    .option('-s', 'Shorthand for --scope=session', false)
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'session.list',
        globalOptions: g,
        scope: opts['scope'],
        g: opts['g'],
        u: opts['u'],
        s: opts['s'],
      } satisfies SessionListCommand;
    });
  cmd_session.command('set', { hidden: !revealHidden })
    .argument('<resources_json>', 'resources_json')
    .addOption(new Option('--persist <persist>', 'Persist to configuration').choices(["universe","user"]))
    .option('--uid <uid>', 'User ID for persistence')
    .action((resources_json, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'session.set',
        globalOptions: g,
        resources_json,
        persist: opts['persist'],
        uid: opts['uid'],
      } satisfies SessionSetCommand;
    });
  cmd_session.command('unset', { hidden: !revealHidden })
    .argument('<resource_names...>', 'resource_names')
    .action((resource_names, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'session.unset',
        globalOptions: g,
        resource_names,
      } satisfies SessionUnsetCommand;
    });

  // --- limit ---
  const cmd_limit = program.command('limit', { hidden: !revealHidden });
  cmd_limit.command('list', { hidden: !revealHidden })
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'limit.list',
        globalOptions: g,
      } satisfies LimitListCommand;
    });
  cmd_limit.command('reset', { hidden: !revealHidden })
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'limit.reset',
        globalOptions: g,
      } satisfies LimitResetCommand;
    });
  cmd_limit.command('delete', { hidden: !revealHidden })
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'limit.delete',
        globalOptions: g,
      } satisfies LimitDeleteCommand;
    });
  cmd_limit.command('create', { hidden: !revealHidden })
    .option('--submissions <submissions>', 'Submission limit')
    .option('--metadata <metadata>', 'Metadata JSON')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'limit.create',
        globalOptions: g,
        submissions: opts['submissions'],
        metadata: opts['metadata'],
      } satisfies LimitCreateCommand;
    });

  // --- service ---
  const cmd_service = program.command('service', { hidden: !revealHidden });
  cmd_service.command('list', { hidden: !revealHidden })
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'service.list',
        globalOptions: g,
      } satisfies ServiceListCommand;
    });
  cmd_service.command('status', { hidden: !revealHidden })
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'service.status',
        globalOptions: g,
      } satisfies ServiceStatusCommand;
    });
  cmd_service.command('rescan', { hidden: !revealHidden })
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'service.rescan',
        globalOptions: g,
      } satisfies ServiceRescanCommand;
    });

  // --- status ---
  const cmd_status = program.command('status', { hidden: !revealHidden });
  cmd_status.command('reload', { hidden: !revealHidden })
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'status.reload',
        globalOptions: g,
      } satisfies StatusReloadCommand;
    });

  return { program, getResult: () => result };
}
