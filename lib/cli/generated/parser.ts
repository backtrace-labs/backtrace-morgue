// AUTO-GENERATED -- DO NOT EDIT
// Generated by scripts/generate-cli.ts from usage.json

import { Command, Option } from 'commander';
import type {
  CliCommand,
  GlobalOptions,
  LoginCommand,
  LogoutCommand,
  CleanCommand,
  DescribeCommand,
  GetCommand,
  PutCommand,
  SetCommand,
  AttachmentAddCommand,
  AttachmentGetCommand,
  AttachmentListCommand,
  AttachmentDeleteCommand,
  ListCommand,
  DeleteCommand,
  ProjectCreateCommand,
  ProjectsListCommand,
  FlamegraphCommand,
  SymboldStatusCommand,
  SymboldSymbolserverListCommand,
  SymboldSymbolserverDetailsCommand,
  SymboldSymbolserverLogsCommand,
  SymboldSymbolserverLogsFilterCommand,
  SymboldSymbolserverAddCommand,
  SymboldSymbolserverUpdateCommand,
  SymboldSymbolserverDeleteCommand,
  SymboldSymbolserverDisableCommand,
  SymboldSymbolserverEnableCommand,
  SymboldWhitelistAddCommand,
  SymboldWhitelistRemoveCommand,
  SymboldWhitelistListCommand,
  SymboldBlacklistAddCommand,
  SymboldBlacklistRemoveCommand,
  SymboldBlacklistListCommand,
  SymboldSkiplistFindCommand,
  SymboldSkiplistRemoveCommand,
  SymboldSkiplistRemoveAllCommand,
  SymboldSkiplistRemoveFilterCommand,
  SymboldQueueListCommand,
  SymboldQueueAddCommand,
  SymboldQueueSizeCommand,
  SymboldQueueSymbolsCommand,
  ReportListCommand,
  ReportCreateCommand,
  ReportDeleteCommand,
  ReportSendCommand,
  MergeCommand,
  UnmergeCommand,
  RepairCommand,
  ReprocessCommand,
  RetentionListCommand,
  RetentionSetCommand,
  RetentionStatusCommand,
  RetentionClearCommand,
  SamplingStatusCommand,
  SamplingResetCommand,
  SamplingConfigureCommand,
  SymbolCommand,
  ScrubberListCommand,
  ScrubberCreateCommand,
  ScrubberModifyCommand,
  ScrubberDeleteCommand,
  SetupCommand,
  NukeCommand,
  TokenCreateCommand,
  TokenListCommand,
  TokenDeleteCommand,
  UserResetCommand,
  UsersAddSignupWhitelistCommand,
  UsersListTeamlessUsersCommand,
  TenantCreateCommand,
  TenantDeleteCommand,
  TenantListCommand,
  SimilarityCommand,
  InviteCreateCommand,
  InviteListCommand,
  InviteDeleteCommand,
  CallstackEvaluateCommand,
  AccessTeamCreateCommand,
  AccessTeamRemoveCommand,
  AccessTeamDetailsCommand,
  AccessTeamAddUserCommand,
  AccessTeamRemoveUserCommand,
  AccessTeamListCommand,
  AccessProjectAddTeamCommand,
  AccessProjectRemoveTeamCommand,
  AccessProjectAddUserCommand,
  AccessProjectRemoveUserCommand,
  AccessProjectDetailsCommand,
  StabilityCreateMetricCommand,
  MetricsImporterSourceCheckQueryCommand,
  MetricsImporterImporterCreateCommand,
  MetricsImporterLogsCommand,
  AlertsTargetCreateCommand,
  AlertsTargetListCommand,
  AlertsTargetGetCommand,
  AlertsTargetUpdateCommand,
  AlertsTargetDeleteCommand,
  AlertsAlertListCommand,
  AlertsAlertGetCommand,
  AlertsAlertUpdateCommand,
  AlertsAlertDeleteCommand,
  AlertsAlertCreateCommand,
  WorkflowsConnectionCreateCommand,
  WorkflowsConnectionListCommand,
  WorkflowsConnectionGetCommand,
  WorkflowsConnectionUpdateCommand,
  WorkflowsConnectionDeleteCommand,
  WorkflowsIntegrationCreateCommand,
  WorkflowsIntegrationListCommand,
  WorkflowsIntegrationGetCommand,
  WorkflowsIntegrationUpdateCommand,
  WorkflowsIntegrationDeleteCommand,
  WorkflowsAlertListCommand,
  WorkflowsAlertGetCommand,
  WorkflowsAlertCreateCommand,
  WorkflowsAlertUpdateCommand,
  WorkflowsAlertDeleteCommand,
  ActionsGetCommand,
  ActionsDisableCommand,
  ActionsEnableCommand,
  ActionsUploadCommand,
  ActionsDeleteCommand,
  AttributeCreateCommand,
  AttributeDeleteCommand,
  ViewCreateCommand,
  ViewDeleteCommand,
  ErrorCommand,
  LogListCommand,
  LogActivateCommand,
  LogDeactivateCommand,
  LogExtractCommand,
  AuditExtractCommand,
  BpgListCommand,
  ControlCommand,
  DeduplicationAddCommand,
  DeduplicationDeleteCommand,
  DeduplicationModifyCommand,
  DeduplicationListCommand,
  LatencyListCommand,
  LatencyActivateCommand,
  LatencyDeactivateCommand,
  LatencyExtractCommand,
  CtsCommand,
  SessionListCommand,
  SessionSetCommand,
  SessionUnsetCommand,
  LimitListCommand,
  LimitResetCommand,
  LimitDeleteCommand,
  LimitCreateCommand,
  ServiceListCommand,
  ServiceStatusCommand,
  ServiceRescanCommand,
  StatusReloadCommand,
} from './types';

const revealHidden = !!process.env.MORGUE_REVEAL_HIDDEN;

function collectRepeatable(val: string, prev: string[]): string[] {
  return prev.concat([val]);
}

function extractGlobalOptions(cmd: Command): GlobalOptions {
  const opts = cmd.optsWithGlobals();
  return {
    endpoint: opts['endpoint'],
    token: opts['token'],
    universe: opts['universe'],
    project: opts['project'],
    timeout: opts['timeout'],
    debug: opts['debug'] ?? false,
    k: opts['k'] ?? false,
  };
}

/**
 * Bidirectional sync between global options and command-local options.
 *
 * Some flags (like --universe, --project, --token) exist both as global flags
 * and as command-specific flags. Commander treats these as separate: a flag
 * before the command name goes to the global, and after goes to the subcommand.
 * But from the user's perspective there is no meaningful distinction — they
 * expect `morgue --universe X nuke` and `morgue nuke --universe X` to behave
 * identically.
 *
 * More critically, if code reads globalOptions.universe in one place and
 * cmd.universe in another, a mismatch could lead to operating on the wrong
 * universe — potentially disastrous for destructive commands like nuke/delete.
 *
 * This function ensures the two are always in sync:
 *   1. Command-local value wins if present (user placed it closest to the command).
 *   2. Otherwise, inherit the global value.
 *   3. The winning value is written back to both globalOptions and opts,
 *      so all downstream code sees the same value regardless of which it reads.
 */
function syncGlobalAndCommandFlags(g: GlobalOptions, opts: Record<string, any>): void {
  if (opts['endpoint'] !== undefined) { g.endpoint = opts['endpoint']; }
  else if (g.endpoint !== undefined) { opts['endpoint'] = g.endpoint; }
  if (opts['token'] !== undefined) { g.token = opts['token']; }
  else if (g.token !== undefined) { opts['token'] = g.token; }
  if (opts['universe'] !== undefined) { g.universe = opts['universe']; }
  else if (g.universe !== undefined) { opts['universe'] = g.universe; }
  if (opts['project'] !== undefined) { g.project = opts['project']; }
  else if (g.project !== undefined) { opts['project'] = g.project; }
  if (opts['timeout'] !== undefined) { g.timeout = opts['timeout']; }
  else if (g.timeout !== undefined) { opts['timeout'] = g.timeout; }
}

export function createProgram(): { program: Command; getResult: () => CliCommand | undefined } {
  let result: CliCommand | undefined;
  const program = new Command('morgue');
  program.allowExcessArguments(true);

  program.option('--endpoint <url>', 'Specify API endpoint');
  program.option('--token <token>', 'Specify API token');
  program.option('--universe <universe>', 'Universe name');
  program.option('--project <project>', 'Project name');
  program.option('--timeout <ms>', 'Set the timeout on API requests in milliseconds');
  program.option('--debug', 'Enable verbose debug printing', false);
  program.option('-k', 'Disable SSL verification with CA', false);

  // --- login ---
  program.command('login')
    .description('Log into a Backtrace server')
    .argument('<url>', 'Server URL (e.g., http://localhost)')
    .action((url, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'login',
        globalOptions: g,
        url,
      } satisfies LoginCommand;
    });

  // --- logout ---
  program.command('logout')
    .description('Log out from the current Backtrace server.')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'logout',
        globalOptions: g,
      } satisfies LogoutCommand;
    });

  // --- clean ---
  program.command('clean')
    .description('Apply sampling retroactively.')
    .argument('<project>', 'Project name or universe/project')
    .option('--filter <filter>', 'Filter expression: attribute,operation,value')
    .option('--limit <n>', 'Number of rows to return')
    .option('--offset <n>', 'Skip first N rows')
    .option('--select <attribute>', 'Select specific attributes', collectRepeatable, [])
    .addOption(new Option('--select-wildcard <type>', 'Select all attributes of the given type').choices(["physical","derived","virtual"]))
    .option('--age <time>', 'Relative timestamp (e.g., 1h, 42d, 10s, etc). Valid suffixes: y (years), M (months), w (weeks), d (days), h (hours), m (minutes), s (seconds)')
    .option('--time <range>', 'Time range using natural language')
    .option('--unique <attribute>', 'Count distinct values', collectRepeatable, [])
    .option('--histogram <attribute>', 'Show all distinct values', collectRepeatable, [])
    .option('--distribution <attribute>', 'Show truncated histogram', collectRepeatable, [])
    .option('--mean <attribute>', 'Calculate mean', collectRepeatable, [])
    .option('--sum <attribute>', 'Sum all values', collectRepeatable, [])
    .option('--range <attribute>', 'Show min and max values', collectRepeatable, [])
    .option('--count <attribute>', 'Count non-null values', collectRepeatable, [])
    .option('--bin <attribute>', 'Linear histogram of values', collectRepeatable, [])
    .option('--head <attribute>', 'First value in factor', collectRepeatable, [])
    .option('--tail <attribute>', 'Last value in factor', collectRepeatable, [])
    .option('--object <attribute>', 'Maximum object identifier', collectRepeatable, [])
    .addOption(new Option('--last <attribute>', 'Last object identifier').argParser(collectRepeatable).default([]).hideHelp(!revealHidden))
    .addOption(new Option('--first <attribute>', 'First object identifier').argParser(collectRepeatable).default([]).hideHelp(!revealHidden))
    .addOption(new Option('--min <attribute>', 'Min object identifier').argParser(collectRepeatable).default([]).hideHelp(!revealHidden))
    .addOption(new Option('--max <attribute>', 'Max object identifier').argParser(collectRepeatable).default([]).hideHelp(!revealHidden))
    .option('--sort <term>', 'Sort results: [-](column|fold_term)', collectRepeatable, [])
    .option('--quantize-uint <spec>', 'Compute quantized column: output_column,input_column,size[,offset]', collectRepeatable, [])
    .addOption(new Option('--raw-query <query>', 'Raw query to use (overrides all other flags)').hideHelp(!revealHidden))
    .addOption(new Option('--table <query>', 'Table to query against').hideHelp(!revealHidden))
    .addOption(new Option('--timestamp-attribute <query>', 'Attribute to use as timestamp').hideHelp(!revealHidden))
    .option('--reverse <attribute>', 'Reverse sort order')
    .option('--template <attribute>', 'Use a server-side named query template (e.g. select)')
    .option('--factor <attribute>', 'Group by attribute')
    .option('--fingerprint <value>', 'Filter by fingerprint: exact match (64 chars) or prefix regex')
    .option('--keep <n>', 'Number of objects to keep per fingerprint')
    .option('--oldest <n>', 'Keep N oldest objects')
    .option('--output', 'Output object IDs to stdout', false)
    .option('--verbose', 'Verbose output', false)
    .action((project, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'clean',
        globalOptions: g,
        queryOptions: {
          filter: opts['filter'],
          limit: opts['limit'],
          offset: opts['offset'],
          select: opts['select'],
          selectWildcard: opts['selectWildcard'],
          age: opts['age'],
          time: opts['time'],
          unique: opts['unique'],
          histogram: opts['histogram'],
          distribution: opts['distribution'],
          mean: opts['mean'],
          sum: opts['sum'],
          range: opts['range'],
          count: opts['count'],
          bin: opts['bin'],
          head: opts['head'],
          tail: opts['tail'],
          object: opts['object'],
          last: opts['last'],
          first: opts['first'],
          min: opts['min'],
          max: opts['max'],
          sort: opts['sort'],
          quantizeUint: opts['quantizeUint'],
          rawQuery: opts['rawQuery'],
          table: opts['table'],
          timestampAttribute: opts['timestampAttribute'],
          reverse: opts['reverse'],
          template: opts['template'],
          factor: opts['factor'],
          fingerprint: opts['fingerprint'],
        },
        project,
        keep: opts['keep'],
        oldest: opts['oldest'],
        output: opts['output'],
        verbose: opts['verbose'],
      } satisfies CleanCommand;
    });

  // --- describe ---
  program.command('describe')
    .description('Requests a list and description of all metadata that can be queried against.')
    .argument('<project>', 'Project name or universe/project')
    .argument('[substring]', 'Filter by substring')
    .option('-r', 'Show disabled attributes', false)
    .option('-u', 'Show only custom attributes', false)
    .option('-a', 'Show all attributes including unused', false)
    .option('-l', 'Show filter and aggregation info', false)
    .option('--json', 'Output JSON format', false)
    .addOption(new Option('--table <table>', 'Table to query against').hideHelp(!revealHidden))
    .action((project, substring, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'describe',
        globalOptions: g,
        project,
        substring,
        r: opts['r'],
        u: opts['u'],
        a: opts['a'],
        l: opts['l'],
        json: opts['json'],
        table: opts['table'],
      } satisfies DescribeCommand;
    });

  // --- get ---
  program.command('get')
    .description('Download object from the Backtrace object store.')
    .argument('<project>', 'Project name or universe/project')
    .argument('<object_id>', 'Object identifier')
    .option('-o, --output <file>', 'Output to file')
    .option('--resource <name>', 'Fetch specific resource')
    .addOption(new Option('--outdir <dir>', 'Output directory for multiple objects').hideHelp(!revealHidden))
    .addOption(new Option('--first <id>', 'First object ID in range').hideHelp(!revealHidden))
    .addOption(new Option('--last <id>', 'Last object ID in range').hideHelp(!revealHidden))
    .action((project, object_id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'get',
        globalOptions: g,
        project,
        object_id,
        output: opts['output'],
        resource: opts['resource'],
        outdir: opts['outdir'],
        first: opts['first'],
        last: opts['last'],
      } satisfies GetCommand;
    });

  // --- put ---
  program.command('put')
    .description('Uploads object file to the Backtrace object store.')
    .argument('<project>', 'Project name or universe/project')
    .argument('<file>', 'File to upload')
    .addOption(new Option('--format <format>', 'File format').choices(["btt","minidump","json","plcrash","symbols","symbols-proguard","sourcemap"]))
    .addOption(new Option('--compression <type>', 'Compression: gzip|deflate').choices(["gzip","deflate"]))
    .option('--kv <pairs>', 'Key-value pairs: key1:value1,key2:value2')
    .option('--form_data', 'Upload using multipart/form-data POST request', false)
    .addOption(new Option('--tag <tag>', 'Symbol tag (used with symbols format)').hideHelp(!revealHidden))
    .addOption(new Option('--symbolication-id <id>', 'Symbolication identifier').hideHelp(!revealHidden))
    .addOption(new Option('--sync', 'Synchronous submission').default(false).hideHelp(!revealHidden))
    .addOption(new Option('--reuse', 'Reuse HTTP connections').default(false).hideHelp(!revealHidden))
    .addOption(new Option('--attachment <file>', 'Attachment file(s) for minidump uploads').hideHelp(!revealHidden))
    .addOption(new Option('--multipart', 'Force multipart form submission').default(false).hideHelp(!revealHidden))
    .addOption(new Option('--benchmark', 'Run submission benchmark').default(false).hideHelp(!revealHidden))
    .addOption(new Option('--samples <n>', 'Number of benchmark samples').hideHelp(!revealHidden))
    .addOption(new Option('--concurrency <n>', 'Benchmark concurrency level').hideHelp(!revealHidden))
    .addOption(new Option('--printids', 'Print object IDs after upload').default(false).hideHelp(!revealHidden))
    .action((project, file, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'put',
        globalOptions: g,
        project,
        file,
        format: opts['format'],
        compression: opts['compression'],
        kv: opts['kv'],
        form_data: opts['form_data'],
        tag: opts['tag'],
        symbolicationId: opts['symbolicationId'],
        sync: opts['sync'],
        reuse: opts['reuse'],
        attachment: opts['attachment'],
        multipart: opts['multipart'],
        benchmark: opts['benchmark'],
        samples: opts['samples'],
        concurrency: opts['concurrency'],
        printids: opts['printids'],
      } satisfies PutCommand;
    });

  // --- set ---
  program.command('set')
    .description('Modify object attributes')
    .argument('<project>', 'Project name or universe/project')
    .argument('<query>', 'Query or object ID')
    .argument('<assignment>', 'key=value assignment')
    .option('--filter <filter>', 'Filter expression: attribute,operation,value')
    .option('--limit <n>', 'Number of rows to return')
    .option('--offset <n>', 'Skip first N rows')
    .option('--select <attribute>', 'Select specific attributes', collectRepeatable, [])
    .addOption(new Option('--select-wildcard <type>', 'Select all attributes of the given type').choices(["physical","derived","virtual"]))
    .option('--age <time>', 'Relative timestamp (e.g., 1h, 42d, 10s, etc). Valid suffixes: y (years), M (months), w (weeks), d (days), h (hours), m (minutes), s (seconds)')
    .option('--time <range>', 'Time range using natural language')
    .option('--unique <attribute>', 'Count distinct values', collectRepeatable, [])
    .option('--histogram <attribute>', 'Show all distinct values', collectRepeatable, [])
    .option('--distribution <attribute>', 'Show truncated histogram', collectRepeatable, [])
    .option('--mean <attribute>', 'Calculate mean', collectRepeatable, [])
    .option('--sum <attribute>', 'Sum all values', collectRepeatable, [])
    .option('--range <attribute>', 'Show min and max values', collectRepeatable, [])
    .option('--count <attribute>', 'Count non-null values', collectRepeatable, [])
    .option('--bin <attribute>', 'Linear histogram of values', collectRepeatable, [])
    .option('--head <attribute>', 'First value in factor', collectRepeatable, [])
    .option('--tail <attribute>', 'Last value in factor', collectRepeatable, [])
    .option('--object <attribute>', 'Maximum object identifier', collectRepeatable, [])
    .addOption(new Option('--last <attribute>', 'Last object identifier').argParser(collectRepeatable).default([]).hideHelp(!revealHidden))
    .addOption(new Option('--first <attribute>', 'First object identifier').argParser(collectRepeatable).default([]).hideHelp(!revealHidden))
    .addOption(new Option('--min <attribute>', 'Min object identifier').argParser(collectRepeatable).default([]).hideHelp(!revealHidden))
    .addOption(new Option('--max <attribute>', 'Max object identifier').argParser(collectRepeatable).default([]).hideHelp(!revealHidden))
    .option('--sort <term>', 'Sort results: [-](column|fold_term)', collectRepeatable, [])
    .option('--quantize-uint <spec>', 'Compute quantized column: output_column,input_column,size[,offset]', collectRepeatable, [])
    .addOption(new Option('--raw-query <query>', 'Raw query to use (overrides all other flags)').hideHelp(!revealHidden))
    .addOption(new Option('--table <query>', 'Table to query against').hideHelp(!revealHidden))
    .addOption(new Option('--timestamp-attribute <query>', 'Attribute to use as timestamp').hideHelp(!revealHidden))
    .option('--reverse <attribute>', 'Reverse sort order')
    .option('--template <attribute>', 'Use a server-side named query template (e.g. select)')
    .option('--factor <attribute>', 'Group by attribute')
    .option('--fingerprint <value>', 'Filter by fingerprint: exact match (64 chars) or prefix regex')
    .action((project, query, assignment, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'set',
        globalOptions: g,
        queryOptions: {
          filter: opts['filter'],
          limit: opts['limit'],
          offset: opts['offset'],
          select: opts['select'],
          selectWildcard: opts['selectWildcard'],
          age: opts['age'],
          time: opts['time'],
          unique: opts['unique'],
          histogram: opts['histogram'],
          distribution: opts['distribution'],
          mean: opts['mean'],
          sum: opts['sum'],
          range: opts['range'],
          count: opts['count'],
          bin: opts['bin'],
          head: opts['head'],
          tail: opts['tail'],
          object: opts['object'],
          last: opts['last'],
          first: opts['first'],
          min: opts['min'],
          max: opts['max'],
          sort: opts['sort'],
          quantizeUint: opts['quantizeUint'],
          rawQuery: opts['rawQuery'],
          table: opts['table'],
          timestampAttribute: opts['timestampAttribute'],
          reverse: opts['reverse'],
          template: opts['template'],
          factor: opts['factor'],
          fingerprint: opts['fingerprint'],
        },
        project,
        query,
        assignment,
      } satisfies SetCommand;
    });

  // --- attachment ---
  const cmd_attachment = program.command('attachment');
  cmd_attachment.description('Manage object attachments');
  cmd_attachment.command('add')
    .description('Add attachment to object')
    .argument('<project>', 'Project name')
    .argument('<oid>', 'Object ID')
    .argument('<filename>', 'File to attach')
    .option('--content-type <id>', 'Content-Type for attachment')
    .option('--attachment-name <name>', 'Attachment name')
    .action((project, oid, filename, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'attachment.add',
        globalOptions: g,
        project,
        oid,
        filename,
        contentType: opts['contentType'],
        attachmentName: opts['attachmentName'],
      } satisfies AttachmentAddCommand;
    });
  cmd_attachment.command('get')
    .description('Download attachment')
    .argument('<project>', 'Project name')
    .argument('<oid>', 'Object ID')
    .option('--attachment-id <id>', 'Attachment ID')
    .option('--attachment-name <name>', 'Attachment name')
    .action((project, oid, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'attachment.get',
        globalOptions: g,
        project,
        oid,
        attachmentId: opts['attachmentId'],
        attachmentName: opts['attachmentName'],
      } satisfies AttachmentGetCommand;
    });
  cmd_attachment.command('list')
    .description('List attachments')
    .argument('<project>', 'Project name')
    .argument('<oid>', 'Object ID')
    .action((project, oid, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'attachment.list',
        globalOptions: g,
        project,
        oid,
      } satisfies AttachmentListCommand;
    });
  cmd_attachment.command('delete')
    .description('Delete attachment')
    .argument('<project>', 'Project name')
    .argument('<oid>', 'Object ID')
    .option('--attachment-id <id>', 'Attachment ID')
    .option('--attachment-name <name>', 'Attachment name')
    .action((project, oid, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'attachment.delete',
        globalOptions: g,
        project,
        oid,
        attachmentId: opts['attachmentId'],
        attachmentName: opts['attachmentName'],
      } satisfies AttachmentDeleteCommand;
    });

  // --- list ---
  program.command('list')
    .description('Query and list objects with filters and aggregations')
    .argument('<project>', 'Project name or universe/project')
    .argument('[substring]', 'Optional substring filter')
    .option('--filter <filter>', 'Filter expression: attribute,operation,value')
    .option('--limit <n>', 'Number of rows to return')
    .option('--offset <n>', 'Skip first N rows')
    .option('--select <attribute>', 'Select specific attributes', collectRepeatable, [])
    .addOption(new Option('--select-wildcard <type>', 'Select all attributes of the given type').choices(["physical","derived","virtual"]))
    .option('--age <time>', 'Relative timestamp (e.g., 1h, 42d, 10s, etc). Valid suffixes: y (years), M (months), w (weeks), d (days), h (hours), m (minutes), s (seconds)')
    .option('--time <range>', 'Time range using natural language')
    .option('--unique <attribute>', 'Count distinct values', collectRepeatable, [])
    .option('--histogram <attribute>', 'Show all distinct values', collectRepeatable, [])
    .option('--distribution <attribute>', 'Show truncated histogram', collectRepeatable, [])
    .option('--mean <attribute>', 'Calculate mean', collectRepeatable, [])
    .option('--sum <attribute>', 'Sum all values', collectRepeatable, [])
    .option('--range <attribute>', 'Show min and max values', collectRepeatable, [])
    .option('--count <attribute>', 'Count non-null values', collectRepeatable, [])
    .option('--bin <attribute>', 'Linear histogram of values', collectRepeatable, [])
    .option('--head <attribute>', 'First value in factor', collectRepeatable, [])
    .option('--tail <attribute>', 'Last value in factor', collectRepeatable, [])
    .option('--object <attribute>', 'Maximum object identifier', collectRepeatable, [])
    .addOption(new Option('--last <attribute>', 'Last object identifier').argParser(collectRepeatable).default([]).hideHelp(!revealHidden))
    .addOption(new Option('--first <attribute>', 'First object identifier').argParser(collectRepeatable).default([]).hideHelp(!revealHidden))
    .addOption(new Option('--min <attribute>', 'Min object identifier').argParser(collectRepeatable).default([]).hideHelp(!revealHidden))
    .addOption(new Option('--max <attribute>', 'Max object identifier').argParser(collectRepeatable).default([]).hideHelp(!revealHidden))
    .option('--sort <term>', 'Sort results: [-](column|fold_term)', collectRepeatable, [])
    .option('--quantize-uint <spec>', 'Compute quantized column: output_column,input_column,size[,offset]', collectRepeatable, [])
    .addOption(new Option('--raw-query <query>', 'Raw query to use (overrides all other flags)').hideHelp(!revealHidden))
    .addOption(new Option('--table <query>', 'Table to query against').hideHelp(!revealHidden))
    .addOption(new Option('--timestamp-attribute <query>', 'Attribute to use as timestamp').hideHelp(!revealHidden))
    .option('--reverse <attribute>', 'Reverse sort order')
    .option('--template <attribute>', 'Use a server-side named query template (e.g. select)')
    .option('--factor <attribute>', 'Group by attribute')
    .option('--fingerprint <value>', 'Filter by fingerprint: exact match (64 chars) or prefix regex')
    .option('--csv <file>', 'Output results to a CSV file')
    .option('--json', 'Output JSON format', false)
    .option('--query', 'Show the built query JSON and exit without performing the request', false)
    .option('--verbose', 'Show detailed query performance data', false)
    .addOption(new Option('--raw', 'Output raw JSON response (use with --query to also execute)').default(false).hideHelp(!revealHidden))
    .addOption(new Option('--set <assignment>', 'Set attributes on matching objects: key=value').hideHelp(!revealHidden))
    .addOption(new Option('--clear <attribute>', 'Clear attributes on matching objects').hideHelp(!revealHidden))
    .addOption(new Option('--implicit-filters <bool>', 'Control implicit time filters (default: true)').hideHelp(!revealHidden))
    .addOption(new Option('--benchmark', 'Run query benchmark').default(false).hideHelp(!revealHidden))
    .addOption(new Option('--concurrency <n>', 'Benchmark concurrency level').hideHelp(!revealHidden))
    .addOption(new Option('--samples <n>', 'Number of benchmark samples').hideHelp(!revealHidden))
    .action((project, substring, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'list',
        globalOptions: g,
        queryOptions: {
          filter: opts['filter'],
          limit: opts['limit'],
          offset: opts['offset'],
          select: opts['select'],
          selectWildcard: opts['selectWildcard'],
          age: opts['age'],
          time: opts['time'],
          unique: opts['unique'],
          histogram: opts['histogram'],
          distribution: opts['distribution'],
          mean: opts['mean'],
          sum: opts['sum'],
          range: opts['range'],
          count: opts['count'],
          bin: opts['bin'],
          head: opts['head'],
          tail: opts['tail'],
          object: opts['object'],
          last: opts['last'],
          first: opts['first'],
          min: opts['min'],
          max: opts['max'],
          sort: opts['sort'],
          quantizeUint: opts['quantizeUint'],
          rawQuery: opts['rawQuery'],
          table: opts['table'],
          timestampAttribute: opts['timestampAttribute'],
          reverse: opts['reverse'],
          template: opts['template'],
          factor: opts['factor'],
          fingerprint: opts['fingerprint'],
        },
        project,
        substring,
        csv: opts['csv'],
        json: opts['json'],
        query: opts['query'],
        verbose: opts['verbose'],
        raw: opts['raw'],
        set: opts['set'],
        clear: opts['clear'],
        implicitFilters: opts['implicitFilters'],
        benchmark: opts['benchmark'],
        concurrency: opts['concurrency'],
        samples: opts['samples'],
      } satisfies ListCommand;
    });

  // --- delete ---
  program.command('delete')
    .description('Delete objects by query or ID')
    .argument('<project>', 'Project name or universe/project')
    .argument('[target...]', 'Query or object IDs')
    .option('--filter <filter>', 'Filter expression: attribute,operation,value')
    .option('--limit <n>', 'Number of rows to return')
    .option('--offset <n>', 'Skip first N rows')
    .option('--select <attribute>', 'Select specific attributes', collectRepeatable, [])
    .addOption(new Option('--select-wildcard <type>', 'Select all attributes of the given type').choices(["physical","derived","virtual"]))
    .option('--age <time>', 'Relative timestamp (e.g., 1h, 42d, 10s, etc). Valid suffixes: y (years), M (months), w (weeks), d (days), h (hours), m (minutes), s (seconds)')
    .option('--time <range>', 'Time range using natural language')
    .option('--unique <attribute>', 'Count distinct values', collectRepeatable, [])
    .option('--histogram <attribute>', 'Show all distinct values', collectRepeatable, [])
    .option('--distribution <attribute>', 'Show truncated histogram', collectRepeatable, [])
    .option('--mean <attribute>', 'Calculate mean', collectRepeatable, [])
    .option('--sum <attribute>', 'Sum all values', collectRepeatable, [])
    .option('--range <attribute>', 'Show min and max values', collectRepeatable, [])
    .option('--count <attribute>', 'Count non-null values', collectRepeatable, [])
    .option('--bin <attribute>', 'Linear histogram of values', collectRepeatable, [])
    .option('--head <attribute>', 'First value in factor', collectRepeatable, [])
    .option('--tail <attribute>', 'Last value in factor', collectRepeatable, [])
    .option('--object <attribute>', 'Maximum object identifier', collectRepeatable, [])
    .addOption(new Option('--min <attribute>', 'Min object identifier').argParser(collectRepeatable).default([]).hideHelp(!revealHidden))
    .addOption(new Option('--max <attribute>', 'Max object identifier').argParser(collectRepeatable).default([]).hideHelp(!revealHidden))
    .option('--sort <term>', 'Sort results: [-](column|fold_term)', collectRepeatable, [])
    .option('--quantize-uint <spec>', 'Compute quantized column: output_column,input_column,size[,offset]', collectRepeatable, [])
    .addOption(new Option('--raw-query <query>', 'Raw query to use (overrides all other flags)').hideHelp(!revealHidden))
    .addOption(new Option('--table <query>', 'Table to query against').hideHelp(!revealHidden))
    .addOption(new Option('--timestamp-attribute <query>', 'Attribute to use as timestamp').hideHelp(!revealHidden))
    .option('--reverse <attribute>', 'Reverse sort order')
    .option('--template <attribute>', 'Use a server-side named query template (e.g. select)')
    .option('--factor <attribute>', 'Group by attribute')
    .option('--fingerprint <value>', 'Filter by fingerprint: exact match (64 chars) or prefix regex')
    .option('--physical-only', 'Delete only physical data (default)', false)
    .option('--crdb-only', 'Delete only indexed data', false)
    .option('--all', 'Delete all related data', false)
    .addOption(new Option('--sync', 'Synchronous deletion').default(false).hideHelp(!revealHidden))
    .addOption(new Option('--chunklen <n>', 'Batch size for deletions').hideHelp(!revealHidden))
    .addOption(new Option('--first <id>', 'First object ID in range').hideHelp(!revealHidden))
    .addOption(new Option('--last <id>', 'Last object ID in range').hideHelp(!revealHidden))
    .action((project, target, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'delete',
        globalOptions: g,
        queryOptions: {
          filter: opts['filter'],
          limit: opts['limit'],
          offset: opts['offset'],
          select: opts['select'],
          selectWildcard: opts['selectWildcard'],
          age: opts['age'],
          time: opts['time'],
          unique: opts['unique'],
          histogram: opts['histogram'],
          distribution: opts['distribution'],
          mean: opts['mean'],
          sum: opts['sum'],
          range: opts['range'],
          count: opts['count'],
          bin: opts['bin'],
          head: opts['head'],
          tail: opts['tail'],
          object: opts['object'],
          min: opts['min'],
          max: opts['max'],
          sort: opts['sort'],
          quantizeUint: opts['quantizeUint'],
          rawQuery: opts['rawQuery'],
          table: opts['table'],
          timestampAttribute: opts['timestampAttribute'],
          reverse: opts['reverse'],
          template: opts['template'],
          factor: opts['factor'],
          fingerprint: opts['fingerprint'],
        },
        project,
        target,
        physicalOnly: opts['physicalOnly'],
        crdbOnly: opts['crdbOnly'],
        all: opts['all'],
        sync: opts['sync'],
        chunklen: opts['chunklen'],
        first: opts['first'],
        last: opts['last'],
      } satisfies DeleteCommand;
    });

  // --- project ---
  const cmd_project = program.command('project');
  cmd_project.description('Create projects');
  cmd_project.command('create')
    .description('Create new project')
    .argument('<name>', 'Project name')
    .action((name, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'project.create',
        globalOptions: g,
        name,
      } satisfies ProjectCreateCommand;
    });

  // --- projects ---
  const cmd_projects = program.command('projects');
  cmd_projects.description('List projects');
  cmd_projects.command('list')
    .description('List all projects')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'projects.list',
        globalOptions: g,
      } satisfies ProjectsListCommand;
    });

  // --- flamegraph ---
  program.command('flamegraph')
    .description('Generate callstack flamegraph')
    .argument('<project>', 'Project name or universe/project')
    .option('--filter <filter>', 'Filter expression: attribute,operation,value')
    .option('--limit <n>', 'Number of rows to return')
    .option('--offset <n>', 'Skip first N rows')
    .option('--select <attribute>', 'Select specific attributes', collectRepeatable, [])
    .addOption(new Option('--select-wildcard <type>', 'Select all attributes of the given type').choices(["physical","derived","virtual"]))
    .option('--age <time>', 'Relative timestamp (e.g., 1h, 42d, 10s, etc). Valid suffixes: y (years), M (months), w (weeks), d (days), h (hours), m (minutes), s (seconds)')
    .option('--time <range>', 'Time range using natural language')
    .option('--histogram <attribute>', 'Show all distinct values', collectRepeatable, [])
    .option('--distribution <attribute>', 'Show truncated histogram', collectRepeatable, [])
    .option('--mean <attribute>', 'Calculate mean', collectRepeatable, [])
    .option('--sum <attribute>', 'Sum all values', collectRepeatable, [])
    .option('--range <attribute>', 'Show min and max values', collectRepeatable, [])
    .option('--count <attribute>', 'Count non-null values', collectRepeatable, [])
    .option('--bin <attribute>', 'Linear histogram of values', collectRepeatable, [])
    .option('--head <attribute>', 'First value in factor', collectRepeatable, [])
    .option('--tail <attribute>', 'Last value in factor', collectRepeatable, [])
    .option('--object <attribute>', 'Maximum object identifier', collectRepeatable, [])
    .addOption(new Option('--last <attribute>', 'Last object identifier').argParser(collectRepeatable).default([]).hideHelp(!revealHidden))
    .addOption(new Option('--first <attribute>', 'First object identifier').argParser(collectRepeatable).default([]).hideHelp(!revealHidden))
    .addOption(new Option('--min <attribute>', 'Min object identifier').argParser(collectRepeatable).default([]).hideHelp(!revealHidden))
    .addOption(new Option('--max <attribute>', 'Max object identifier').argParser(collectRepeatable).default([]).hideHelp(!revealHidden))
    .option('--sort <term>', 'Sort results: [-](column|fold_term)', collectRepeatable, [])
    .option('--quantize-uint <spec>', 'Compute quantized column: output_column,input_column,size[,offset]', collectRepeatable, [])
    .addOption(new Option('--raw-query <query>', 'Raw query to use (overrides all other flags)').hideHelp(!revealHidden))
    .addOption(new Option('--table <query>', 'Table to query against').hideHelp(!revealHidden))
    .addOption(new Option('--timestamp-attribute <query>', 'Attribute to use as timestamp').hideHelp(!revealHidden))
    .option('--template <attribute>', 'Use a server-side named query template (e.g. select)')
    .option('--factor <attribute>', 'Group by attribute')
    .option('--fingerprint <value>', 'Filter by fingerprint: exact match (64 chars) or prefix regex')
    .option('-o, --output <file>', 'Output SVG file')
    .option('--unique', 'Sample only unique crashes', false)
    .option('--reverse', 'Sample from leaf functions', false)
    .action((project, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'flamegraph',
        globalOptions: g,
        queryOptions: {
          filter: opts['filter'],
          limit: opts['limit'],
          offset: opts['offset'],
          select: opts['select'],
          selectWildcard: opts['selectWildcard'],
          age: opts['age'],
          time: opts['time'],
          histogram: opts['histogram'],
          distribution: opts['distribution'],
          mean: opts['mean'],
          sum: opts['sum'],
          range: opts['range'],
          count: opts['count'],
          bin: opts['bin'],
          head: opts['head'],
          tail: opts['tail'],
          object: opts['object'],
          last: opts['last'],
          first: opts['first'],
          min: opts['min'],
          max: opts['max'],
          sort: opts['sort'],
          quantizeUint: opts['quantizeUint'],
          rawQuery: opts['rawQuery'],
          table: opts['table'],
          timestampAttribute: opts['timestampAttribute'],
          template: opts['template'],
          factor: opts['factor'],
          fingerprint: opts['fingerprint'],
        },
        project,
        output: opts['output'],
        unique: opts['unique'],
        reverse: opts['reverse'],
      } satisfies FlamegraphCommand;
    });

  // --- symbold ---
  const cmd_symbold = program.command('symbold');
  cmd_symbold.description('Manage Backtrace symbold service');
  cmd_symbold.command('status')
    .description('Get symbold service status')
    .argument('<project>', 'Project name')
    .action((project, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'symbold.status',
        globalOptions: g,
        project,
      } satisfies SymboldStatusCommand;
    });
  const cmd_symbold_symbolserver = cmd_symbold.command('symbolserver');
  cmd_symbold_symbolserver.description('Manage symbol servers');
  cmd_symbold_symbolserver.command('list')
    .description('List symbol servers')
    .argument('<project>', 'Project name')
    .option('--page <n>', 'Page number')
    .option('--take <n>', 'Items per page')
    .action((project, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'symbold.symbolserver.list',
        globalOptions: g,
        project,
        page: opts['page'],
        take: opts['take'],
      } satisfies SymboldSymbolserverListCommand;
    });
  cmd_symbold_symbolserver.command('details')
    .description('Get symbol server details')
    .argument('<id>', 'Symbol server ID')
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.symbolserver.details',
        globalOptions: g,
        id,
      } satisfies SymboldSymbolserverDetailsCommand;
    });
  const cmd_symbold_symbolserver_logs = cmd_symbold_symbolserver.command('logs');
  cmd_symbold_symbolserver_logs.description('Get symbol server logs');
  cmd_symbold_symbolserver_logs
    .description('Get symbol server logs')
    .argument('<id>', 'Symbol server ID')
    .option('--take <n>', 'Number of logs')
    .option('--page <n>', 'Page number')
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.symbolserver.logs',
        globalOptions: g,
        id,
        take: opts['take'],
        page: opts['page'],
      } satisfies SymboldSymbolserverLogsCommand;
    });
  cmd_symbold_symbolserver.command('add')
    .description('Add symbol server')
    .argument('<project>', 'Project name')
    .argument('<url>', 'Symbol server URL')
    .option('--name <name>', 'Server name')
    .option('--concurrentdownload <n>', 'Max concurrent downloads')
    .option('--retrylimit <n>', 'Retry limit')
    .option('--timeout <ms>', 'Download timeout')
    .addOption(new Option('--whitelist <bool>', 'Use whitelist').choices(["true","false"]))
    .addOption(new Option('--retain <bool>', 'Retain original symbols').choices(["true","false"]))
    .option('--servercredentials.username <username>', 'Symbol server auth user name')
    .option('--servercredentials.password <password>', 'Symbol server auth password')
    .option('--aws.accesskey <access_key>', 'AWS S3 access key')
    .option('--aws.secret <secret_key>', 'AWS S3 secret')
    .option('--aws.bucketname <bucket_name>', 'AWS S3 bucket name')
    .addOption(new Option('--aws.lowerfile <bool>', 'Determine if symbold should use lower case symbol name').choices(["true","false"]))
    .addOption(new Option('--aws.lowerid <bool>', 'Determine if symbold should use lower case debug id').choices(["true","false"]))
    .addOption(new Option('--aws.usepdb <bool>', 'Determine a way to generate url to S3 symbols').choices(["true","false"]))
    .option('--proxy.host <proxy_host>', 'Proxy host')
    .option('--proxy.port <proxy_port>', 'Proxy port')
    .option('--proxy.username <proxy_username>', 'Proxy username')
    .option('--proxy.password <proxy_password>', 'Proxy password')
    .addOption(new Option('--force <bool>', 'Force creation').choices(["true","false"]))
    .option('--retrytimeout <ms>', 'Download retry timeout')
    .action((project, url, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'symbold.symbolserver.add',
        globalOptions: g,
        project,
        url,
        name: opts['name'],
        concurrentdownload: opts['concurrentdownload'],
        retrylimit: opts['retrylimit'],
        timeout: g.timeout,
        whitelist: opts['whitelist'],
        retain: opts['retain'],
        servercredentialsUsername: opts['servercredentialsUsername'],
        servercredentialsPassword: opts['servercredentialsPassword'],
        awsAccesskey: opts['awsAccesskey'],
        awsSecret: opts['awsSecret'],
        awsBucketname: opts['awsBucketname'],
        awsLowerfile: opts['awsLowerfile'],
        awsLowerid: opts['awsLowerid'],
        awsUsepdb: opts['awsUsepdb'],
        proxyHost: opts['proxyHost'],
        proxyPort: opts['proxyPort'],
        proxyUsername: opts['proxyUsername'],
        proxyPassword: opts['proxyPassword'],
        force: opts['force'],
        retrytimeout: opts['retrytimeout'],
      } satisfies SymboldSymbolserverAddCommand;
    });
  cmd_symbold_symbolserver.command('update')
    .description('Update symbol server')
    .argument('<id>', 'Symbol server ID')
    .option('--name <name>', 'Server name')
    .option('--symbolServerUrl <url>', 'Symbol server URL')
    .option('--concurrentdownload <n>', 'Max concurrent downloads')
    .option('--retrylimit <n>', 'Retry limit')
    .option('--timeout <ms>', 'Download timeout')
    .option('--retrytimeout <ms>', 'Download retry timeout')
    .addOption(new Option('--whitelist <bool>', 'Use whitelist').choices(["true","false"]))
    .addOption(new Option('--retain <bool>', 'Retain original symbols').choices(["true","false"]))
    .option('--servercredentials.username <username>', 'Symbol server auth user name')
    .option('--servercredentials.password <password>', 'Symbol server auth password')
    .option('--aws.accesskey <access_key>', 'AWS S3 access key')
    .option('--aws.secret <secret_key>', 'AWS S3 secret')
    .option('--aws.bucketname <bucket_name>', 'AWS S3 bucket name')
    .addOption(new Option('--aws.lowerfile <bool>', 'Determine if symbold should use lower case symbol name').choices(["true","false"]))
    .addOption(new Option('--aws.lowerid <bool>', 'Determine if symbold should use lower case debug id').choices(["true","false"]))
    .addOption(new Option('--aws.usepdb <bool>', 'Determine a way to generate url to S3 symbols').choices(["true","false"]))
    .option('--proxy.host <proxy_host>', 'Proxy host')
    .option('--proxy.port <proxy_port>', 'Proxy port')
    .option('--proxy.username <proxy_username>', 'Proxy username')
    .option('--proxy.password <proxy_password>', 'Proxy password')
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      result = {
        kind: 'symbold.symbolserver.update',
        globalOptions: g,
        id,
        name: opts['name'],
        symbolServerUrl: opts['symbolServerUrl'],
        concurrentdownload: opts['concurrentdownload'],
        retrylimit: opts['retrylimit'],
        timeout: g.timeout,
        retrytimeout: opts['retrytimeout'],
        whitelist: opts['whitelist'],
        retain: opts['retain'],
        servercredentialsUsername: opts['servercredentialsUsername'],
        servercredentialsPassword: opts['servercredentialsPassword'],
        awsAccesskey: opts['awsAccesskey'],
        awsSecret: opts['awsSecret'],
        awsBucketname: opts['awsBucketname'],
        awsLowerfile: opts['awsLowerfile'],
        awsLowerid: opts['awsLowerid'],
        awsUsepdb: opts['awsUsepdb'],
        proxyHost: opts['proxyHost'],
        proxyPort: opts['proxyPort'],
        proxyUsername: opts['proxyUsername'],
        proxyPassword: opts['proxyPassword'],
      } satisfies SymboldSymbolserverUpdateCommand;
    });
  cmd_symbold_symbolserver.command('delete')
    .description('Delete symbol server')
    .argument('<id>', 'Symbol server ID')
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.symbolserver.delete',
        globalOptions: g,
        id,
      } satisfies SymboldSymbolserverDeleteCommand;
    });
  cmd_symbold_symbolserver.command('disable')
    .description('Disable symbol server')
    .argument('<id>', 'Symbol server ID')
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.symbolserver.disable',
        globalOptions: g,
        id,
      } satisfies SymboldSymbolserverDisableCommand;
    });
  cmd_symbold_symbolserver.command('enable')
    .description('Enable symbol server')
    .argument('<id>', 'Symbol server ID')
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.symbolserver.enable',
        globalOptions: g,
        id,
      } satisfies SymboldSymbolserverEnableCommand;
    });
  const cmd_symbold_whitelist = cmd_symbold.command('whitelist');
  cmd_symbold_whitelist.description('Manage whitelist');
  cmd_symbold_whitelist.command('add')
    .description('Add to whitelist')
    .argument('<server_id>', 'Symbol server ID')
    .option('--name <name>', 'Item name')
    .action((server_id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.whitelist.add',
        globalOptions: g,
        server_id,
        name: opts['name'],
      } satisfies SymboldWhitelistAddCommand;
    });
  cmd_symbold_whitelist.command('remove')
    .description('Remove from whitelist')
    .argument('<id>', 'Item ID')
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.whitelist.remove',
        globalOptions: g,
        id,
      } satisfies SymboldWhitelistRemoveCommand;
    });
  cmd_symbold_whitelist.command('list')
    .description('List whitelist items')
    .argument('<server_id>', 'Symbol server ID')
    .option('--page <n>', 'Page number')
    .option('--take <n>', 'Items per page')
    .action((server_id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.whitelist.list',
        globalOptions: g,
        server_id,
        page: opts['page'],
        take: opts['take'],
      } satisfies SymboldWhitelistListCommand;
    });
  const cmd_symbold_blacklist = cmd_symbold.command('blacklist');
  cmd_symbold_blacklist.description('Manage blacklist');
  cmd_symbold_blacklist.command('add')
    .description('Add to blacklist')
    .argument('<server_id>', 'Symbol server ID')
    .option('--name <name>', 'Item name')
    .action((server_id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.blacklist.add',
        globalOptions: g,
        server_id,
        name: opts['name'],
      } satisfies SymboldBlacklistAddCommand;
    });
  cmd_symbold_blacklist.command('remove')
    .description('Remove from blacklist')
    .argument('<id>', 'Item ID')
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.blacklist.remove',
        globalOptions: g,
        id,
      } satisfies SymboldBlacklistRemoveCommand;
    });
  cmd_symbold_blacklist.command('list')
    .description('List blacklist items')
    .argument('<server_id>', 'Symbol server ID')
    .option('--page <n>', 'Page number')
    .option('--take <n>', 'Items per page')
    .action((server_id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.blacklist.list',
        globalOptions: g,
        server_id,
        page: opts['page'],
        take: opts['take'],
      } satisfies SymboldBlacklistListCommand;
    });
  const cmd_symbold_skiplist = cmd_symbold.command('skiplist');
  cmd_symbold_skiplist.description('Manage skiplist');
  cmd_symbold_skiplist.command('find')
    .description('Find items in skiplist')
    .argument('<server_id>', 'Symbol server ID')
    .argument('<filter>', 'Filter string')
    .option('--page <n>', 'Page number')
    .option('--take <n>', 'Items per page')
    .action((server_id, filter, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.skiplist.find',
        globalOptions: g,
        server_id,
        filter,
        page: opts['page'],
        take: opts['take'],
      } satisfies SymboldSkiplistFindCommand;
    });
  const cmd_symbold_skiplist_remove = cmd_symbold_skiplist.command('remove');
  cmd_symbold_skiplist_remove.description('Remove from skiplist');
  cmd_symbold_skiplist_remove
    .description('Remove from skiplist')
    .argument('[id]', 'Item ID')
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.skiplist.remove',
        globalOptions: g,
        id,
      } satisfies SymboldSkiplistRemoveCommand;
    });
  const cmd_symbold_queue = cmd_symbold.command('queue');
  cmd_symbold_queue.description('Manage symbold queue');
  cmd_symbold_queue.command('list')
    .description('List queue events')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.queue.list',
        globalOptions: g,
      } satisfies SymboldQueueListCommand;
    });
  cmd_symbold_queue.command('add')
    .description('Add to queue')
    .argument('<project>', 'Universe/project')
    .argument('<symbol>', 'Missing symbol')
    .argument('<oid>', 'Object ID')
    .action((project, symbol, oid, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'symbold.queue.add',
        globalOptions: g,
        project,
        symbol,
        oid,
      } satisfies SymboldQueueAddCommand;
    });
  cmd_symbold_queue.command('size')
    .description('Get queue size')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.queue.size',
        globalOptions: g,
      } satisfies SymboldQueueSizeCommand;
    });
  cmd_symbold_queue.command('symbols')
    .description('List missing symbols from events')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.queue.symbols',
        globalOptions: g,
      } satisfies SymboldQueueSymbolsCommand;
    });
  cmd_symbold_symbolserver_logs.command('filter')
    .argument('<id>', 'Symbol server ID')
    .argument('<filter>', 'Filter string')
    .option('--take <n>', 'Number of logs')
    .option('--page <n>', 'Page number')
    .action((id, filter, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.symbolserver.logs.filter',
        globalOptions: g,
        id,
        filter,
        take: opts['take'],
        page: opts['page'],
      } satisfies SymboldSymbolserverLogsFilterCommand;
    });
  cmd_symbold_skiplist_remove.command('all')
    .description('Remove all items')
    .argument('[id]', 'Item ID')
    .argument('<server_id>', 'Symbol server ID')
    .action((id, server_id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.skiplist.remove.all',
        globalOptions: g,
        id,
        server_id,
      } satisfies SymboldSkiplistRemoveAllCommand;
    });
  cmd_symbold_skiplist_remove.command('filter')
    .description('Remove by filter')
    .argument('[id]', 'Item ID')
    .argument('<server_id>', 'Symbol server ID')
    .argument('<filter>', 'Filter criteria')
    .action((id, server_id, filter, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'symbold.skiplist.remove.filter',
        globalOptions: g,
        id,
        server_id,
        filter,
      } satisfies SymboldSkiplistRemoveFilterCommand;
    });

  // --- report ---
  const cmd_report = program.command('report');
  cmd_report.description('Manage scheduled reports');
  cmd_report.command('list')
    .description('List reports')
    .argument('<project>', 'Project name')
    .action((project, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'report.list',
        globalOptions: g,
        project,
      } satisfies ReportListCommand;
    });
  cmd_report.command('create')
    .description('Create report')
    .argument('<project>', 'Project name')
    .option('--filter <filter>', 'Filter expression: attribute,operation,value')
    .option('--limit <n>', 'Number of rows to return')
    .option('--offset <n>', 'Skip first N rows')
    .option('--select <attribute>', 'Select specific attributes', collectRepeatable, [])
    .addOption(new Option('--select-wildcard <type>', 'Select all attributes of the given type').choices(["physical","derived","virtual"]))
    .option('--age <time>', 'Relative timestamp (e.g., 1h, 42d, 10s, etc). Valid suffixes: y (years), M (months), w (weeks), d (days), h (hours), m (minutes), s (seconds)')
    .option('--time <range>', 'Time range using natural language')
    .option('--unique <attribute>', 'Count distinct values', collectRepeatable, [])
    .option('--histogram <attribute>', 'Show all distinct values', collectRepeatable, [])
    .option('--distribution <attribute>', 'Show truncated histogram', collectRepeatable, [])
    .option('--mean <attribute>', 'Calculate mean', collectRepeatable, [])
    .option('--sum <attribute>', 'Sum all values', collectRepeatable, [])
    .option('--range <attribute>', 'Show min and max values', collectRepeatable, [])
    .option('--count <attribute>', 'Count non-null values', collectRepeatable, [])
    .option('--bin <attribute>', 'Linear histogram of values', collectRepeatable, [])
    .option('--head <attribute>', 'First value in factor', collectRepeatable, [])
    .option('--tail <attribute>', 'Last value in factor', collectRepeatable, [])
    .option('--object <attribute>', 'Maximum object identifier', collectRepeatable, [])
    .addOption(new Option('--last <attribute>', 'Last object identifier').argParser(collectRepeatable).default([]).hideHelp(!revealHidden))
    .addOption(new Option('--first <attribute>', 'First object identifier').argParser(collectRepeatable).default([]).hideHelp(!revealHidden))
    .addOption(new Option('--min <attribute>', 'Min object identifier').argParser(collectRepeatable).default([]).hideHelp(!revealHidden))
    .addOption(new Option('--max <attribute>', 'Max object identifier').argParser(collectRepeatable).default([]).hideHelp(!revealHidden))
    .option('--sort <term>', 'Sort results: [-](column|fold_term)', collectRepeatable, [])
    .option('--quantize-uint <spec>', 'Compute quantized column: output_column,input_column,size[,offset]', collectRepeatable, [])
    .addOption(new Option('--raw-query <query>', 'Raw query to use (overrides all other flags)').hideHelp(!revealHidden))
    .addOption(new Option('--table <query>', 'Table to query against').hideHelp(!revealHidden))
    .addOption(new Option('--timestamp-attribute <query>', 'Attribute to use as timestamp').hideHelp(!revealHidden))
    .option('--reverse <attribute>', 'Reverse sort order')
    .option('--template <attribute>', 'Use a server-side named query template (e.g. select)')
    .option('--factor <attribute>', 'Group by attribute')
    .option('--fingerprint <value>', 'Filter by fingerprint: exact match (64 chars) or prefix regex')
    .option('--rcpt <email>', 'Recipient email', collectRepeatable, [])
    .option('--title <title>', 'Report title')
    .addOption(new Option('--period <period>', 'Period: week|day (default: week)').choices(["week","day"]))
    .addOption(new Option('--timezone <timezone>', 'Timezone for report').hideHelp(!revealHidden))
    .addOption(new Option('--day <n>', 'Day of week (1=Monday)').hideHelp(!revealHidden))
    .addOption(new Option('--hour <n>', 'Hour of day to send (default: 9)').hideHelp(!revealHidden))
    .addOption(new Option('--include-users', 'Whether to include users in the report').default(false).hideHelp(!revealHidden))
    .action((project, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'report.create',
        globalOptions: g,
        queryOptions: {
          filter: opts['filter'],
          limit: opts['limit'],
          offset: opts['offset'],
          select: opts['select'],
          selectWildcard: opts['selectWildcard'],
          age: opts['age'],
          time: opts['time'],
          unique: opts['unique'],
          histogram: opts['histogram'],
          distribution: opts['distribution'],
          mean: opts['mean'],
          sum: opts['sum'],
          range: opts['range'],
          count: opts['count'],
          bin: opts['bin'],
          head: opts['head'],
          tail: opts['tail'],
          object: opts['object'],
          last: opts['last'],
          first: opts['first'],
          min: opts['min'],
          max: opts['max'],
          sort: opts['sort'],
          quantizeUint: opts['quantizeUint'],
          rawQuery: opts['rawQuery'],
          table: opts['table'],
          timestampAttribute: opts['timestampAttribute'],
          reverse: opts['reverse'],
          template: opts['template'],
          factor: opts['factor'],
          fingerprint: opts['fingerprint'],
        },
        project,
        rcpt: opts['rcpt'],
        title: opts['title'],
        period: opts['period'],
        timezone: opts['timezone'],
        day: opts['day'],
        hour: opts['hour'],
        includeUsers: opts['includeUsers'],
      } satisfies ReportCreateCommand;
    });
  cmd_report.command('delete')
    .description('Delete report')
    .argument('<project>', 'Project name')
    .argument('<id>', 'Report ID')
    .action((project, id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'report.delete',
        globalOptions: g,
        project,
        id,
      } satisfies ReportDeleteCommand;
    });
  cmd_report.command('send')
    .description('Send report immediately')
    .argument('<project>', 'Project name')
    .argument('<id>', 'Report ID')
    .argument('<email>', 'Recipient email address')
    .action((project, id, email, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'report.send',
        globalOptions: g,
        project,
        id,
        email,
      } satisfies ReportSendCommand;
    });

  // --- merge ---
  program.command('merge')
    .description('Merge fingerprints into group')
    .argument('<project>', 'Project name')
    .argument('<fingerprints...>', 'Fingerprints to merge')
    .action((project, fingerprints, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'merge',
        globalOptions: g,
        project,
        fingerprints,
      } satisfies MergeCommand;
    });

  // --- unmerge ---
  program.command('unmerge')
    .description('Unmerge fingerprints from group')
    .argument('<project>', 'Project name')
    .argument('<fingerprints...>', 'Fingerprints to unmerge')
    .action((project, fingerprints, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'unmerge',
        globalOptions: g,
        project,
        fingerprints,
      } satisfies UnmergeCommand;
    });

  // --- repair ---
  program.command('repair')
    .description('Repair project\'s attribute database')
    .argument('<project>', 'Project name or universe/project')
    .action((project, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'repair',
        globalOptions: g,
        project,
      } satisfies RepairCommand;
    });

  // --- reprocess ---
  program.command('reprocess')
    .description('Reprocess objects')
    .argument('<project>', 'Project name or universe/project')
    .argument('[target...]', 'Query or object IDs')
    .option('--first <n>', 'First object ID')
    .option('--last <n>', 'Last object ID')
    .action((project, target, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'reprocess',
        globalOptions: g,
        project,
        target,
        first: opts['first'],
        last: opts['last'],
      } satisfies ReprocessCommand;
    });

  // --- retention ---
  const cmd_retention = program.command('retention');
  cmd_retention.description('Configure retention policy');
  cmd_retention.command('list')
    .description('List retention policies')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'retention.list',
        globalOptions: g,
      } satisfies RetentionListCommand;
    });
  cmd_retention.command('set')
    .description('Set retention policy')
    .argument('<name>', 'Namespace name')
    .option('--dryrun', 'Show command without executing', false)
    .addOption(new Option('--type <type>', 'Which scope to set the retention policy on').choices(["instance","universe","project"]))
    .option('--rules <n>', 'Number of rules')
    .option('--age <spec>', 'Object age: [R,]O,T[,TE]')
    .option('--max-age <n>', 'Max age in seconds')
    .option('--compress <rule>', 'Compress matching objects')
    .option('--delete <subsets>', 'Delete subsets: physical|crdb')
    .option('--physical-only <rule>', 'Delete only physical data')
    .action((name, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'retention.set',
        globalOptions: g,
        name,
        dryrun: opts['dryrun'],
        type: opts['type'],
        rules: opts['rules'],
        age: opts['age'],
        maxAge: opts['maxAge'],
        compress: opts['compress'],
        delete: opts['delete'],
        physicalOnly: opts['physicalOnly'],
      } satisfies RetentionSetCommand;
    });
  cmd_retention.command('status')
    .description('Get retention status')
    .argument('[name]', 'Namespace name')
    .addOption(new Option('--type <type>', 'Type: universe|project').choices(["universe","project"]))
    .option('--recursive', 'Show recursive status for child namespaces', false)
    .option('--raw', 'Output raw JSON', false)
    .option('--verbose', 'Verbosity level (0-3, repeatable for more detail)', false)
    .option('--instances', 'Show namespace instances per rule', false)
    .option('--includeall', 'Include all namespaces (e.g. symbols)', false)
    .action((name, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'retention.status',
        globalOptions: g,
        name,
        type: opts['type'],
        recursive: opts['recursive'],
        raw: opts['raw'],
        verbose: opts['verbose'],
        instances: opts['instances'],
        includeall: opts['includeall'],
      } satisfies RetentionStatusCommand;
    });
  cmd_retention.command('clear')
    .description('Clear retention policy')
    .argument('<name>', 'Namespace name')
    .addOption(new Option('--type <type>', 'Type: project').choices(["instance","universe","project"]))
    .action((name, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'retention.clear',
        globalOptions: g,
        name,
        type: opts['type'],
      } satisfies RetentionClearCommand;
    });

  // --- sampling ---
  const cmd_sampling = program.command('sampling');
  cmd_sampling.description('Manage object sampling');
  cmd_sampling.command('status')
    .description('Get sampling status')
    .option('--fingerprint <fp>', 'Specific fingerprint')
    .option('--project <project>', 'Specific project')
    .option('--max-groups <n>', 'Max groups to display')
    .option('-a, --all', 'Display all groups (no truncation)', false)
    .option('--verbose', 'Show additional accept/reject statistics', false)
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      result = {
        kind: 'sampling.status',
        globalOptions: g,
        fingerprint: opts['fingerprint'],
        project: g.project,
        maxGroups: opts['maxGroups'],
        all: opts['all'],
        verbose: opts['verbose'],
      } satisfies SamplingStatusCommand;
    });
  cmd_sampling.command('reset')
    .description('Reset sampling')
    .option('--fingerprint <fp>', 'Specific fingerprint')
    .option('--project <project>', 'Specific project')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      result = {
        kind: 'sampling.reset',
        globalOptions: g,
        fingerprint: opts['fingerprint'],
        project: g.project,
      } satisfies SamplingResetCommand;
    });
  cmd_sampling.command('configure')
    .description('Configure sampling')
    .option('--project <project>', 'Project name')
    .option('--universe <universe>', 'Universe name')
    .option('--disable', 'Disable sampling', false)
    .option('--clear', 'Clear project config', false)
    .option('--attribute <attr>', 'Sample by attribute')
    .option('--backoff <spec>', 'Backoff: count,interval')
    .option('--buckets <n>', 'Max sampling buckets')
    .option('--process-whitelisted <bool>', 'Sample whitelisted symbols')
    .option('--process-private <bool>', 'Sample private symbols')
    .option('--reset-interval <interval>', 'Reset interval')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      result = {
        kind: 'sampling.configure',
        globalOptions: g,
        project: g.project,
        universe: g.universe,
        disable: opts['disable'],
        clear: opts['clear'],
        attribute: opts['attribute'],
        backoff: opts['backoff'],
        buckets: opts['buckets'],
        processWhitelisted: opts['processWhitelisted'],
        processPrivate: opts['processPrivate'],
        resetInterval: opts['resetInterval'],
      } satisfies SamplingConfigureCommand;
    });

  // --- symbol ---
  program.command('symbol')
    .description('Manage debug symbols')
    .argument('<project>', 'Project name or universe/project')
    .argument('[action]', 'Action to perform')
    .option('--tag <tag>', 'Filter by tag')
    .option('--filter <filter>', 'Filter symbols')
    .option('--json', 'JSON output', false)
    .option('-o, --output <file>', 'Output to file')
    .action((project, action, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'symbol',
        globalOptions: g,
        project,
        action,
        tag: opts['tag'],
        filter: opts['filter'],
        json: opts['json'],
        output: opts['output'],
      } satisfies SymbolCommand;
    });

  // --- scrubber ---
  const cmd_scrubber = program.command('scrubber');
  cmd_scrubber.description('Manage data scrubbers');
  cmd_scrubber.command('list')
    .description('List scrubbers')
    .argument('<project>', 'Project name')
    .action((project, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'scrubber.list',
        globalOptions: g,
        project,
      } satisfies ScrubberListCommand;
    });
  cmd_scrubber.command('create')
    .description('Create scrubber')
    .argument('<project>', 'Project name')
    .option('--name <name>', 'Scrubber name')
    .option('--regexp <pattern>', 'Pattern to match')
    .addOption(new Option('--builtin <type>', 'Which builtin to use').choices(["ssn","ccn","key","env","all"]))
    .option('--format <format>', 'Scrubber format (default: all)')
    .option('--target <target>', 'Scrubber target (default: all)')
    .option('--enable <value>', 'Enable scrubber')
    .action((project, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'scrubber.create',
        globalOptions: g,
        project,
        name: opts['name'],
        regexp: opts['regexp'],
        builtin: opts['builtin'],
        format: opts['format'],
        target: opts['target'],
        enable: opts['enable'],
      } satisfies ScrubberCreateCommand;
    });
  cmd_scrubber.command('modify')
    .description('Modify scrubber')
    .argument('<project>', 'Project name')
    .argument('<id>', 'Scrubber ID')
    .option('--name <name>', 'Scrubber name')
    .option('--regexp <pattern>', 'New pattern')
    .option('--builtin <type>', 'Builtin type (not modifiable)')
    .option('--format <format>', 'Scrubber format')
    .option('--target <target>', 'Scrubber target')
    .option('--enable <value>', 'Enable/disable')
    .action((project, id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'scrubber.modify',
        globalOptions: g,
        project,
        id,
        name: opts['name'],
        regexp: opts['regexp'],
        builtin: opts['builtin'],
        format: opts['format'],
        target: opts['target'],
        enable: opts['enable'],
      } satisfies ScrubberModifyCommand;
    });
  cmd_scrubber.command('delete')
    .description('Delete scrubber')
    .argument('<project>', 'Project name')
    .argument('<id>', 'Scrubber ID')
    .action((project, id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'scrubber.delete',
        globalOptions: g,
        project,
        id,
      } satisfies ScrubberDeleteCommand;
    });

  // --- setup ---
  program.command('setup')
    .description('Configure initial organization and user for on-premise server')
    .argument('<url>', 'Server URL')
    .addOption(new Option('--setup-json <file>', 'Path to JSON file with setup configuration').hideHelp(!revealHidden))
    .action((url, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'setup',
        globalOptions: g,
        url,
        setupJson: opts['setupJson'],
      } satisfies SetupCommand;
    });

  // --- nuke ---
  program.command('nuke')
    .description('Delete object and dependencies')
    .option('--universe <universe>', 'Universe name')
    .option('--project <project>', 'Project name')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      result = {
        kind: 'nuke',
        globalOptions: g,
        universe: g.universe,
        project: g.project,
      } satisfies NukeCommand;
    });

  // --- token ---
  const cmd_token = program.command('token');
  cmd_token.description('Manage API tokens');
  cmd_token.command('create')
    .description('Create token')
    .option('--project <project>', 'Project name')
    .addOption(new Option('--capability <cap>', 'Capabilities to assign to this token').choices(["symbol:post","error:post","query:post","sync:post"]).default([]))
    .option('--metadata <json>', 'Token metadata JSON')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      result = {
        kind: 'token.create',
        globalOptions: g,
        project: g.project,
        capability: opts['capability'],
        metadata: opts['metadata'],
      } satisfies TokenCreateCommand;
    });
  cmd_token.command('list')
    .description('List tokens')
    .option('--universe <universe>', 'Universe name')
    .option('--project <project>', 'Project name')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      result = {
        kind: 'token.list',
        globalOptions: g,
        universe: g.universe,
        project: g.project,
      } satisfies TokenListCommand;
    });
  cmd_token.command('delete')
    .description('Delete token')
    .argument('<token>', 'Token SHA256 or prefix')
    .action((token, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (token !== undefined) { g.token = token; }
      result = {
        kind: 'token.delete',
        globalOptions: g,
        token,
      } satisfies TokenDeleteCommand;
    });

  // --- user ---
  const cmd_user = program.command('user');
  cmd_user.description('Manage users');
  cmd_user.command('reset')
    .description('Reset user password')
    .option('--universe <universe>', 'Universe name')
    .option('--user <user>', 'Username')
    .option('--password <password>', 'New password')
    .option('--role <role>', 'User role')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      result = {
        kind: 'user.reset',
        globalOptions: g,
        universe: g.universe,
        user: opts['user'],
        password: opts['password'],
        role: opts['role'],
      } satisfies UserResetCommand;
    });

  // --- users ---
  const cmd_users = program.command('users');
  cmd_users.description('Manage users');
  cmd_users.command('add-signup-whitelist')
    .description('Add domain to signup whitelist')
    .option('--universe <universe>', 'Universe name')
    .option('--domain <domain>', 'Domain name')
    .option('--role <role>', 'Default role')
    .option('--method <method>', 'Auth method')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      result = {
        kind: 'users.add-signup-whitelist',
        globalOptions: g,
        universe: g.universe,
        domain: opts['domain'],
        role: opts['role'],
        method: opts['method'],
      } satisfies UsersAddSignupWhitelistCommand;
    });
  cmd_users.command('list-teamless-users')
    .description('List users not in any team')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'users.list-teamless-users',
        globalOptions: g,
      } satisfies UsersListTeamlessUsersCommand;
    });

  // --- tenant ---
  const cmd_tenant = program.command('tenant');
  cmd_tenant.description('Manage isolated tenants (enterprise)');
  cmd_tenant.command('create')
    .description('Create tenant')
    .argument('<name>', 'Tenant name')
    .action((name, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'tenant.create',
        globalOptions: g,
        name,
      } satisfies TenantCreateCommand;
    });
  cmd_tenant.command('delete')
    .description('Delete tenant')
    .argument('<name>', 'Tenant name')
    .action((name, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'tenant.delete',
        globalOptions: g,
        name,
      } satisfies TenantDeleteCommand;
    });
  cmd_tenant.command('list')
    .description('List all tenants')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'tenant.list',
        globalOptions: g,
      } satisfies TenantListCommand;
    });

  // --- similarity ---
  program.command('similarity')
    .description('Compute crash group similarity')
    .argument('<project>', 'Project name or universe/project')
    .argument('[filter]', 'Filter expression')
    .option('--threshold <n>', 'Min callstack length')
    .option('--truncate <n>', 'Shorten callstack before comparing')
    .option('--intersection <n>', 'Min common symbols')
    .option('--distance <n>', 'Max edit distance')
    .option('--fingerprint <fp>', 'Compare to specific fingerprint')
    .option('--json', 'Return JSON result', false)
    .action((project, filter, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'similarity',
        globalOptions: g,
        project,
        filter,
        threshold: opts['threshold'],
        truncate: opts['truncate'],
        intersection: opts['intersection'],
        distance: opts['distance'],
        fingerprint: opts['fingerprint'],
        json: opts['json'],
      } satisfies SimilarityCommand;
    });

  // --- invite ---
  const cmd_invite = program.command('invite');
  cmd_invite.description('Invite users to system');
  cmd_invite.command('create')
    .description('Create invitation')
    .argument('<username>', 'Username')
    .argument('<email>', 'Email address')
    .addOption(new Option('--role <role>', 'Role to assign the user').choices(["guest","member","admin"]))
    .option('--metadata <data>', 'Additional metadata')
    .option('--tenant <tenant>', 'Tenant name')
    .addOption(new Option('--method <method>', 'Authentication method to use').choices(["password","saml","pam"]))
    .action((username, email, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'invite.create',
        globalOptions: g,
        username,
        email,
        role: opts['role'],
        metadata: opts['metadata'],
        tenant: opts['tenant'],
        method: opts['method'],
      } satisfies InviteCreateCommand;
    });
  cmd_invite.command('list')
    .description('List pending invitations')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'invite.list',
        globalOptions: g,
      } satisfies InviteListCommand;
    });
  cmd_invite.command('delete')
    .description('Delete invitation')
    .argument('<email>', 'Email address of invitation to delete')
    .option('--universe <universe>', 'Universe name')
    .action((email, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      result = {
        kind: 'invite.delete',
        globalOptions: g,
        email,
        universe: g.universe,
      } satisfies InviteDeleteCommand;
    });

  // --- callstack ---
  const cmd_callstack = program.command('callstack');
  cmd_callstack.description('Evaluate callstack processing');
  cmd_callstack.command('evaluate')
    .description('Check callstack results')
    .argument('<project>', 'Project name')
    .argument('<target>', 'Object ID or JSON file')
    .option('--name <name>', 'Callstack format name')
    .option('--language <language>', 'Language filter')
    .option('--platform <platform>', 'Platform filter')
    .action((project, target, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'callstack.evaluate',
        globalOptions: g,
        project,
        target,
        name: opts['name'],
        language: opts['language'],
        platform: opts['platform'],
      } satisfies CallstackEvaluateCommand;
    });

  // --- access ---
  const cmd_access = program.command('access');
  cmd_access.description('Control access permissions');
  const cmd_access_team = cmd_access.command('team');
  cmd_access_team.description('Manage teams');
  cmd_access_team.command('create')
    .description('Create team')
    .argument('<team>', 'Team name')
    .action((team, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'access.team.create',
        globalOptions: g,
        team,
      } satisfies AccessTeamCreateCommand;
    });
  cmd_access_team.command('remove')
    .description('Remove team')
    .argument('<team>', 'Team name')
    .action((team, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'access.team.remove',
        globalOptions: g,
        team,
      } satisfies AccessTeamRemoveCommand;
    });
  cmd_access_team.command('details')
    .description('Get team details')
    .argument('<team>', 'Team name')
    .action((team, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'access.team.details',
        globalOptions: g,
        team,
      } satisfies AccessTeamDetailsCommand;
    });
  cmd_access_team.command('add-user')
    .description('Add user to team')
    .argument('<team>', 'Team name')
    .argument('<user>', 'Username')
    .action((team, user, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'access.team.add-user',
        globalOptions: g,
        team,
        user,
      } satisfies AccessTeamAddUserCommand;
    });
  cmd_access_team.command('remove-user')
    .description('Remove user from team')
    .argument('<team>', 'Team name')
    .argument('<user>', 'Username')
    .action((team, user, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'access.team.remove-user',
        globalOptions: g,
        team,
        user,
      } satisfies AccessTeamRemoveUserCommand;
    });
  cmd_access_team.command('list')
    .description('List all teams')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'access.team.list',
        globalOptions: g,
      } satisfies AccessTeamListCommand;
    });
  const cmd_access_project = cmd_access.command('project');
  cmd_access_project.description('Manage project access');
  cmd_access_project.command('add-team')
    .description('Add team to project')
    .argument('<project>', 'Project name')
    .argument('<team>', 'Team name')
    .argument('<role>', 'Role to assign to the user')
    .action((project, team, role, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'access.project.add-team',
        globalOptions: g,
        project,
        team,
        role,
      } satisfies AccessProjectAddTeamCommand;
    });
  cmd_access_project.command('remove-team')
    .description('Remove team from project')
    .argument('<project>', 'Project name')
    .argument('<team>', 'Team name')
    .action((project, team, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'access.project.remove-team',
        globalOptions: g,
        project,
        team,
      } satisfies AccessProjectRemoveTeamCommand;
    });
  cmd_access_project.command('add-user')
    .description('Add user to project')
    .argument('<project>', 'Project name')
    .argument('<user>', 'Username')
    .argument('<role>', 'Role to assign to the user')
    .action((project, user, role, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'access.project.add-user',
        globalOptions: g,
        project,
        user,
        role,
      } satisfies AccessProjectAddUserCommand;
    });
  cmd_access_project.command('remove-user')
    .description('Remove user from project')
    .argument('<project>', 'Project name')
    .argument('<user>', 'Username')
    .action((project, user, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'access.project.remove-user',
        globalOptions: g,
        project,
        user,
      } satisfies AccessProjectRemoveUserCommand;
    });
  cmd_access_project.command('details')
    .description('Get project access details')
    .argument('<project>', 'Project name')
    .action((project, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'access.project.details',
        globalOptions: g,
        project,
      } satisfies AccessProjectDetailsCommand;
    });

  // --- stability ---
  const cmd_stability = program.command('stability');
  cmd_stability.description('Manage stability metrics');
  cmd_stability.command('create-metric')
    .description('Create metric')
    .option('--universe <universe>', 'Universe name')
    .option('--project <project>', 'Project name')
    .option('--metric-group <group>', 'Metric group name')
    .option('--name <name>', 'Metric name')
    .option('--attribute <spec>', 'Attribute: name,value')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      result = {
        kind: 'stability.create-metric',
        globalOptions: g,
        universe: g.universe,
        project: g.project,
        metricGroup: opts['metricGroup'],
        name: opts['name'],
        attribute: opts['attribute'],
      } satisfies StabilityCreateMetricCommand;
    });

  // --- metrics-importer ---
  const cmd_metricsImporter = program.command('metrics-importer');
  cmd_metricsImporter.description('Control metrics importer');
  cmd_metricsImporter.command('logs')
    .description('Display logs')
    .option('--project <project>', 'Project name')
    .option('--source-id <id>', 'Source ID')
    .option('--importer-id <id>', 'Importer ID')
    .option('--limit <n>', 'Log limit')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      result = {
        kind: 'metrics-importer.logs',
        globalOptions: g,
        project: g.project,
        sourceId: opts['sourceId'],
        importerId: opts['importerId'],
        limit: opts['limit'],
      } satisfies MetricsImporterLogsCommand;
    });
  const cmd_metricsImporter_source = cmd_metricsImporter.command('source');
  cmd_metricsImporter_source.description('Manage sources');
  cmd_metricsImporter_source.command('check-query')
    .description('Validate query')
    .option('--source <id>', 'Source UUID')
    .option('--project <project>', 'Project name')
    .option('--query <query>', 'Query to validate')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      result = {
        kind: 'metrics-importer.source.check-query',
        globalOptions: g,
        source: opts['source'],
        project: g.project,
        query: opts['query'],
      } satisfies MetricsImporterSourceCheckQueryCommand;
    });
  const cmd_metricsImporter_importer = cmd_metricsImporter.command('importer');
  cmd_metricsImporter_importer.description('Manage importers');
  cmd_metricsImporter_importer.command('create')
    .description('Create importer')
    .option('--project <project>', 'Project name')
    .option('--source <id>', 'Source UUID')
    .option('--name <name>', 'Importer name')
    .option('--start-at <time>', 'Start time (RFC3339)')
    .option('--metric <name>', 'Metric name')
    .option('--metric-group <group>', 'Metric group')
    .option('--query <query>', 'Data query')
    .option('--delay <n>', 'Delay in seconds')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      result = {
        kind: 'metrics-importer.importer.create',
        globalOptions: g,
        project: g.project,
        source: opts['source'],
        name: opts['name'],
        startAt: opts['startAt'],
        metric: opts['metric'],
        metricGroup: opts['metricGroup'],
        query: opts['query'],
        delay: opts['delay'],
      } satisfies MetricsImporterImporterCreateCommand;
    });

  // --- alerts ---
  const cmd_alerts = program.command('alerts');
  cmd_alerts.description('Manage alerts');
  const cmd_alerts_target = cmd_alerts.command('target');
  cmd_alerts_target.description('Manage alert targets');
  cmd_alerts_target.command('create')
    .description('Create target')
    .option('--project <project>', 'Project name')
    .option('--name <name>', 'Target name')
    .option('--workflow-name <name>', 'Workflow name')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      result = {
        kind: 'alerts.target.create',
        globalOptions: g,
        project: g.project,
        name: opts['name'],
        workflowName: opts['workflowName'],
      } satisfies AlertsTargetCreateCommand;
    });
  cmd_alerts_target.command('list')
    .description('List targets')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'alerts.target.list',
        globalOptions: g,
      } satisfies AlertsTargetListCommand;
    });
  cmd_alerts_target.command('get')
    .description('Get target')
    .option('--id <id>', 'Target ID')
    .option('--name <name>', 'Target name')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'alerts.target.get',
        globalOptions: g,
        id: opts['id'],
        name: opts['name'],
      } satisfies AlertsTargetGetCommand;
    });
  cmd_alerts_target.command('update')
    .description('Update target')
    .option('--id <id>', 'Target ID')
    .option('--name <name>', 'Target name')
    .option('--rename <name>', 'New name')
    .option('--workflow-name <name>', 'New workflow')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'alerts.target.update',
        globalOptions: g,
        id: opts['id'],
        name: opts['name'],
        rename: opts['rename'],
        workflowName: opts['workflowName'],
      } satisfies AlertsTargetUpdateCommand;
    });
  cmd_alerts_target.command('delete')
    .description('Delete target')
    .option('--id <id>', 'Target ID')
    .option('--name <name>', 'Target name')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'alerts.target.delete',
        globalOptions: g,
        id: opts['id'],
        name: opts['name'],
      } satisfies AlertsTargetDeleteCommand;
    });
  const cmd_alerts_alert = cmd_alerts.command('alert');
  cmd_alerts_alert.description('Manage alerts');
  cmd_alerts_alert.command('list')
    .description('List alerts')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'alerts.alert.list',
        globalOptions: g,
      } satisfies AlertsAlertListCommand;
    });
  cmd_alerts_alert.command('get')
    .description('Get alert')
    .option('--id <id>', 'Alert ID')
    .option('--name <name>', 'Alert name')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'alerts.alert.get',
        globalOptions: g,
        id: opts['id'],
        name: opts['name'],
      } satisfies AlertsAlertGetCommand;
    });
  cmd_alerts_alert.command('update')
    .description('Update alert')
    .option('--filter <filter>', 'Filter expression: attribute,operation,value')
    .option('--limit <n>', 'Number of rows to return')
    .option('--offset <n>', 'Skip first N rows')
    .option('--select <attribute>', 'Select specific attributes', collectRepeatable, [])
    .addOption(new Option('--select-wildcard <type>', 'Select all attributes of the given type').choices(["physical","derived","virtual"]))
    .option('--age <time>', 'Relative timestamp (e.g., 1h, 42d, 10s, etc). Valid suffixes: y (years), M (months), w (weeks), d (days), h (hours), m (minutes), s (seconds)')
    .option('--time <range>', 'Time range using natural language')
    .option('--unique <attribute>', 'Count distinct values', collectRepeatable, [])
    .option('--histogram <attribute>', 'Show all distinct values', collectRepeatable, [])
    .option('--distribution <attribute>', 'Show truncated histogram', collectRepeatable, [])
    .option('--mean <attribute>', 'Calculate mean', collectRepeatable, [])
    .option('--sum <attribute>', 'Sum all values', collectRepeatable, [])
    .option('--range <attribute>', 'Show min and max values', collectRepeatable, [])
    .option('--count <attribute>', 'Count non-null values', collectRepeatable, [])
    .option('--bin <attribute>', 'Linear histogram of values', collectRepeatable, [])
    .option('--head <attribute>', 'First value in factor', collectRepeatable, [])
    .option('--tail <attribute>', 'Last value in factor', collectRepeatable, [])
    .option('--object <attribute>', 'Maximum object identifier', collectRepeatable, [])
    .addOption(new Option('--last <attribute>', 'Last object identifier').argParser(collectRepeatable).default([]).hideHelp(!revealHidden))
    .addOption(new Option('--first <attribute>', 'First object identifier').argParser(collectRepeatable).default([]).hideHelp(!revealHidden))
    .addOption(new Option('--min <attribute>', 'Min object identifier').argParser(collectRepeatable).default([]).hideHelp(!revealHidden))
    .addOption(new Option('--max <attribute>', 'Max object identifier').argParser(collectRepeatable).default([]).hideHelp(!revealHidden))
    .option('--sort <term>', 'Sort results: [-](column|fold_term)', collectRepeatable, [])
    .option('--quantize-uint <spec>', 'Compute quantized column: output_column,input_column,size[,offset]', collectRepeatable, [])
    .addOption(new Option('--raw-query <query>', 'Raw query to use (overrides all other flags)').hideHelp(!revealHidden))
    .addOption(new Option('--table <query>', 'Table to query against').hideHelp(!revealHidden))
    .addOption(new Option('--timestamp-attribute <query>', 'Attribute to use as timestamp').hideHelp(!revealHidden))
    .option('--reverse <attribute>', 'Reverse sort order')
    .option('--template <attribute>', 'Use a server-side named query template (e.g. select)')
    .option('--factor <attribute>', 'Group by attribute')
    .option('--fingerprint <value>', 'Filter by fingerprint: exact match (64 chars) or prefix regex')
    .option('--id <id>', 'Alert ID')
    .option('--name <name>', 'Alert name')
    .option('--rename <name>', 'New name')
    .option('--enabled <bool>', 'Enable/disable alert')
    .option('--query-period <time>', 'Query period')
    .option('--min-notification-interval <time>', 'Min notification interval')
    .option('--mute-until <ts>', 'Mute until timestamp')
    .option('--target-name <name>', 'Target name', collectRepeatable, [])
    .option('--target-id <id>', 'Target ID', collectRepeatable, [])
    .option('--trigger <spec>', 'Trigger spec: column,index,op,warn,crit')
    .option('--replace-query', 'Replace query', false)
    .option('--clear-targets', 'Clear targets', false)
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'alerts.alert.update',
        globalOptions: g,
        queryOptions: {
          filter: opts['filter'],
          limit: opts['limit'],
          offset: opts['offset'],
          select: opts['select'],
          selectWildcard: opts['selectWildcard'],
          age: opts['age'],
          time: opts['time'],
          unique: opts['unique'],
          histogram: opts['histogram'],
          distribution: opts['distribution'],
          mean: opts['mean'],
          sum: opts['sum'],
          range: opts['range'],
          count: opts['count'],
          bin: opts['bin'],
          head: opts['head'],
          tail: opts['tail'],
          object: opts['object'],
          last: opts['last'],
          first: opts['first'],
          min: opts['min'],
          max: opts['max'],
          sort: opts['sort'],
          quantizeUint: opts['quantizeUint'],
          rawQuery: opts['rawQuery'],
          table: opts['table'],
          timestampAttribute: opts['timestampAttribute'],
          reverse: opts['reverse'],
          template: opts['template'],
          factor: opts['factor'],
          fingerprint: opts['fingerprint'],
        },
        id: opts['id'],
        name: opts['name'],
        rename: opts['rename'],
        enabled: opts['enabled'],
        queryPeriod: opts['queryPeriod'],
        minNotificationInterval: opts['minNotificationInterval'],
        muteUntil: opts['muteUntil'],
        targetName: opts['targetName'],
        targetId: opts['targetId'],
        trigger: opts['trigger'],
        replaceQuery: opts['replaceQuery'],
        clearTargets: opts['clearTargets'],
      } satisfies AlertsAlertUpdateCommand;
    });
  cmd_alerts_alert.command('delete')
    .description('Delete alert')
    .option('--id <id>', 'Alert ID')
    .option('--name <name>', 'Alert name')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'alerts.alert.delete',
        globalOptions: g,
        id: opts['id'],
        name: opts['name'],
      } satisfies AlertsAlertDeleteCommand;
    });
  cmd_alerts_alert.command('create')
    .description('Create alert')
    .option('--filter <filter>', 'Filter expression: attribute,operation,value')
    .option('--limit <n>', 'Number of rows to return')
    .option('--offset <n>', 'Skip first N rows')
    .option('--select <attribute>', 'Select specific attributes', collectRepeatable, [])
    .addOption(new Option('--select-wildcard <type>', 'Select all attributes of the given type').choices(["physical","derived","virtual"]))
    .option('--age <time>', 'Relative timestamp (e.g., 1h, 42d, 10s, etc). Valid suffixes: y (years), M (months), w (weeks), d (days), h (hours), m (minutes), s (seconds)')
    .option('--time <range>', 'Time range using natural language')
    .option('--unique <attribute>', 'Count distinct values', collectRepeatable, [])
    .option('--histogram <attribute>', 'Show all distinct values', collectRepeatable, [])
    .option('--distribution <attribute>', 'Show truncated histogram', collectRepeatable, [])
    .option('--mean <attribute>', 'Calculate mean', collectRepeatable, [])
    .option('--sum <attribute>', 'Sum all values', collectRepeatable, [])
    .option('--range <attribute>', 'Show min and max values', collectRepeatable, [])
    .option('--count <attribute>', 'Count non-null values', collectRepeatable, [])
    .option('--bin <attribute>', 'Linear histogram of values', collectRepeatable, [])
    .option('--head <attribute>', 'First value in factor', collectRepeatable, [])
    .option('--tail <attribute>', 'Last value in factor', collectRepeatable, [])
    .option('--object <attribute>', 'Maximum object identifier', collectRepeatable, [])
    .addOption(new Option('--last <attribute>', 'Last object identifier').argParser(collectRepeatable).default([]).hideHelp(!revealHidden))
    .addOption(new Option('--first <attribute>', 'First object identifier').argParser(collectRepeatable).default([]).hideHelp(!revealHidden))
    .addOption(new Option('--min <attribute>', 'Min object identifier').argParser(collectRepeatable).default([]).hideHelp(!revealHidden))
    .addOption(new Option('--max <attribute>', 'Max object identifier').argParser(collectRepeatable).default([]).hideHelp(!revealHidden))
    .option('--sort <term>', 'Sort results: [-](column|fold_term)', collectRepeatable, [])
    .option('--quantize-uint <spec>', 'Compute quantized column: output_column,input_column,size[,offset]', collectRepeatable, [])
    .addOption(new Option('--raw-query <query>', 'Raw query to use (overrides all other flags)').hideHelp(!revealHidden))
    .addOption(new Option('--table <query>', 'Table to query against').hideHelp(!revealHidden))
    .addOption(new Option('--timestamp-attribute <query>', 'Attribute to use as timestamp').hideHelp(!revealHidden))
    .option('--reverse <attribute>', 'Reverse sort order')
    .option('--template <attribute>', 'Use a server-side named query template (e.g. select)')
    .option('--factor <attribute>', 'Group by attribute')
    .option('--fingerprint <value>', 'Filter by fingerprint: exact match (64 chars) or prefix regex')
    .option('--universe <universe>', 'Universe name')
    .option('--project <project>', 'Project name')
    .option('--name <name>', 'Alert name')
    .option('--enabled <bool>', 'Enable alert')
    .option('--query-period <time>', 'Query period')
    .option('--min-notification-interval <time>', 'Min notification interval')
    .option('--mute-until <ts>', 'Mute until timestamp')
    .option('--target-name <name>', 'Target name', collectRepeatable, [])
    .option('--target-id <id>', 'Target ID', collectRepeatable, [])
    .option('--trigger <spec>', 'Trigger spec: column,index,op,warn,crit')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      result = {
        kind: 'alerts.alert.create',
        globalOptions: g,
        queryOptions: {
          filter: opts['filter'],
          limit: opts['limit'],
          offset: opts['offset'],
          select: opts['select'],
          selectWildcard: opts['selectWildcard'],
          age: opts['age'],
          time: opts['time'],
          unique: opts['unique'],
          histogram: opts['histogram'],
          distribution: opts['distribution'],
          mean: opts['mean'],
          sum: opts['sum'],
          range: opts['range'],
          count: opts['count'],
          bin: opts['bin'],
          head: opts['head'],
          tail: opts['tail'],
          object: opts['object'],
          last: opts['last'],
          first: opts['first'],
          min: opts['min'],
          max: opts['max'],
          sort: opts['sort'],
          quantizeUint: opts['quantizeUint'],
          rawQuery: opts['rawQuery'],
          table: opts['table'],
          timestampAttribute: opts['timestampAttribute'],
          reverse: opts['reverse'],
          template: opts['template'],
          factor: opts['factor'],
          fingerprint: opts['fingerprint'],
        },
        universe: g.universe,
        project: g.project,
        name: opts['name'],
        enabled: opts['enabled'],
        queryPeriod: opts['queryPeriod'],
        minNotificationInterval: opts['minNotificationInterval'],
        muteUntil: opts['muteUntil'],
        targetName: opts['targetName'],
        targetId: opts['targetId'],
        trigger: opts['trigger'],
      } satisfies AlertsAlertCreateCommand;
    });

  // --- workflows ---
  const cmd_workflows = program.command('workflows');
  cmd_workflows.description('Manage workflow integrations');
  const cmd_workflows_connection = cmd_workflows.command('connection');
  cmd_workflows_connection.description('Manage connections');
  cmd_workflows_connection.command('create')
    .description('Create connection')
    .option('--name <name>', 'Connection name')
    .option('--plugin <id>', 'Plugin ID')
    .option('--options <json>', 'Plugin options')
    .option('--from-file <file>', 'Load from file')
    .option('--raw', 'Output raw JSON', false)
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'workflows.connection.create',
        globalOptions: g,
        name: opts['name'],
        plugin: opts['plugin'],
        options: opts['options'],
        fromFile: opts['fromFile'],
        raw: opts['raw'],
      } satisfies WorkflowsConnectionCreateCommand;
    });
  cmd_workflows_connection.command('list')
    .description('List connections')
    .option('--raw', 'Output raw JSON', false)
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'workflows.connection.list',
        globalOptions: g,
        raw: opts['raw'],
      } satisfies WorkflowsConnectionListCommand;
    });
  cmd_workflows_connection.command('get')
    .description('Get connection')
    .argument('<id>', 'Connection ID')
    .option('--raw', 'Output raw JSON', false)
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'workflows.connection.get',
        globalOptions: g,
        id,
        raw: opts['raw'],
      } satisfies WorkflowsConnectionGetCommand;
    });
  cmd_workflows_connection.command('update')
    .description('Update connection')
    .argument('<id>', 'Connection ID')
    .option('--name <name>', 'New name')
    .option('--options <json>', 'New options')
    .option('--from-file <file>', 'Load from file')
    .option('--raw', 'Output raw JSON', false)
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'workflows.connection.update',
        globalOptions: g,
        id,
        name: opts['name'],
        options: opts['options'],
        fromFile: opts['fromFile'],
        raw: opts['raw'],
      } satisfies WorkflowsConnectionUpdateCommand;
    });
  cmd_workflows_connection.command('delete')
    .description('Delete connection')
    .argument('<id>', 'Connection ID')
    .option('--raw', 'Output raw JSON', false)
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'workflows.connection.delete',
        globalOptions: g,
        id,
        raw: opts['raw'],
      } satisfies WorkflowsConnectionDeleteCommand;
    });
  const cmd_workflows_integration = cmd_workflows.command('integration');
  cmd_workflows_integration.description('Manage integrations');
  cmd_workflows_integration.command('create')
    .description('Create integration')
    .option('--project <project>', 'Project name')
    .option('--name <name>', 'Integration name')
    .option('--plugin <id>', 'Plugin ID')
    .option('--options <json>', 'Plugin options')
    .addOption(new Option('--state <state>', 'Initial state to set for the integration').choices(["enabled","disabled","stopped"]))
    .option('--connection <id>', 'Connection ID')
    .option('--synchronize-issues <bool>', 'Enable issue sync')
    .option('--synchronize-issues-on-add <bool>', 'Sync on add')
    .option('--from-file <file>', 'Load from file')
    .option('--raw', 'Output raw JSON', false)
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      result = {
        kind: 'workflows.integration.create',
        globalOptions: g,
        project: g.project,
        name: opts['name'],
        plugin: opts['plugin'],
        options: opts['options'],
        state: opts['state'],
        connection: opts['connection'],
        synchronizeIssues: opts['synchronizeIssues'],
        synchronizeIssuesOnAdd: opts['synchronizeIssuesOnAdd'],
        fromFile: opts['fromFile'],
        raw: opts['raw'],
      } satisfies WorkflowsIntegrationCreateCommand;
    });
  cmd_workflows_integration.command('list')
    .description('List integrations')
    .option('--raw', 'Output raw JSON', false)
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'workflows.integration.list',
        globalOptions: g,
        raw: opts['raw'],
      } satisfies WorkflowsIntegrationListCommand;
    });
  cmd_workflows_integration.command('get')
    .description('Get integration')
    .argument('<id>', 'Integration ID')
    .option('--raw', 'Output raw JSON', false)
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'workflows.integration.get',
        globalOptions: g,
        id,
        raw: opts['raw'],
      } satisfies WorkflowsIntegrationGetCommand;
    });
  cmd_workflows_integration.command('update')
    .description('Update integration')
    .argument('<id>', 'Integration ID')
    .option('--options <json>', 'New options')
    .option('--state <state>', 'New state')
    .option('--connection <id>', 'New connection ID')
    .option('--from-file <file>', 'Load from file')
    .option('--raw', 'Output raw JSON', false)
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'workflows.integration.update',
        globalOptions: g,
        id,
        options: opts['options'],
        state: opts['state'],
        connection: opts['connection'],
        fromFile: opts['fromFile'],
        raw: opts['raw'],
      } satisfies WorkflowsIntegrationUpdateCommand;
    });
  cmd_workflows_integration.command('delete')
    .description('Delete integration')
    .argument('<id>', 'Integration ID')
    .option('--raw', 'Output raw JSON', false)
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'workflows.integration.delete',
        globalOptions: g,
        id,
        raw: opts['raw'],
      } satisfies WorkflowsIntegrationDeleteCommand;
    });
  const cmd_workflows_alert = cmd_workflows.command('alert');
  cmd_workflows_alert.description('Manage workflow alerts');
  cmd_workflows_alert.command('list')
    .description('List workflow alerts')
    .option('--raw', 'Output raw JSON', false)
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'workflows.alert.list',
        globalOptions: g,
        raw: opts['raw'],
      } satisfies WorkflowsAlertListCommand;
    });
  cmd_workflows_alert.command('get')
    .description('Get workflow alert')
    .argument('<id>', 'Alert ID')
    .option('--raw', 'Output raw JSON', false)
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'workflows.alert.get',
        globalOptions: g,
        id,
        raw: opts['raw'],
      } satisfies WorkflowsAlertGetCommand;
    });
  cmd_workflows_alert.command('create')
    .description('Create workflow alert')
    .option('--project <project>', 'Project name')
    .option('--name <name>', 'Alert name')
    .option('--condition.name <type>', 'Condition type')
    .option('--frequency <ms>', 'Frequency in ms')
    .option('--execution-delay <ms>', 'Delay in ms')
    .addOption(new Option('--state <state>', 'Initial state to set for the integration').choices(["enabled","disabled","stopped"]))
    .option('--filter <filter>', 'Filter expression')
    .option('--integration <id>', 'Integration ID')
    .option('--from-file <file>', 'Load from file')
    .option('--raw', 'Output raw JSON', false)
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      result = {
        kind: 'workflows.alert.create',
        globalOptions: g,
        project: g.project,
        name: opts['name'],
        conditionName: opts['conditionName'],
        frequency: opts['frequency'],
        executionDelay: opts['executionDelay'],
        state: opts['state'],
        filter: opts['filter'],
        integration: opts['integration'],
        fromFile: opts['fromFile'],
        raw: opts['raw'],
      } satisfies WorkflowsAlertCreateCommand;
    });
  cmd_workflows_alert.command('update')
    .description('Update workflow alert')
    .argument('<id>', 'Alert ID')
    .option('--name <name>', 'New name')
    .option('--from-file <file>', 'Load from file')
    .option('--raw', 'Output raw JSON', false)
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'workflows.alert.update',
        globalOptions: g,
        id,
        name: opts['name'],
        fromFile: opts['fromFile'],
        raw: opts['raw'],
      } satisfies WorkflowsAlertUpdateCommand;
    });
  cmd_workflows_alert.command('delete')
    .description('Delete workflow alert')
    .argument('<id>', 'Alert ID')
    .option('--raw', 'Output raw JSON', false)
    .action((id, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'workflows.alert.delete',
        globalOptions: g,
        id,
        raw: opts['raw'],
      } satisfies WorkflowsAlertDeleteCommand;
    });

  // --- actions ---
  const cmd_actions = program.command('actions');
  cmd_actions.description('Manage actions configuration');
  cmd_actions.command('get')
    .description('Display actions config')
    .option('--universe <universe>', 'Universe name')
    .option('--project <project>', 'Project name')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      result = {
        kind: 'actions.get',
        globalOptions: g,
        universe: g.universe,
        project: g.project,
      } satisfies ActionsGetCommand;
    });
  cmd_actions.command('disable')
    .description('Disable actions')
    .option('--universe <universe>', 'Universe name')
    .option('--project <project>', 'Project name')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      result = {
        kind: 'actions.disable',
        globalOptions: g,
        universe: g.universe,
        project: g.project,
      } satisfies ActionsDisableCommand;
    });
  cmd_actions.command('enable')
    .description('Enable actions')
    .option('--universe <universe>', 'Universe name')
    .option('--project <project>', 'Project name')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      result = {
        kind: 'actions.enable',
        globalOptions: g,
        universe: g.universe,
        project: g.project,
      } satisfies ActionsEnableCommand;
    });
  cmd_actions.command('upload')
    .description('Upload actions config')
    .argument('<path>', 'Config file path')
    .option('--universe <universe>', 'Universe name')
    .option('--project <project>', 'Project name')
    .action((path, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      result = {
        kind: 'actions.upload',
        globalOptions: g,
        path,
        universe: g.universe,
        project: g.project,
      } satisfies ActionsUploadCommand;
    });
  cmd_actions.command('delete')
    .description('Delete actions config')
    .option('--universe <universe>', 'Universe name')
    .option('--project <project>', 'Project name')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      result = {
        kind: 'actions.delete',
        globalOptions: g,
        universe: g.universe,
        project: g.project,
      } satisfies ActionsDeleteCommand;
    });

  // --- attribute ---
  const cmd_attribute = program.command('attribute');
  cmd_attribute.description('Manage project attributes');
  cmd_attribute.command('create')
    .description('Create attribute')
    .argument('<project>', 'Project name')
    .argument('<name>', 'Attribute name')
    .option('--description <desc>', 'Attribute description')
    .addOption(new Option('--type <type>', 'Attribute value type').choices(["bitmap","uint8","uint16","uint32","uint64","uint128","uuid","dictionary"]))
    .addOption(new Option('--format <format>', 'Attribute display format').choices(["none","commit","semver","callstack","hostname","bytes","kilobytes","gigabytes","nanoseconds","milliseconds","seconds","unix_timestamp","js_timestamp","gps_timestamp","memory_address","labels","sha256","uuid","ipv4","ipv6"]))
    .action((project, name, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'attribute.create',
        globalOptions: g,
        project,
        name,
        description: opts['description'],
        type: opts['type'],
        format: opts['format'],
      } satisfies AttributeCreateCommand;
    });
  cmd_attribute.command('delete')
    .description('Delete attribute')
    .argument('<project>', 'Project name')
    .argument('<name>', 'Attribute name')
    .action((project, name, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'attribute.delete',
        globalOptions: g,
        project,
        name,
      } satisfies AttributeDeleteCommand;
    });

  // --- view ---
  const cmd_view = program.command('view');
  cmd_view.description('Manage query views');
  cmd_view.command('create')
    .description('Create view')
    .argument('<project>', 'Project name')
    .argument('<name>', 'View name')
    .option('--queries <file>', 'Queries JSON file')
    .option('--payload <file>', 'Payload JSON file')
    .action((project, name, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'view.create',
        globalOptions: g,
        project,
        name,
        queries: opts['queries'],
        payload: opts['payload'],
      } satisfies ViewCreateCommand;
    });
  cmd_view.command('delete')
    .description('Delete view')
    .argument('<project>', 'Project name')
    .argument('<name>', 'View name')
    .action((project, name, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'view.delete',
        globalOptions: g,
        project,
        name,
      } satisfies ViewDeleteCommand;
    });

  // --- error ---
  program.command('error', { hidden: !revealHidden })
    .description('Throw test error')
    .argument('<message>', 'Error message')
    .action((message, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'error',
        globalOptions: g,
        message,
      } satisfies ErrorCommand;
    });

  // --- log ---
  const cmd_log = program.command('log', { hidden: !revealHidden });
  cmd_log.description('Managed log operations');
  cmd_log.command('list', { hidden: !revealHidden })
    .description('List available logs')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'log.list',
        globalOptions: g,
      } satisfies LogListCommand;
    });
  cmd_log.command('activate', { hidden: !revealHidden })
    .description('Activate a log')
    .argument('<name>', 'Log name')
    .action((name, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'log.activate',
        globalOptions: g,
        name,
      } satisfies LogActivateCommand;
    });
  cmd_log.command('deactivate', { hidden: !revealHidden })
    .description('Deactivate a log')
    .argument('<name>', 'Log name')
    .action((name, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'log.deactivate',
        globalOptions: g,
        name,
      } satisfies LogDeactivateCommand;
    });
  cmd_log.command('extract', { hidden: !revealHidden })
    .description('Extract log entries')
    .argument('<name>', 'Log name')
    .option('--json', 'Output JSON format', false)
    .option('--table', 'Output as table', false)
    .action((name, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'log.extract',
        globalOptions: g,
        name,
        json: opts['json'],
        table: opts['table'],
      } satisfies LogExtractCommand;
    });

  // --- audit ---
  const cmd_audit = program.command('audit', { hidden: !revealHidden });
  cmd_audit.description('Audit log operations');
  cmd_audit.command('extract', { hidden: !revealHidden })
    .description('Extract audit log')
    .option('--json', 'Output JSON format', false)
    .option('--table', 'Output as table', false)
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'audit.extract',
        globalOptions: g,
        json: opts['json'],
        table: opts['table'],
      } satisfies AuditExtractCommand;
    });

  // --- bpg ---
  const cmd_bpg = program.command('bpg', { hidden: !revealHidden });
  cmd_bpg.description('BPG operations');
  cmd_bpg.command('list', { hidden: !revealHidden })
    .description('List BPG objects of a given type')
    .argument('<type>', 'BPG type (e.g. configuration/project)')
    .option('--raw <json>', 'Raw BPG JSON request')
    .action((type, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'bpg.list',
        globalOptions: g,
        type,
        raw: opts['raw'],
      } satisfies BpgListCommand;
    });

  // --- control ---
  program.command('control', { hidden: !revealHidden })
    .description('Trigger control operations')
    .option('--smr', 'Trigger grace period', false)
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'control',
        globalOptions: g,
        smr: opts['smr'],
      } satisfies ControlCommand;
    });

  // --- deduplication ---
  const cmd_deduplication = program.command('deduplication', { hidden: !revealHidden });
  cmd_deduplication.description('Manage deduplication rules');
  cmd_deduplication.command('add', { hidden: !revealHidden })
    .description('Add deduplication rules')
    .argument('<project>', 'Universe/project')
    .option('--name <name>', 'Rule name')
    .option('--rules <file>', 'Rules JSON file')
    .option('--priority <n>', 'Rule priority')
    .option('--platform <platform>', 'Platform filter')
    .addOption(new Option('--owner <uid>', 'Owner user ID').hideHelp(!revealHidden))
    .action((project, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'deduplication.add',
        globalOptions: g,
        project,
        name: opts['name'],
        rules: opts['rules'],
        priority: opts['priority'],
        platform: opts['platform'],
        owner: opts['owner'],
      } satisfies DeduplicationAddCommand;
    });
  cmd_deduplication.command('delete', { hidden: !revealHidden })
    .description('Delete deduplication rule')
    .argument('<project>', 'Universe/project')
    .option('--name <name>', 'Rule name')
    .action((project, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'deduplication.delete',
        globalOptions: g,
        project,
        name: opts['name'],
      } satisfies DeduplicationDeleteCommand;
    });
  cmd_deduplication.command('modify', { hidden: !revealHidden })
    .description('Modify deduplication rule')
    .argument('<project>', 'Universe/project')
    .option('--name <name>', 'Rule name')
    .option('--rules <file>', 'Rules JSON file')
    .option('--priority <n>', 'Rule priority')
    .action((project, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'deduplication.modify',
        globalOptions: g,
        project,
        name: opts['name'],
        rules: opts['rules'],
        priority: opts['priority'],
      } satisfies DeduplicationModifyCommand;
    });
  cmd_deduplication.command('list', { hidden: !revealHidden })
    .description('List deduplication rules')
    .argument('<project>', 'Universe/project')
    .option('--name <name>', 'Filter by rule name')
    .option('--verbose', 'Show detailed rule info', false)
    .action((project, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'deduplication.list',
        globalOptions: g,
        project,
        name: opts['name'],
        verbose: opts['verbose'],
      } satisfies DeduplicationListCommand;
    });

  // --- latency ---
  const cmd_latency = program.command('latency', { hidden: !revealHidden });
  cmd_latency.description('Latency histogram testing');
  cmd_latency.command('list', { hidden: !revealHidden })
    .description('List available histograms')
    .argument('[pattern]', 'Regex filter')
    .action((pattern, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'latency.list',
        globalOptions: g,
        pattern,
      } satisfies LatencyListCommand;
    });
  cmd_latency.command('activate', { hidden: !revealHidden })
    .description('Activate histogram')
    .argument('<name>', 'Histogram name')
    .option('--samples <n>', 'Number of samples')
    .action((name, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'latency.activate',
        globalOptions: g,
        name,
        samples: opts['samples'],
      } satisfies LatencyActivateCommand;
    });
  cmd_latency.command('deactivate', { hidden: !revealHidden })
    .description('Deactivate histogram')
    .argument('<name>', 'Histogram name')
    .action((name, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'latency.deactivate',
        globalOptions: g,
        name,
      } satisfies LatencyDeactivateCommand;
    });
  cmd_latency.command('extract', { hidden: !revealHidden })
    .description('Extract histogram data')
    .argument('<name>', 'Histogram name')
    .option('--raw', 'Output raw values', false)
    .action((name, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'latency.extract',
        globalOptions: g,
        name,
        raw: opts['raw'],
      } satisfies LatencyExtractCommand;
    });

  // --- cts ---
  program.command('cts', { hidden: !revealHidden })
    .description('Set marker for uniquely introduced issues')
    .argument('<project>', 'Project name')
    .argument('<attribute>', 'Attribute name')
    .argument('<value>', 'Target value')
    .option('--query', 'Show query JSON without executing', false)
    .action((project, attribute, value, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      syncGlobalAndCommandFlags(g, opts);
      if (project !== undefined) { g.project = project; }
      result = {
        kind: 'cts',
        globalOptions: g,
        project,
        attribute,
        value,
        query: opts['query'],
      } satisfies CtsCommand;
    });

  // --- session ---
  const cmd_session = program.command('session', { hidden: !revealHidden });
  cmd_session.description('Manage session resource overrides');
  cmd_session.command('list', { hidden: !revealHidden })
    .description('List active sessions')
    .addOption(new Option('--scope <scope>', 'Session scope').choices(["global","user","session"]))
    .option('-g', 'Shorthand for --scope=global', false)
    .option('-u', 'Shorthand for --scope=user', false)
    .option('-s', 'Shorthand for --scope=session', false)
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'session.list',
        globalOptions: g,
        scope: opts['scope'],
        g: opts['g'],
        u: opts['u'],
        s: opts['s'],
      } satisfies SessionListCommand;
    });
  cmd_session.command('set', { hidden: !revealHidden })
    .description('Set resource override values')
    .argument('<resources_json>', 'JSON object of resources')
    .addOption(new Option('--persist <scope>', 'Persist to configuration').choices(["universe","user"]))
    .option('--uid <uid>', 'User ID for persistence')
    .action((resources_json, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'session.set',
        globalOptions: g,
        resources_json,
        persist: opts['persist'],
        uid: opts['uid'],
      } satisfies SessionSetCommand;
    });
  cmd_session.command('unset', { hidden: !revealHidden })
    .description('Unset resource override values')
    .argument('<resource_names...>', 'Resource names to unset')
    .action((resource_names, opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'session.unset',
        globalOptions: g,
        resource_names,
      } satisfies SessionUnsetCommand;
    });

  // --- limit ---
  const cmd_limit = program.command('limit', { hidden: !revealHidden });
  cmd_limit.description('Manage submission limits');
  cmd_limit.command('list', { hidden: !revealHidden })
    .description('List current limits')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'limit.list',
        globalOptions: g,
      } satisfies LimitListCommand;
    });
  cmd_limit.command('reset', { hidden: !revealHidden })
    .description('Reset limits for a universe')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'limit.reset',
        globalOptions: g,
      } satisfies LimitResetCommand;
    });
  cmd_limit.command('delete', { hidden: !revealHidden })
    .description('Delete limits for a universe')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'limit.delete',
        globalOptions: g,
      } satisfies LimitDeleteCommand;
    });
  cmd_limit.command('create', { hidden: !revealHidden })
    .description('Create submission limit')
    .option('--submissions <n>', 'Submission limit')
    .option('--metadata <json>', 'Metadata JSON')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'limit.create',
        globalOptions: g,
        submissions: opts['submissions'],
        metadata: opts['metadata'],
      } satisfies LimitCreateCommand;
    });

  // --- service ---
  const cmd_service = program.command('service', { hidden: !revealHidden });
  cmd_service.description('Service layer operations');
  cmd_service.command('list', { hidden: !revealHidden })
    .description('List services')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'service.list',
        globalOptions: g,
      } satisfies ServiceListCommand;
    });
  cmd_service.command('status', { hidden: !revealHidden })
    .description('Get service status')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'service.status',
        globalOptions: g,
      } satisfies ServiceStatusCommand;
    });
  cmd_service.command('rescan', { hidden: !revealHidden })
    .description('Rescan services')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'service.rescan',
        globalOptions: g,
      } satisfies ServiceRescanCommand;
    });

  // --- status ---
  const cmd_status = program.command('status', { hidden: !revealHidden });
  cmd_status.description('Server status operations');
  cmd_status.command('reload', { hidden: !revealHidden })
    .description('Get reload status')
    .action((opts, cmd) => {
      const g = extractGlobalOptions(cmd);
      result = {
        kind: 'status.reload',
        globalOptions: g,
      } satisfies StatusReloadCommand;
    });

  return { program, getResult: () => result };
}
